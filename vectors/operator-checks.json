{"source":{"conformance":"01681aee5fe61f1fcac4b86eb5b958b4a697ba68365d04c2fbb7de02b268b8ea","index":"4312cd9f6288d710a8952b8e25b8fd7d8f331d4fe8ede4202ea0fb518085c801"},"checks":["#115 F.5r-d recovery counts the adopted frame — a head that moved without the counter under-claims the stream by one","#115 F.5r-d recovery moves the WHOLE GROUP — the interval bound follows the adopted document, not only the head","#122 ADVERSARIAL F.5r-c gap: a SECOND instance emitting a gap record from the head it observed is REFUSED — a gap record extends the chain, so it forks like any append","#122 ADVERSARIAL F.5r-c resume: resuming to a head the store does NOT hold is REFUSED — that difference is another writer, and overwriting it CAUSES the fork","#122 ADVERSARIAL F.5r-d recordFrame: a REFUSED frame moves NOTHING — the guard runs first, so a stream that correctly refused to fork does not count a frame it never emitted","#122 ADVERSARIAL F.5r-e a store with no `del` is REFUSED rather than silently leaving the interval open — measured in production: the empty-string write was rejected 400, the seal reported success, and the next hour would have claimed bounds beginning before itself","#122 ADVERSARIAL advanceHead with cas: the loser of a concurrent write is REFUSED and must not publish","#122 ADVERSARIAL advanceHead: extending a head we did NOT observe is REFUSED — that is the fork, named before it is published","#122 ADVERSARIAL: a second appender on a shared head is REFUSED — the fork is named, not produced","#122 F.5r-a Tiers: a PREVENTING store survives the per-tier namespace — dropping cas in the wrapper would silently downgrade the guarantee the stream then honestly reports","#122 F.5r-a Tiers: a tier stream writes into the OPERATOR's store, not into a private map","#122 F.5r-a Tiers: the two tiers hold DIFFERENT heads under DIFFERENT keys — one key would present one stream's head as another's","#122 F.5r-c CONTROL: the roster reader actually resolves enclosing names — against a planted unguarded writer it names the offender, so a green roster is not a parse failure","#122 F.5r-c gap: the FIRST gap record from the observed head is accepted","#122 F.5r-c gap: the store holds the head of the accepted record, so the object and the store never disagree","#122 F.5r-c resume: on an UNSEEDED store the operator claim stands — nobody has written, so there is nothing to contradict","#122 F.5r-c resume: resuming to the head the store DOES hold is accepted — the operator and the store agree","#122 F.5r-c resume: the refused resume left the stored head UNCHANGED","#122 F.5r-c the head-key WRITER ROSTER, enumerated from source, reduces to the guard — a partly guarded layer forks exactly like an unguarded one","#122 F.5r-d loadStreamState reads the whole group back — an operator sealing an interval needs the count and the checkpoint head, and reading them one key at a time is where an operator invents its own names","#122 F.5r-d recordCheckpoint: the sealed interval is CLOSED IN THE STORE — this reset lived only in the object, so a stream resumed elsewhere read the PREVIOUS interval's start and would have sealed the next hour with bounds that begin before it","#122 F.5r-d recordFrame USES store.incr when the store offers it — the count is a read-modify-write, and taking the atomic primitive is not optional politeness","#122 F.5r-d recordFrame: one call moves head, count and the interval start — a caller cannot advance one and forget another","#122 F.5r-d recordFrame: the interval START holds while its END follows the frames — the bounds are what was WRITTEN, not what a grid predicted","#122 F.5r-d the next interval opens at the FIRST frame after the seal, and the count stays CUMULATIVE across the boundary","#122 F.5r-e recordCheckpoint CLEARS by deleting, not by writing a sentinel — the key is GONE, not holding an empty string a substrate may refuse to store","#122 a stream resumed from the store continues THE SAME chain in another object","#122 advanceHead with cas reports PREVENTED — the stronger guarantee, and only when the store gives it","#122 advanceHead: a missing next is refused rather than storing nothing under the head","#122 advanceHead: an UNSEEDED store accepts the first head when the caller DECLARES it — nobody has written yet, which is not a disagreement","#122 advanceHead: extending the head we observed is accepted","#122 advanceHead: the refused write left the store UNCHANGED — a refusal that still wrote would be worse than none","#122 frames are available to the caller AT the checkpoint and cleared by the next interval first frame","#122 the guarantee is named honestly: get/set yields detected, never prevented","#122 with store.cas the layer reports prevented — the stronger guarantee, and only when it holds it","#124 ADVERSARIAL F.5r-f a head belonging to NEITHER our observation nor our emission is REFUSED — that is another writer extending this stream, and publishing into it is the fork","#124 ADVERSARIAL F.5r-g a document that NEITHER is nor extends the stored head is REFUSED — adopting it could chain the next frame beneath another writer's live branch, which is the fork this prevents","#124 ADVERSARIAL F.5r-h an empty head is REFUSED when the caller did not declare an unseeded stream — a port that answers \"absent\" on a failed read would otherwise disarm the guard, because null reads as \"nobody has written yet\" and the write proceeds","#124 F.5r-f ADVERSARIAL the lost advance is REPAIRED, not repeated — publish succeeded, the head write did not, and the next interval must continue from what was PUBLISHED","#124 F.5r-f END TO END reconciling before the next interval REPAIRS the head to the published document, so the next frame extends it instead of forking it","#124 F.5r-f END TO END the store still holds the OLD head after the dropped write — this is the state in which a writer forks itself","#124 F.5r-f on an UNSEEDED store the emission stands — nobody has written, so there is nothing our advance could have raced","#124 F.5r-f reconcileHead with no last emission answers UNVERIFIED — a fresh process cannot rule this out from its own information, and saying so beats assuming","#124 F.5r-f reconcileHead: the advance DID land — already-advanced, and nothing is written twice","#124 F.5r-f the refusal left the stored head UNCHANGED — a refusal that still wrote would take the stream from its rightful writer","#124 F.5r-f the repair is IDEMPOTENT — re-asserting names the same successor of the same predecessor, so a retry is not a second advance","#124 F.5r-g ADVERSARIAL the published document EXTENDS the stored head — `prev` is the proof, carried in the document, that this is the successor, so the head is RECOVERED","#124 F.5r-g END TO END after recovery the next frame extends the PUBLISHED document, not the one the stale pointer named","#124 F.5r-g an EMPTY pointer adopts the published head — nothing was ever recorded, so there is nothing to contradict","#124 F.5r-g the pointer already names the published head — CONSISTENT, and nothing is written","#124 F.5r-g the refusal left the stored head UNCHANGED — a recovery that overwrites on disagreement takes the stream from whoever holds it","#124 F.5r-g with NO document the answer is UNVERIFIED, not a guess — reading the published set is the operator's capability, and a layer that faked it would assert what it cannot observe","#124 F.5r-g.1 every state the layer RETURNS is in the declared set — a returned value outside it would be a fifth vocabulary nobody agreed to","#124 F.5r-g.1 the outcome vocabulary is DECLARED BY THE LAYER and total — an operator that has to invent a word for one of these cases makes its telemetry incomparable with every other operator's","#124 F.5r-h the refusal wrote NOTHING — a guard that refuses and stores anyway leaves the lie in place","AnchorBatch-wrong-leaf→null","AnchorBatch→verifyAnchor-inclusion","KeyLog→authoritative","Layers:assemble-valid","Layers:missing-subordinate→seed-mismatch","Stream→provisional-no-checkpoint","Stream→verifyStream-chain-consistent","Substrate:<6conf→unproven","Substrate:bitcoin-ots-≥6conf→anchored","Tiers:continuation-after-gap","Tiers:separate-prev-streams","Tiers:signed-gap-record","differential:checkpoint-bytes-match-hardened","differential:first-checkpoint-roots-in-genesis","differential:interval-is-observed","sealTree:120-seals-at-depth-2","sealTree:an-hour-at-second-resolution-seals-at-depth-2 (3600 → 57 nodes)","sealTree:at-the-law-stays-flat","sealTree:every-node-and-the-root-verify","sealTree:refuses-an-empty-set-rather-than-sealing-nothing","walkChain-depth1-verifies-referent"]}
