{
  "name": "@ust-protocol/operator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The STATEFUL UST operator layer — chains, key logs, checkpoints, anchor batches. Builds on ust-protocol (the stateless base) and produces exactly what it verifies. NOTE: the bare name `@ust-protocol/operator` on npm belongs to a third party (a state-management library) and must never be taken — this package lives under the scope we publish.",
  "main": "index.mjs",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.mjs",
      "default": "./index.mjs"
    }
  },
  "files": [
    "index.mjs",
    "index.d.ts",
    "README.md"
  ],
  "scripts": {
    "test": "node conformance.mjs"
  },
  "dependencies": {
    "ust-protocol": "^1.0.0-rc.45"
  },
  "license": "Apache-2.0"
}
