{
  "_": "Per-round accounting for the law math → spec → code → vector → test. Every layer is either a REFERENCE that must resolve, or an explicit exclusion WITH a reason. A skip is legitimate — rev92(d) has no formal-model content and forcing prose there would be the patch this repo rejects — but it must be a DECISION, not an absence. See thelabmd/UST-Protocol#99. Records are keyed by `rev` for the CLOSED reference-checker ladder (historical) or by `round` for the LIVE version-line counter. A round is accounted where the work is recorded, not where a finished audit arc used to be. BEFORE EXCLUDING THE `vector` LAYER, read `what_a_vector_pins` at the top of vectors/conformance-vectors.json. It is derived from the corpus, not declared: a vector is a FIXED INPUT and a DECLARED RESULT, and a document is only one of the input forms (measured 2026-08-10 — 40 of 228 vectors take a document; 75 take a value, 59 a genesis-plus-log, 27 raw bytes with no document at all). The layer may be excluded only when the subject has NO fixed input — a live network, a registry read, wall-clock time. \"There is no document to pin\" is NOT such a reason, and it was used 46 times here before anyone compared it with the corpus (UST#147).",
  "unaccounted_pin": 89,
  "_pin": "Rows in the ladder with no record here. May only ever SHRINK. 94 rows existed when the gate landed; the three most recent are accounted.",
  "records": [
    {
      "rev": "rev95",
      "math": "the property it tests must be one the ceremony itself",
      "spec": "A tool that performs a CEREMONY",
      "code": "packages/ust-cli/index.mjs :: cmdRotate self-check",
      "vector": {
        "excluded": "the rule governs a TOOL's ceremony self-check — what a CLI must verify before emitting. The corpus pins document bytes and the verdicts they must produce; a ceremony is not a document, and a vector could only restate the conformance check. The protocol FACT underneath it (a caller boolean never earns authoritative) IS pinned, in conformance and by rev94's seam grid."
      },
      "test": "rev95 a caller no-fork boolean never earns authoritative — the rung a ceremony self-check may not demand of it"
    },
    {
      "rev": "rev94",
      "math": "the decision is not a projection of either field; it is the CONJUNCTION of the two facts the model separates",
      "spec": "A candidate is authority-BOUND when its identity strength is `corroborated` or",
      "code": "packages/ust-protocol/index.mjs :: forkChoice bound predicate",
      "vector": "fc-02-suspect-key-reaches-predicate-and-is-refused",
      "test": "rev94 fork-choice binding == derived identity ≥ corroborated, over the WHOLE seam grid (24 states)"
    },
    {
      "rev": "rev93",
      "math": "a NAMED floor value means **the measurement ran and earned nothing**",
      "spec": "A slot a verifier could not fill has exactly TWO honest markers",
      "code": "packages/ust-protocol/index.mjs :: resolveAuthority no-evidence branch",
      "vector": {
        "excluded": "the rule governs the SHAPE of a verifier's own report — which marker fills an unfilled slot — and the vector corpus pins document bytes and the verdicts they must produce, not the report's field layout. A vector could only restate the conformance check in JSON."
      },
      "test": "rev93 empty slot: NAMED floor when measured (INVALID→tier NONE), ABSENT when unmeasurable (INDETERMINATE→no tier), and no basis either way"
    },
    {
      "rev": "rev92",
      "math": {
        "excluded": "the round's second half (argv parsing) has no formal-model content: 'a verdict is a function of its argument, so a substituted argument yields a true statement about the wrong object' is what a function IS, not a theorem. Writing model prose to satisfy a gate is the patch this repo rejects. The round's FIRST half (the axis closure) is modelled and cited under math."
      },
      "spec": {
        "excluded": "the axis split (a report may be wider than what it scores) governs attestDiscovery — a TOOL's own §20.1 conformance verdict, not a protocol verdict. The normative spec defines what the serving contract REQUIRES; which of a checker's probes are scored is that checker's business, and legislating it would put tool policy into the wire standard."
      },
      "code": "packages/ust-cli/index.mjs :: positionals + readLogFiles",
      "vector": {
        "excluded": "the defect is in a CLI's argument handling and a log reader's shape routing — neither is expressible as protocol vectors, which pin documents and verdicts. Pinned instead by stream-consumption-gate, which enumerates the flag sets and the served shapes."
      },
      "test": "rev92 completeness ⊥ tier: an undeclared grid bounds the RANGE and subtracts nothing from Π"
    },
    {
      "round": 76,
      "math": "round 76 completes `mutating(c)`: both classes, same conjunct, same shape",
      "spec": "Cadence mutation is ROOT-ONLY",
      "code": "packages/ust-protocol/index.mjs :: resolveCadenceBytes root conjunct",
      "vector": "cadr-09-nonroot-signer-rejected",
      "test": "#107 an OPERATIONAL key may NOT move the cadence grid — `active` is NECESSARY, not SUFFICIENT"
    },
    {
      "round": 77,
      "math": {
        "excluded": "This round changes where a round is RECORDED and how that record is enforced — repo bookkeeping, not the verifier. The formal model describes what a verifier may conclude from bytes; a table's closure is not a property of any document, and writing model prose to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, and no conformance obligation changed. A spec sentence here would describe our changelog to an implementer who will never read it."
      },
      "code": "tools/changelog-rev-gate.mjs :: LADDER_CLOSED_AT ceiling",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, so a second implementation can check itself. This round touches no document and no verdict; a vector could only restate a gate that already runs from the file it guards."
      },
      "test": {
        "excluded": "The test layer here is the gates' own negative controls — the closed-ladder ceiling reproduced against round 76's exact mistake, the live-coverage leg proven able to fail, and the line-marker checked against a deliberately wrong heading. They run under npm run test:changelog / test:ladder / the npm-drift gate, and are not conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 78,
      "math": "The correction is one enumeration read in BOTH directions rather than two lists that happen to agree today",
      "spec": "The role is a PARTITION of the class\n   registry, enforced in BOTH directions",
      "code": "packages/ust-protocol/index.mjs :: AUTHORITY_CLASSES role partition",
      "vector": "role-observation-in-key",
      "test": "#97/tlx a data class in the KEY role → E-MALFORMED (the partition is two-sided)"
    },
    {
      "round": 79,
      "math": "So role separation is a DECLARED refinement, the same shape as every other strength here",
      "spec": "Key ROLES — a DECLARED refinement, never a floor change",
      "code": "packages/ust-protocol/index.mjs :: resolveKeysBytes declared-role regime",
      "vector": "role-06-two-active-keys-distinct-roles",
      "test": "#106 under a DECLARED genesis an add with NO role → E-KEY (a missing field is never the strongest claim)"
    },
    {
      "round": 80,
      "math": {
        "excluded": "This round is about DISTRIBUTION: whether the bytes a registry serves can be loaded. The formal model states what a verifier may conclude from a document, and no document, verdict or admissibility rule moved. A packaging defect is invisible to a model that reasons about transcripts, and writing model prose to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. PORTING.md tells an independent implementation what it must REPRODUCE, and it must reproduce the checker, not our npm manifest — the defect was in how OUR artifact was assembled, not in what any implementation owes."
      },
      "code": "packages/ust-protocol/package.json :: files",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce so a second implementation can check itself against them. This round touches no document and no verdict; the artifact under test is a TARBALL, and the check that it loads has to run on a packed package rather than on a transcript."
      },
      "test": {
        "excluded": "The test layer here is tools/package-contents-gate.mjs and its own vacuity guards — it fails if no package was packed or if no packed file was resolved, and its recursive listing exists because a top-level-only probe found nothing in diarium/bin and would have passed silently. It runs as its own gate, not as a conformance check, because conformance covers the verifier and this covers what we ship."
      }
    },
    {
      "round": 81,
      "math": {
        "excluded": "This round renders a sealed entry into a second place and gates that the two agree. The formal model states what a verifier may conclude from a document; how this repository DISPLAYS one of its own signed documents is not a property of any transcript, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. An implementer conforming to UST owes nothing about our changelog, and a spec sentence here would describe our bookkeeping to a reader who will never need it."
      },
      "code": "tools/recap-render.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce so a second implementation can check itself. The artifact under test here is a MARKDOWN FILE compared against a store the vectors do not describe; a vector could only restate a gate that already runs from the files it guards."
      },
      "test": {
        "excluded": "The test layer is the gate's own negative controls, run rather than described: a hand-edited word inside a quote, a registry naming a content_hash no sealed entry carries, and the floor lowered to 74 so six undecided rounds are named. They run as npm run test:recap, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 82,
      "math": "`s` is an operator assertion, never a positional inference",
      "spec": "`s` MUST be a key the operator names, and MUST NOT be derived from position in the log",
      "code": "packages/ust-cli/index.mjs :: rotateKeylog supersedesKeyId",
      "vector": {
        "excluded": "role-07-supersedes-propagates-role ALREADY pins the document-level property this round is about: data added first, issuance second, supersedes naming the FIRST, successor expected to inherit data. The verifier was correct and covered; the defect was entirely producer-side, and no transcript can reveal whether its supersedes was chosen well or badly. A new vector would restate a covered rule."
      },
      "test": {
        "excluded": "The test layer is packages/ust-cli/regression.mjs — four checks pinning refusal on ambiguity, refusal of an inactive key_id, supersession of the NAMED key, and inheritance of its role, plus a non-vacuity leg asserting the fixture is one the OLD selection would have got wrong. They run under npm run test:cli, not as conformance checks, because conformance covers the verifier and this covers a ceremony tool."
      }
    },
    {
      "round": 83,
      "math": {
        "excluded": "This round decides WHERE a sealed document of ours is displayed and what unit earns one. The formal model states what a verifier may conclude from a transcript; the placement of our own diary entry is a property of no document, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, and no conformance obligation changed. An implementation conforming to UST owes nothing about where we publish a diary entry."
      },
      "code": "tools/recap-render.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce so a second implementation can check itself. Nothing here touches a document or a verdict — the artifacts are a markdown file, a registry and a GitHub comment, none of which a vector describes."
      },
      "test": {
        "excluded": "The test layer is the gate's own legs, run rather than described: a re-added `#### round N — recap` block in CHANGELOG.md fails, a record with neither issue nor reason fails, a registry hash absent from the store fails by name, and the floor lowered names undecided rounds. They run as npm run test:recap, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 84,
      "math": {
        "excluded": "Every protocol layer was already correct here, and that is the finding. §F.5e.1 states the DECLARED regime and §F.5e.2 the admission criterion; the verifier refuses both errors a producer could make (an undeclared role is E-MALFORMED, a missing one under declaration is E-KEY), so a producer wanting valid output has no freedom left for the model to constrain. Unlike round 82, where the verifier accepts ANY active key and only the producer can go wrong, nothing here is under-determined."
      },
      "spec": {
        "excluded": "Same reason, and it is worth the contrast: round 82 needed a producer clause in §12.2 because the verifier could not tell a well-chosen `supersedes` from a badly chosen one. Here it can — both mistakes are already refused by rules the spec states — so a clause would restate what §12.2 already determines. What the spec's field list DOES gain is enforcement: keyop-contract-gate now binds it to the reducer in both directions."
      },
      "code": "packages/ust-cli/index.mjs :: addKeylogKey",
      "vector": {
        "excluded": "role-01 (undeclared genesis, plain add → active_count 2), role-02 (undeclared genesis + role → E-MALFORMED) and role-03 (declared genesis, add with no role → E-KEY) already pin all three cases of the rule this round moves into the core. The verifier was right and covered; the defect was a tool refusing what those vectors admit, which no transcript can express."
      },
      "test": {
        "excluded": "The test layer is packages/ust-cli/regression.mjs — five checks pinning a required role under declaration, refusal of an undeclared role name, refusal of a role under an undeclared genesis, the plain parallel add that had no path at all, and that it carries no role — plus keyop-contract-gate's two negative controls. They run under npm run test:cli and npm run test:keyop, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 85,
      "math": {
        "excluded": "This round grades our own assurance machinery. The formal model states what a verifier may conclude from a transcript; how strong each of OUR gates is, is not a property of any document, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. An independent implementation owes nothing about how we grade our own CI."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The artifacts here are a workflow file and the source of the gates themselves; no transcript and no verdict is involved, so a vector could only restate a gate that runs from the files it grades."
      },
      "test": {
        "excluded": "The test layer is the gate's four negative controls, run rather than described: a new CI step with no grade fails, an over-claimed 1a fails, a canfail:yes with no leg fails, a removed marker fails. They run as npm run test:assurance-map, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 86,
      "math": {
        "excluded": "This round registers and MEASURES string sets the code already uses. No admissibility rule, no verdict and no transition changed — the model states what a verifier may conclude, and a registry of the words it prints is bookkeeping over that. The one thing the measurement surfaced that IS model-adjacent, an error code in the verdict slot, is filed as #111 rather than settled here."
      },
      "spec": {
        "excluded": "Nothing normative moved: the §14 vocabulary is unchanged, and the sets added to REGISTRY are not among the regions gen-spec-registry renders into the spec, so no spec text changed. Whether the totality guard's shape should change is #111's first box, and changing it would be the round that moves this layer."
      },
      "code": "packages/ust-protocol/index.mjs :: REGISTRY results/completeness/forkChoiceResults",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. These sets are about which WORDS the implementation emits, measured from its source; a vector would exercise a verdict rather than enumerate the vocabulary. #111's last box asks for the vector that IS missing — one pinning the totality-guard verdict shape — and that belongs to the round that decides the shape."
      },
      "test": {
        "excluded": "The test layer is spec-code-sync's three MEASURED comparisons — results, completeness and forkChoiceResults against actual code usage — plus the negative control they already carry: a set that drifts fails by name. They run as npm run test:spec-sync, not as conformance checks, which cover the verifier only. A conformance check name was written here first, copied from round 79 to satisfy the layer; the gate accepted it because the name exists, which is precisely the accounting-from-memory failure this registry was built to stop."
      }
    },
    {
      "round": 87,
      "math": {
        "excluded": "This round records the trajectory of our own assurance machinery. The formal model states what a verifier may conclude from a transcript; how many of OUR gates are hand-typed this week is a property of no document, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. An implementation conforming to UST owes nothing about how we track our own CI over time."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The artifact here is a ledger of counts over the workflow file; no transcript and no verdict is involved, so a vector could only restate a check that runs from the files it counts."
      },
      "test": {
        "excluded": "The test layer is the verification leg itself, which is the whole mechanism rather than an addition to it: a plain run recomputes the distribution and fails if the last recorded row disagrees, so a hand-edited row and an unrecorded grade move both fail. It runs as npm run test:assurance-map, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 88,
      "math": {
        "excluded": "This round adds negative controls to one of our gates and corrects the sentence describing where its weakness is. No admissibility rule, verdict or transition moved; the formal model describes what a verifier may conclude from bytes, not how strong our own tooling is."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The clean-room parity requirement PORTING.md states is unchanged — what changed is whether our gate can be shown able to fail."
      },
      "code": "tools/primitive-parity-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, and the parity corpus already IS a set of inputs run through both implementations. A vector here would duplicate that corpus into a file without widening it, which is the appearance of coverage rather than coverage."
      },
      "test": {
        "excluded": "The test layer is the two controls themselves, and both were run: a ghost name in the entry-point expectation breaks the comparison, and a raised corpus floor fails by name. They run as npm run test:primitive-parity, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 89,
      "math": {
        "excluded": "A partition checked on one side is a shape this model already states for KEY ROLES (§F.5e.4, round 78), and the statement is general — the object here is our capability triage list, not a transcript. Restating the same rule about our own tooling would add a model sentence that no verifier can act on, which is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The capability stance the spec derives is unchanged — what changed is whether OUR gate notices a triage entry whose subject is gone."
      },
      "code": "tools/capability-parity.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The artifact here is a triage list over the core's export table; no transcript and no verdict is involved, and the export table is read live rather than fixtured."
      },
      "test": {
        "excluded": "The test layer is the four controls, each run: a name that cannot exist must be absent from the live export set, the reverse detector must flag an unexported triaged name, a deliberately dead PRIMITIVES entry must fail, and a reason under the floor must fail. They run as npm run test:parity, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 90,
      "math": {
        "excluded": "The display rule is about what a HUMAN is shown, not about what a verifier may conclude — a document's verdict is identical whether or not the renderer neutralizes format characters. The model states admissibility and verdicts; a rendering obligation has no term in it, which is why #73 was a surface gate from the start."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently and no verifier accepts or refuses differently. The A-label homograph guard this round PINS is already normative (§4.3a) — what changed is that our display gate now asserts its own scope premise instead of arguing it in a comment."
      },
      "code": "tools/display-safety-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The A-label refusal this round leans on is already covered by the identity-hardening vectors from #40; what is new is a gate reading that refusal to justify its own scope, which no vector can express."
      },
      "test": {
        "excluded": "The test layer is three controls, each run: a synthetic DOM-writing file must fail until it carries the marker, the DOM-write detector must fire on a writer and not on inert text, and the premise leg must fire when the A-label guard is weakened — which is how the leg's own vacuity was found. They run as npm run test:display, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 91,
      "math": {
        "excluded": "A retired mechanism is a fact about this repository's history, not about what a verifier may conclude from a document. §F.5e.0 already states WHY the `rotate` transition is absent — the model carries the reasoning; this gate carries the guard that it stays absent, and those are different layers by design."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The retirements themselves are already normative where they belong — §12.2 states there is deliberately no `rotate` transition."
      },
      "code": "tools/retired-mechanisms-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, and the `rotate` refusal already has one from round 74 asserting the REASON and not merely the refusal. What this round adds is a guard over our own shipped sources, which no transcript can express."
      },
      "test": {
        "excluded": "The test layer is three controls, each run: `op:\"rotate\"` restored to the MCP description fails (the exact three-round miss, reproduced), an exemption that is no longer needed fails, and a blind roster or over-stripped source fails as vacuous. They run as npm run test:retired, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 92,
      "math": {
        "excluded": "Which English word names two mechanisms is a fact about our prose, not about what a verifier may conclude from a document. The model already carries the qualified pair everywhere it reasons (rounds 72, 75); a term-collision registry over our own text has no term in it."
      },
      "spec": {
        "excluded": "Nothing normative moved: the spec's qualification is already done and its residual pin is unchanged. What #112 raises IS spec-adjacent — a public export name — but renaming a published export is a breaking decision, and the round that decides it is the round that moves this layer."
      },
      "code": "tools/ambiguous-term-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A word's ambiguity in prose produces no document and changes no verdict; the wire tags that carry these concepts were never ambiguous and are already pinned by the vectors that use them."
      },
      "test": {
        "excluded": "The test layer is three controls run against a synthetic line so they cannot drift with the prose they guard: a bare word must count, a qualified one must not, an absent one must not. Verified by replacing the qualifier with a catch-all, which makes every use look qualified, and they fire. They run as npm run test:ambiguous-terms, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 93,
      "math": {
        "excluded": "This round changes what counts as evidence that OUR gates can fail. The formal model states what a verifier may conclude from a document; where a negative control lives is a property of our workflow, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. An independent implementation owes nothing about how we evidence our own controls."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The artifacts here are marker lines and a workflow command; the adversary the citations point AT is the mutation applicator, which is itself the vector-adjacent mechanism and already registered."
      },
      "test": {
        "excluded": "The test layer is three controls, each run: a citation to a file that does not exist fails, a citation to a file that itself cites fails, and removing `git diff --exit-code` from a script puts its step back in the weakest quadrant. They run as npm run test:assurance-map, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 94,
      "math": {
        "excluded": "These two gates read our documents and our changelog. The formal model states what a verifier may concludes from a transcript; whether OUR heading scanner can find a heading is not a property of any document, and model prose written to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. What moved is whether two of our own readers can be shown able to fail."
      },
      "code": "tools/changelog-rev-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The inputs here are synthetic strings inside the gates themselves, chosen so they cannot drift with the prose being guarded; a vector would move the control back into the document it is meant to be independent of."
      },
      "test": {
        "excluded": "The test layer is the ten synthetic legs plus two controls-of-the-controls, each run: blinding the heading scanner fails, and breaking the ceiling comparison fails. They run as npm run test:model-domain and npm run test:changelog, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 95,
      "math": {
        "excluded": "This round gives our own suites adversaries. The formal model states what a verifier may conclude from a transcript; whether OUR SSRF test notices a broken guard is not a property of any document, and the mutations themselves are already the model-adjacent mechanism, registered in the corpus the model's lockstep guard reads."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The three rules the new mutants break are already normative (§12.2 roles, §F.5e.3 cadence, and the SSRF refusal PORTING.md requires); what changed is that breaking them now fails a suite."
      },
      "code": "tools/mutations.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, and the two protocol rules here already have theirs from rounds 76 and 84. What this round adds is an adversary for suites that are not verifiers — an SSRF door and a CLI ceremony — which no transcript can express."
      },
      "test": {
        "excluded": "The test layer is the drift-guards observer itself, run: it injects each of the three new mutations and requires the named suite to exit non-zero, and it rejected one of them until the assertion it exposed was strengthened. It runs as npm run test:drift-guards, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 96,
      "math": {
        "excluded": "The three rules these mutants break are already stated in the model where they belong — §12.3's sequence rule and #42's consumer-rooted admission. What this round adds is an adversary for suites that observe them, which is a property of our tooling and not of any transcript."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The diary cap is our own rules.md and normative for nobody else."
      },
      "code": "tools/mutations.mjs",
      "vector": {
        "excluded": "Two of the three rules already have their vectors — `ac-sequence-skip` in the arc corpus is precisely what the sequence mutant makes pass wrongly, which is why that mutant is observable at all. The third is a cap in our own diary, which no conformance vector describes."
      },
      "test": {
        "excluded": "The test layer is the drift-guards observer, run: it injects all 14 gated mutations and requires each named gate to exit non-zero. It runs as npm run test:drift-guards, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 97,
      "math": {
        "excluded": "The rule vocabulary the fuzzer now derives is the reference checker's own, and the model already binds every rule it cites to an executed check. Where the fuzzer READS that vocabulary from is a property of our tooling, not of any transcript."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. PORTING.md requires an independent implementation to reproduce the checker's rules, and that requirement is unchanged — what changed is that our own fuzzer reads them instead of copying them."
      },
      "code": "packages/ust-protocol/reference-checker.fuzz.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The fuzzer's inputs are generated from a seed and are deliberately not a corpus; pinning them as vectors would freeze the one thing about it that is meant to be generated."
      },
      "test": {
        "excluded": "The test layer is two controls in the fuzzer itself, run: every live checker rule must be present in the pool, or the fuzzer exercises a subset while reporting a run over the whole; and every adversarial non-rule must carry a reason over the floor. It runs as npm run test:reference-checker-fuzz, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 98,
      "math": {
        "excluded": "Which CLI commands take positional arguments is a property of our tool's surface, not of what a verifier may conclude from a transcript. The stream verdicts these commands produce are already bound by the model where they belong."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. §11.3's stream semantics are unchanged — what changed is whether our gate notices a new command that parses positionals."
      },
      "code": "tools/stream-consumption-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The artifact here is argv handling in a command-line tool; the flag-parsing behaviour is already exercised by this gate's own positionals() legs, which are unit assertions rather than transcripts."
      },
      "test": {
        "excluded": "The test layer is the new coverage leg plus two controls, run: removing a command's row makes the gate name it, and the scan is asserted to find at least two commands and to reject a name that cannot exist. They run as npm run test:consumption, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 99,
      "math": {
        "excluded": "Whether our ceremony tool reaches the network before its air-gapped half returns is a property of that tool, not of what a verifier may conclude from a transcript. The model states what a genesis document must satisfy; how the operator produced it offline is outside every term in it."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. §20.1's serving contract and the ceremony's own obligations are unchanged — what changed is how much of the offline half is watched."
      },
      "code": "tools/offline-ceremony-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The property here is positional inside a command — that no network call precedes a return — which produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The test layer is the derivation's controls plus an injected breach, run: a fetch placed inside askHidden (one of the five that nothing watched before) is caught, the derivation must find at least six callees and must reject a name that cannot exist, and the extractor must fail if a body runs past a third of the file. They run as npm run test:offline, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 100,
      "math": {
        "excluded": "The rung ladder and the one non-rung are already stated where they belong — §F.5.0's lattice and §12.1a's caller override — and this round changes neither. Which of OUR files gets scanned for them is a property of our tooling and has no term in the model."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. What changed is that the exemption must now cite the section that already makes it legal, which strengthens our gate rather than the spec."
      },
      "code": "tools/report-shape-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, and the verdict vocabulary itself is already pinned by REGISTRY and measured by spec-code-sync since round 86. This round is about which SOURCES are scanned for it, which no transcript expresses."
      },
      "test": {
        "excluded": "The test layer is two controls, both run: a probe file emitting an invented rung is caught by the derived roster, and an exemption whose reason drops its section citation fails. They run as npm run test:report-shape, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 101,
      "math": {
        "excluded": "The identity ladder this round derives from is already stated in §F.5.0's lattice, and what a ceremony may assert about it is rev95's realization note, which stands. The model says what a verifier concludes; which of OUR tool's lines may mention a rung is a property of the tool."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The rungs and their meanings are unchanged — what changed is that our gate reads them from the core instead of naming one."
      },
      "code": "tools/ceremony-selfcheck-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A ceremony self-check produces no document — it is a refusal inside a command — and the rungs it must not condition on are already pinned by the vectors that exercise them."
      },
      "test": {
        "excluded": "The test layer is the control, run: a self-check conditioning on `corroborated` is caught, which was silently legal before this round, plus two legs asserting the ladder derivation is in use and rejects a rung that does not exist. They run as npm run test:ceremony, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 102,
      "math": {
        "excluded": "This round defines what kind of thing a hand-typed literal is in OUR gates and regrades three of them. The formal model states what a verifier may conclude from a transcript; the taxonomy of our own tooling has no term in it, and writing one to satisfy a gate is the patch this repo rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The exclusion markers model-domain reads are already normative in the formal model and unchanged."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A grading criterion for our own gates produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The test layer is the citation control, run: an ATTEST_EXEMPT reason that drops its issue reference fails. The regrades themselves are checked continuously by the assurance map, which refuses a declaration its code cannot support. They run as npm run test:serving and npm run test:assurance-map, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 103,
      "math": "The criterion of §F.5e.2 governs the INPUT, and inverts on the OUTPUT",
      "spec": "the three OUTCOME KINDS of §15 (VALID / INVALID / INDETERMINATE)",
      "code": "packages/ust-protocol/index.mjs :: verifyAsync totality guard",
      "vector": "fc-01-impostor-not-canonical",
      "test": "round-19 P1-02 hostile accessor/Proxy args (5 boundaries) → structured E-MALFORMED, never a host throw",
      "_math_was": "§14's verdict vocabulary is already stated and unchanged — this round brings two returns INTO it rather than moving it. The forkChoice discriminator is a field the verifier does not act on and could not enter a strict allowlist under §F.5e.2; it is a producer-side aid to the reader, which the model has no term for.",
      "_vector_was": "The path is reached by passing a hostile Proxy as an argument, which is not a document and cannot be expressed as a byte vector. The pin is the round-19 P1-02 conformance check, which now asserts the shape it used to merely record — and #111's request for a vector is answered by naming why one cannot exist rather than by fabricating one."
    },
    {
      "round": 104,
      "math": {
        "excluded": "The two checkpoint mechanisms are already distinguished in the model — §11.3's stream point and §12.3's authority point are separate objects with separate theorems. What this round changes is the NAME an implementation exports for one of them, which is a producer-surface fact with no term in the model."
      },
      "spec": "§12.3 verification reuses the three OUTCOME KINDS of §15",
      "code": "packages/ust-protocol/index.mjs :: buildStreamCheckpoint",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. An alias produces byte-identical documents by construction — `buildCheckpoint === buildStreamCheckpoint` is asserted directly — so a vector would exercise the same bytes twice under two names."
      },
      "test": {
        "excluded": "The test layer is the export-surface leg and its two controls, run: the probe must see the known bare-named `buildCheckpoint`, and the code-aware qualifier must recognise `buildStreamCheckpoint` — without which the gate would demand a rename of a name that already says its chain. A new bare-named export raises the count and fails. They run as npm run test:ambiguous-terms, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 105,
      "math": {
        "excluded": "Fail-closed is a property the model already requires of the VERIFIER — §14 is normative, ordered and fail-closed. This round applies the same direction to our gates, which are not verifiers and have no term in the model; restating the rule about our tooling would be model prose written to satisfy a gate."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The spec already demands fail-closed of an implementation; what changed is that three of OUR gates now obey it too."
      },
      "code": "tools/version-truth-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The inputs here are a corrupt manifest, an unreadable file and an absent module — states of the filesystem, not transcripts, and each is exercised by breaking it and running the gate."
      },
      "test": {
        "excluded": "The test layer is the three controls, each run twice — before the fix all three gates PASSED with their input broken, after it all three fail by name. They run as npm run test:version-truth, test:retired and test:inclusion, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 106,
      "math": {
        "excluded": "Vacuity is a property the model already names — §F.9 — and this round applies it to our own gates rather than to a verifier's checks. The formal model states what a verifier may conclude from a transcript; whether OUR corpus was read is not a property of any document."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The documents the floors guard are unchanged — what changed is that emptying one now fails."
      },
      "code": "tools/vacuity-battery.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. The input here is an emptied source file, a state of the tree rather than a transcript, and it is exercised by emptying it and running the step."
      },
      "test": {
        "excluded": "The test layer is the two controls, each run twice — before the floors both gates PASSED on an emptied input, after them both fail. They run as npm run test:ambiguous-terms and test:vacuity, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 107,
      "math": {
        "excluded": "Which of OUR files describes a key_op to a producer is a property of this repository's surfaces, not of what a verifier may conclude. The admissible ops and fields the contracts must match are already bound to the reducer, and that binding is what the model speaks about."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. §12.2's field list is one of the two contracts checked, and it is unchanged."
      },
      "code": "tools/keyop-contract-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A producer-facing DESCRIPTION is prose or a schema string, not a document, and the entries it must match are already pinned by the vectors that exercise add and revoke."
      },
      "test": {
        "excluded": "The test layer is two controls, both run: a third producer-shaped field list added to a shipped file is named by the gate, and the detector is asserted to tell a field list from prose that merely mentions key_op. They run as npm run test:keyop, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 108,
      "math": {
        "excluded": "Whether OUR assertions name the reason they test is a property of our test suites. The model's own binding rule already demands that a cited theorem resolve to an executed check, which is the same discipline one layer up and is unchanged by this round."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. The refusals whose reasons are now matched are unchanged; only the assertions about them are stricter."
      },
      "code": "packages/ust-protocol/drift-guards.test.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce, and round 74 already added the vector that asserts a REASON rather than a code for the transition it covers. This round is about assertions over a CLI core and a meta-gate, neither of which produces a transcript."
      },
      "test": {
        "excluded": "The test layer is two controls, both run: a gate made red BEFORE injection is named instead of counted, and a refusal message replaced with an unrelated one makes the named regression check fail. They run as npm run test:drift-guards and test:cli, not as conformance checks, which cover the verifier only."
      }
    },
    {
      "round": 109,
      "math": {
        "excluded": "Whether OUR enumeration reports what it skips is a property of our tooling. The model's own totality discipline — a domain enumerated in both directions — is the rule being applied here, and it is unchanged by applying it."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. Which of our packages appear in the README is not a conformance obligation."
      },
      "code": "tools/package-enumeration-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A package manifest flag produces no document and changes no verdict; it is exercised by flipping the flag and running the gate."
      },
      "test": {
        "excluded": "The test layer is the control, run: a package marked private without a stated reason now fails, where before the gate passed in silence. Four further probes are recorded as negative results with what they probed. They run as npm run test:packages, not as a conformance check, which covers the verifier only."
      }
    },
    {
      "round": 110,
      "math": {
        "excluded": "The measurement is about our tooling's ability to notice its own blindness. Nothing in the formal model asserts anything about how our gates extract from source; the model's own totality obligations are unchanged and were not consulted."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently. How a gate of ours extracts a set from a source file is not a conformance obligation on anybody."
      },
      "code": {
        "excluded": "The pass changed no file, and that is its result. Seven extraction sites were blinded by making their patterns unmatchable and every one exited 1, so there was nothing to repair; writing a change anyway would be grading to a conclusion."
      },
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A blinded regex in one of our gates produces no document and changes no verdict; the probe is running the gate against a source it can no longer match."
      },
      "test": {
        "excluded": "The probes ARE the test layer and they are recorded rather than committed: each blinding is a one-line pattern edit, run, restore. Committing them would freeze a hostile edit of a gate into the tree; the reproduction is stated in the changelog row instead."
      }
    },
    {
      "round": 111,
      "math": {
        "excluded": "Whether OUR roster reads every step of OUR workflow is a property of our tooling, not of the protocol. The model's totality obligations are unchanged; the rule being applied — enumerate the domain, never a pattern that resembles it — is the one this repository already imposes on the verifier."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. Which of our CI steps carry an assurance marker binds nobody implementing UST."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A workflow step this gate could not parse produces no document and changes no verdict; the corpus mutant is the pinned artefact instead, injecting the exact shape that used to vanish."
      },
      "test": {
        "excluded": "This layer resolves against vectors/conformance-checks.json, the roster of executed CONFORMANCE checks — and conformance exercises the verifier, not our workflow parser. The test is the corpus mutant `unreadable-ci-step` in tools/mutations.mjs, run by drift-guards, which injects a CI step whose command the roster cannot read and requires assurance-map to go red: 14 gated mutations became 15. Naming a conformance check here to satisfy the resolver is the round-86 patch this repository rejects."
      }
    },
    {
      "round": 112,
      "math": {
        "excluded": "Whether a suite of ours DEMONSTRATES its can-fail leg or merely asserts one in prose is a property of our evidence discipline. The formal model states obligations on the verifier and is unchanged; nothing here alters what a document must satisfy."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. Where our CI installs an optional peer binds nobody implementing UST."
      },
      "code": "tools/assurance-map-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A reverted regex in one of our gates and a reordered workflow step produce no document and change no verdict; the two corpus mutants are the pinned artefacts instead."
      },
      "test": {
        "excluded": "This layer resolves against vectors/conformance-checks.json, the roster of executed CONFORMANCE checks, and conformance exercises the verifier rather than our anchoring connectors. The tests are the corpus mutants `rekor-treehead-signature-unchecked` and `ots-explorer-conflict-ignored` in tools/mutations.mjs, run by drift-guards: 15 gated mutations became 17, and each makes the suite that cites it go red."
      }
    },
    {
      "round": 113,
      "math": {
        "excluded": "Whether OUR regression suite names the refusal it asserts is a property of our evidence discipline, not of the protocol. The model states obligations on the verifier; none of them changed, and none of them speak about the CLI's ceremony helpers."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. How our CLI regression asserts a throw binds nobody implementing UST."
      },
      "code": "packages/ust-cli/regression.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. Asserting the REASON for a host throw in a ceremony helper produces no document and changes no verdict; the demonstration is the swap control described in the changelog row."
      },
      "test": {
        "excluded": "This layer resolves against vectors/conformance-checks.json, the roster of executed CONFORMANCE checks, and conformance exercises the verifier rather than the CLI's ceremony surface. The test is the suite itself — packages/ust-cli/regression.mjs, run as npm run test:cli-regression — where a wrong pattern fails the named check, and swapping the two token patterns fails exactly two."
      }
    },
    {
      "round": 114,
      "math": {
        "excluded": "Whether OUR package pages enumerate OUR surfaces is a property of our documentation, not of the protocol. The formal model states obligations on producers and verifiers; none of them changed, and the totality discipline being applied here is the one this repository already imposes on itself."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. Which commands a README lists, and how a first screen groups them, binds nobody implementing UST."
      },
      "code": "tools/printed-command-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A README table and a help screen produce no document and change no verdict; the artefacts here are the two enumeration legs and the placeholder rule, exercised by editing the surfaces they read."
      },
      "test": {
        "excluded": "The controls ran and are recorded rather than committed: removing the required road from the screen line makes the gate fail by name, and emptying either README fails the enumeration in the direction that names the missing command or tool. Committing a broken screen to freeze the control would ship a wrong first impression, which is the thing this round exists to fix."
      }
    },
    {
      "round": 115,
      "math": {
        "excluded": "Which npm tag resolves to which of OUR builds, and whether OUR audit invitation states measured numbers, are properties of our distribution and our documentation. The formal model states obligations on producers and verifiers and is unchanged by both."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. A dist-tag is a registry pointer and binds nobody implementing UST."
      },
      "code": "tools/audit-claims-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A registry pointer and a number in a markdown file produce no document and change no verdict; the artefacts are the two enumeration legs, one over the registry's tags and one over the claims in the section."
      },
      "test": {
        "excluded": "The live control for the tag leg is deliberately NOT run and says so in the file: moving a tag to a stale version would serve a stranger the wrong build for the duration. The mechanism is controlled instead against a real external datum — rc.36's deprecation must remain visible to the probe — and the claims gate is controlled by its own blind-domain floor plus an explicit refusal of the retired 32/32 string."
      }
    },
    {
      "round": 116,
      "math": {
        "excluded": "Whether OUR package pages name their family, carry an install line, and ship an example that executes is a property of our documentation. The formal model states obligations on producers and verifiers and is untouched by every change in this round."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. A README title and a runnable snippet bind nobody implementing UST."
      },
      "code": "tools/printed-command-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A page title, an install line and a marked example produce no document and change no verdict; the artefacts are the four enumeration legs over the package pages."
      },
      "test": {
        "excluded": "The test is the gate EXECUTING the marked examples: each `runnable:` block is written out and run inside the repo, so a page promising 'copy this and it works' is checked by doing it. Controlled by breaking one and watching the named check fail. It runs as npm run test:printed-commands, not as a conformance check, which exercises the verifier only."
      }
    },
    {
      "round": 117,
      "math": {
        "excluded": "The invariant was already stated and needed no change — §12.1's own words, *a genesis whose value carries a NON-EMPTY roles array DECLARES role separation*, are what decided the predicate. This round brought the code to the mathematics rather than the other way round, which is why nothing in the model moved."
      },
      "spec": "non-empty `roles` array DECLARES role",
      "code": "packages/ust-protocol/index.mjs",
      "vector": {
        "excluded": "The refusal already has its enforcement in resolveKeys and is exercised by building the genesis and reading the error; what changed is the BUILDER, which can no longer emit that document at all. A vector pins bytes a producer may emit, and the point of this round is that these bytes are no longer emittable."
      },
      "test": {
        "excluded": "The test is the runnable example on the package page, executed by the printed-command gate: it mints a genesis and key-log and reaches VALID:HIGH, so the construction path is checked by running it rather than asserted. The builder's four cases (null, [], undefined, non-empty) were each measured directly; the ceremony leg is covered by the CLI regression, which runs green with the stricter self-check."
      }
    },
    {
      "round": 118,
      "math": {
        "excluded": "The rule that decided the fix is TypeScript's grammar, not this protocol's model: a required parameter may not follow an optional one. Nothing in the formal model speaks about declaration files, and no obligation on producers or verifiers moved."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. A .d.ts is a convenience for one language's consumers and binds nobody implementing UST."
      },
      "code": "tools/gen-types.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A declaration file produces no document and changes no verdict; the artefact here is the gate, which compiles every shipped .d.ts with a real compiler rather than pattern-matching it."
      },
      "test": {
        "excluded": "The test is tools/types-integrity-gate.mjs, run as a CI step: it compiles each shipped declaration file and compares its declarations against the imported module's exports in both directions. Its control writes the exact shape that shipped — a required parameter after an optional one — and requires TS1016, so a compile leg that stopped running fails by name rather than passing for free."
      }
    },
    {
      "round": 119,
      "math": {
        "excluded": "What a declaration file says about a return value is a statement about one language's view of our code, not about the protocol. No obligation on producers or verifiers moved, and the model says nothing about TypeScript."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. A return type in a .d.ts binds nobody implementing UST."
      },
      "code": "tools/gen-types.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A return type produces no document and changes no verdict; the artefact is the generator refusing to claim one branch of a function that has several."
      },
      "test": {
        "excluded": "The check is the types-integrity gate already added in round 118 — it compiles every shipped declaration file, and a return type that names one branch of several compiles fine, so the gate cannot adjudicate this. What decided it was the consumer probe: a TypeScript file importing resolveKeys and calling it, which failed before and compiles now. Stated rather than dressed as gated: the gate does not cover this, and saying so is the point."
      }
    },
    {
      "round": 120,
      "math": {
        "excluded": "What sits at the root of our repository is a property of our housekeeping, not of the protocol. The model states obligations on producers and verifiers and is untouched; the rule applied here — enumerate the level you actually mean — is the one this repository already imposes on itself."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently, no conformance obligation changed. A directory listing binds nobody implementing UST."
      },
      "code": "tools/root-inventory-gate.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A tracked directory produces no document and changes no verdict; the artefact is the gate's new domain plus the CI step that finally executes it."
      },
      "test": {
        "excluded": "The control is run and recorded rather than committed: a stray directory staged into the index makes the gate fail by name, and committing that directory to freeze the control would ship the very thing this round removes. It runs as a CI step now, which is the change that matters — before this round the gate was never executed at all."
      }
    },
    {
      "round": 121,
      "math": "The escape is checked against what the protocol can ADDRESS",
      "spec": "An hour rarely fits in one node, and the shape below is the general one",
      "code": "packages/ust-operator/index.mjs",
      "vector": "breadth-flat-over-law-reject",
      "test": "#101 ADVERSARIAL: a capacity grant does NOT admit an over-breadth FLAT seal — capacity is VOLUME, breadth is STRUCTURE"
    },
    {
      "round": 122,
      "math": {
        "excluded": "A package name is a fact about the npm registry, not about the protocol. No obligation on producers or verifiers moved; the model is untouched by what a directory is called."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts or refuses differently. Which name our operator layer carries binds nobody implementing UST."
      },
      "code": "tools/npm-drift-check.mjs",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A rename produces no document and changes no verdict; the artefact is the gate learning to tell an unpublished package from an unreadable one."
      },
      "test": {
        "excluded": "The check is the npm-drift gate itself, run against a tree containing a genuinely unpublished package — which is what exposed the ordering bug in the first place. Committing a fake unpublished package to freeze that control would add a package nobody wants for the sake of a test."
      }
    },
    {
      "round": 123,
      "math": "`Ind_L` is not measurable in `σ(bytes)` and no function of the observations decides",
      "spec": "an implementation asked to carry an independence coordinate alongside the copies MUST refuse it",
      "code": "packages/ust-protocol/index.mjs :: replicationAgreement — the core owns both the verdict and the refusal; packages/ust-cli/index.mjs calls it instead of deciding",
      "vector": "replication-self-declared-independence-reject",
      "test": "#102 ADVERSARIAL: a producer-declared locator set does NOT raise the independence coordinate (F.5a.1 transposed to serving)"
    },
    {
      "round": 124,
      "math": {
        "excluded": "A dependency range is a fact about a package registry, not about the protocol. No obligation on producers or verifiers moved and no verdict changes; the model has nothing to say about how an implementation is installed."
      },
      "spec": {
        "excluded": "Nothing normative moved: the wire, the canonical form and every verdict are untouched. A conforming implementation in another language has no package.json at all."
      },
      "code": "tools/dependency-floor-gate.mjs :: enumerates every cross-package edge from the manifests and pins the floor to the sibling's current version",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A manifest range produces no document and changes no verdict; the artifact under test here is the install, not the transcript."
      },
      "test": {
        "excluded": "The gate IS the test, and it carries its own control: a floor one release behind must be red. There is no conformance check because no conformance behaviour moved."
      }
    },
    {
      "round": 125,
      "math": {
        "excluded": "The shape of our own release ledger is not a protocol object. No obligation on producers or verifiers moved and no verdict changes."
      },
      "spec": {
        "excluded": "Nothing normative moved: a conforming implementation in another language keeps no CHANGELOG of ours."
      },
      "code": "tools/version-truth-gate.mjs :: the record's SHAPE is checked where its domain was already enumerated — one section per version, no section both unpublished and PUBLISHED, rounds descend, one row per round",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. A section header produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The gate IS the test and carries its own blindness control: fewer than 20 parsed round rows fails, so the scanner going blind cannot make the legs pass for free."
      }
    },
    {
      "round": 126,
      "math": "a surface that is THERE is evidence whichever way the profile reads",
      "spec": "A profile MUST NOT relocate the standard surfaces",
      "code": "packages/ust-protocol/index.mjs :: surfaceVerdict decides the (declared, observed) 2x2 and refuses a relocated standard surface; packages/ust-cli/index.mjs fetches /.well-known/ust and defers the meaning of absence to it",
      "vector": "surface-profile-relocation-reject",
      "test": "#102 ADVERSARIAL: a profile cannot RELOCATE a standard surface — a named key-log location is refused, resolution stays at the well-known path"
    },
    {
      "round": 127,
      "math": {
        "excluded": "The soundness/completeness pair for a DECLARATION is a property of the reference implementation's packaging, not of the protocol: no obligation on producers or verifiers moved, no verdict changes, and a conforming implementation in another language ships no .d.ts at all. The invariant is stated where it is enforced (tools/gen-types.mjs, tools/consumer-probe-gate.mjs) and in issue 117."
      },
      "spec": {
        "excluded": "Nothing normative moved. The wire, the canonical form and every verdict are untouched."
      },
      "code": "tools/gen-types.mjs :: the optionality prover asks whether EVERY use is guarded, counts guard REGIONS rather than sites, and ignores comments; the object-literal return rule no longer matches any block body with a colon",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they must produce. A type declaration produces no document and changes no verdict; the artifact under test is the consumer's compile, not the transcript."
      },
      "test": {
        "excluded": "The gate IS the test and it carries its own control: a consumer-shaped mistake must produce TS2322, so the compile leg cannot pass for free. There is no conformance check because no protocol behaviour moved — the artifact under test is a consumer's compile, and the reference checker is unchanged."
      }
    },
    {
      "round": 128,
      "math": {
        "excluded": "Same reason as round 127: the soundness of a TypeScript declaration is a property of this implementation's packaging, not of the protocol. No obligation on producers or verifiers moved and no verdict changed."
      },
      "spec": {
        "excluded": "Nothing normative moved; a conforming implementation in another language ships no declaration file."
      },
      "code": "tools/gen-types.mjs :: emits UstState/UstDocument/UstVerdict, every key measured from a real value, and states at the top of each generated file what `unknown` promises",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. An interface produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The gate IS the test: it rebuilds the genesis, seals and verifies it, and compares declared keys against the real values in both directions, with a control requiring TS2322 so the compile leg cannot pass for free. No conformance check because no protocol behaviour moved."
      }
    },
    {
      "round": 129,
      "math": {
        "excluded": "As in rounds 127-128: declaration soundness is a property of this implementation's packaging, not of the protocol. No producer obligation moved and no verdict changed."
      },
      "spec": {
        "excluded": "Nothing normative moved; a conforming implementation in another language ships no declaration file."
      },
      "code": "tools/gen-types.mjs :: UstKeyResolution and UstStreamVerdict declared as discriminated unions, every branch read from a real value rather than from the source",
      "vector": {
        "excluded": "The corpus is the SOURCE of the measurement here rather than its target: the resolving key-log vectors supplied the success branch. No new vector, because no document bytes and no verdict changed."
      },
      "test": {
        "excluded": "The gate IS the test: it checks both branches of each union against values rebuilt from the corpus, in both directions, with a control requiring TS2322. No conformance check because no protocol behaviour moved."
      }
    },
    {
      "round": 130,
      "math": "Therefore no composition over anything `ust_id` can name reaches the depth ceiling",
      "spec": "### 21.2 Attestation — a sealed hour over N constituents",
      "code": "tools/worked-example-gate.mjs :: every fenced json block declares SKELETON or WORKED EXAMPLE, and a marked one is verified by the reference checker",
      "vector": "depth-law-covers-whole-ustid-space",
      "test": "#101 THEOREM F.9.5-b: the depth bound STRICTLY DOMINATES the addressing — the entire ust_id second-space seals at depth 7 <= 8"
    },
    {
      "round": 131,
      "math": "a quantifier over an unknown set is not a claim",
      "spec": "**And it MUST declare the substrates it anchors into.**",
      "code": "packages/ust-protocol/index.mjs :: anchorRollup reuses surfaceVerdict for the per-substrate verdict and adds only the roll-up over the declared set",
      "vector": "anchor-rollup-one-silent-leg-is-partial",
      "test": "#120 ADVERSARIAL: darkness is NOT decidable from one substrate — one silent declared leg yields PARTIAL, never dark"
    },
    {
      "round": 132,
      "math": {
        "excluded": "Declaration soundness is a property of this implementation's packaging, not of the protocol; no producer obligation moved and no verdict changed."
      },
      "spec": {
        "excluded": "Nothing normative moved: the verdict's runtime shape is unchanged, only its declaration caught up with it."
      },
      "code": "tools/gen-types.mjs :: UstVerdict declares the conditional publisher / publisher_claimed fields as optional; tools/consumer-probe-gate.mjs :: the reverse direction enumerates every declared interface instead of a hand-picked list",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. An interface field produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The gate IS the test: an interface without a real sample now fails, so the gap that hid this cannot recur silently."
      }
    },
    {
      "round": 133,
      "math": "no function of `I_A` separates them, and `A` cannot refuse what it cannot see",
      "spec": "the head MUST be shared among whatever writes the stream, not private to each writer",
      "code": "packages/ust-operator/index.mjs :: Stream takes a store port, names STREAM_KEYS as the layer's contract, resumes from the store and refuses E-FORK on a stale head",
      "vector": "stream-fork-visible-only-with-both-branches",
      "test": "#122 ADVERSARIAL: two appenders from one head both succeed when the head is PRIVATE — the fork is produced and neither can see it"
    },
    {
      "round": 134,
      "math": {
        "excluded": "F.5r already states the rule; this round changes WHERE it is reachable from, not what it says. No obligation on producers or verifiers moved and no verdict changed."
      },
      "spec": {
        "excluded": "Nothing normative moved: the producer duty in §11.3 is unchanged, only the layer's surface for meeting it."
      },
      "code": "packages/ust-operator/index.mjs :: advanceHead exported on its own; append routes through it so the rule has ONE body",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. An exported function produces no document and changes no verdict."
      },
      "test": {
        "excluded": "The layer's own suite covers every outcome of advanceHead, including that a refused advance leaves the store unchanged, plus a source-read check that append has no second implementation of the rule."
      }
    },
    {
      "round": 135,
      "math": "Hence the guarded FRACTION is irrelevant",
      "spec": "This obligation is over EVERY operation that advances the head, not over the principal one.",
      "code": "packages/ust-operator/index.mjs :: gap and resume route through advanceHead; Tiers takes the operator store and namespaces it per tier, preserving cas",
      "vector": {
        "excluded": "No document bytes and no verdict change: a gap record and a checkpoint are byte-identical before and after. What moved is WHICH code paths may write the head key, which produces no document."
      },
      "test": "#122 F.5r-c the head-key WRITER ROSTER, enumerated from source, reduces to the guard — a partly guarded layer forks exactly like an unguarded one"
    },
    {
      "round": 136,
      "math": "Not one door per VALUE, but one door per EVENT",
      "spec": "The head, the cumulative frame count and the observed interval are ONE record of one event, so a producer",
      "code": "packages/ust-operator/index.mjs :: recordFrame / recordCheckpoint / loadStreamState; append, gap and checkpoint route through them; store.incr used when offered",
      "vector": {
        "excluded": "No document bytes and no verdict change: the same frames and the same checkpoint are produced before and after. What moved is which stored values one call is responsible for, which produces no document."
      },
      "test": "#122 ADVERSARIAL F.5r-d recordFrame: a REFUSED frame moves NOTHING — the guard runs first, so a stream that correctly refused to fork does not count a frame it never emitted"
    },
    {
      "round": 137,
      "math": "Absence is not a value, and a store's value domain need not contain a representation of it",
      "spec": "Where a producer's state includes a value that is CLEARED — the start of an open interval is one — the",
      "code": "packages/ust-operator/index.mjs :: `del` enters the port contract; recordCheckpoint clears with it and refuses E-STORE without it; the per-tier namespace forwards it",
      "vector": {
        "excluded": "No document bytes and no verdict change: the checkpoint produced is byte-identical. What moved is how the layer clears its own state between intervals, which produces no document."
      },
      "test": "#122 ADVERSARIAL F.5r-e a store with no `del` is REFUSED rather than silently leaving the interval open — measured in production: the empty-string write was rejected 400, the seal reported success, and the next hour would have claimed bounds beginning before itself"
    },
    {
      "round": 138,
      "math": "The guard is SOUND — it answers exactly the question posed — and the question was",
      "spec": "The duty is not only against OTHER writers. A producer that publishes a frame and then fails to record the",
      "code": "packages/ust-operator/index.mjs :: reconcileHead + advanceHead's `published` arm — the stored head must be our observation or our emission, and re-asserting a lost advance is idempotent",
      "vector": "sa-04-stale-head-repeat-refused",
      "test": "#124 F.5r-f ADVERSARIAL the lost advance is REPAIRED, not repeated — publish succeeded, the head write did not, and the next interval must continue from what was PUBLISHED",
      "_vector_was": "No document bytes and no verdict change: the frames and the checkpoint are byte-identical. What moved is which head a producer may extend, which produces no document."
    },
    {
      "round": 139,
      "math": "The stream's head is defined by what exists, not by what is remembered",
      "spec": "That unverified state is recoverable, and a producer that can read its own published documents MUST recover",
      "code": "packages/ust-operator/index.mjs :: recoverHead — consistent / recovered / unverified, refusing a document that neither is nor extends the stored head, and writing through the single guard",
      "vector": "sa-03-self-fork-sibling-refused",
      "test": "#124 F.5r-g ADVERSARIAL the published document EXTENDS the stored head — `prev` is the proof, carried in the document, that this is the successor, so the head is RECOVERED",
      "_vector_was": "No document bytes and no verdict change: recovery reads documents that already exist and writes only the producer's pointer, which appears in no document."
    },
    {
      "round": 140,
      "math": "A total set of outcomes is an INTERFACE, not prose",
      "spec": "The outcomes of that head-recovery form a TOTAL set — consistent, recovered, unverified, refused — and a producer",
      "code": "packages/ust-operator/index.mjs :: HEAD_STATES declares the complete outcome set, refusal included, so an operator reports one of those words rather than one of its own",
      "vector": {
        "excluded": "No document bytes and no verdict change: the vocabulary describes a producer's recovery outcome, which appears in no document a verifier reads."
      },
      "test": "#124 F.5r-g.1 the outcome vocabulary is DECLARED BY THE LAYER and total — an operator that has to invent a word for one of these cases makes its telemetry incomparable with every other operator's"
    },
    {
      "round": 141,
      "math": {
        "excluded": "What a generator can soundly infer is a property of this implementation's packaging, not of the protocol: no producer obligation moved and no verdict changed. The mathematical content — that a total outcome set is an interface — was decided in round 140."
      },
      "spec": {
        "excluded": "Nothing normative moved. §11.3 already requires a producer to report one of the declared states; this round only makes that set nameable by a typed consumer."
      },
      "code": "tools/gen-types.mjs :: an exported const initialized with an object literal of quoted strings declares its keys and values; the renderer stops discarding the inferred type",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. A declaration produces no document and changes no verdict."
      },
      "test": "tools/types-integrity-gate.mjs :: every declared vocabulary is compared against the RUNTIME object, with a floor so the leg cannot go blind; mutation-tested by renaming a member"
    },
    {
      "round": 142,
      "math": "A frame carrying no `prev`",
      "spec": "A frame carrying no `prev` ASSERTS that the stream begins there. A producer MUST NOT emit one because it could",
      "code": "packages/ust-operator/index.mjs :: an empty stored head is refused past the first frame unless the caller declares an unseeded stream; the three legitimate paths declare it against evidence",
      "vector": "sa-05-midstream-origin-frame-refused",
      "test": "#124 ADVERSARIAL F.5r-h an empty head is REFUSED when the caller did not declare an unseeded stream — a port that answers \"absent\" on a failed read would otherwise disarm the guard, because null reads as \"nobody has written yet\" and the write proceeds",
      "_vector_was": "No document bytes and no verdict change: the frames produced are byte-identical. What moved is when a producer may treat an empty head as authorization, which produces no document."
    },
    {
      "round": 143,
      "math": "the protocol's name tells a machine to APPLY THIS PROTOCOL'S VERIFIER. That is an instruction, not a",
      "spec": "An artifact that carries this protocol's name MUST verify under it. The name is an instruction to a machine —",
      "code": "packages/ust-protocol/gen-byte-vectors.mjs :: the byte-vector corpus states `conformance_for` instead of claiming to be a document of the protocol",
      "vector": {
        "excluded": "Vectors pin document bytes and the verdicts they produce. This round is about artifacts that are NOT documents wearing the name, which by definition produce no verdict to pin."
      },
      "test": "#115 F.5t ADVERSARIAL a labelled NON-DOCUMENT is INDISTINGUISHABLE from a damaged one — same verdict AND same error code as a truncated document, so a benign file emits the signal of a broken transfer"
    },
    {
      "round": 144,
      "math": "A root is published TWICE, and only one of the two publications can enumerate (#127)",
      "spec": "The batch root is published as the `anchor` subtype (§11.3): `root`",
      "code": "packages/ust-protocol/index.mjs :: PREV_ONLY_SUBTYPES + buildAnchorCommitment — `root` FOLLOWS the subtype (required for anchor, forbidden for checkpoint/gap) instead of being flatly prohibited",
      "vector": "subtype-anchor-with-root",
      "test": "F4b prev-only subtype vocabulary is TOTAL in the corpus — every runtime name admitted AND refused by a vector"
    },
    {
      "round": 145,
      "math": {
        "excluded": "Stated as 'no new rule' at the time, and that was WRONG about one thing: the round showed that key-form makes the binding predicate VACUOUS, which is why a green battery proved nothing. Carried up in round 147 as F.5w — this record is left as it stood so the lag is visible rather than rewritten."
      },
      "spec": {
        "excluded": "The spec already states each of the three. A copy that lags the spec is a code defect, not a spec gap; changing the text would have hidden which layer was wrong."
      },
      "code": "docs/ust-verify.mjs :: C2 subtype discipline, the two-sided W3 partition, key binding to the authority key-log, and INDETERMINATE-is-not-an-integrity-error at both sites",
      "vector": "subtype-anchor-with-root",
      "test": "F4b prev-only subtype vocabulary is TOTAL in the corpus — every runtime name admitted AND refused by a vector"
    },
    {
      "round": 146,
      "math": "A declared absence is the STRONGEST statement that nothing was observed — so it may never back a no-event claim (#115)",
      "spec": "Everything else is blind ⇒ `observation-gap`. That includes a slot where the publisher was UNREACHABLE (a blind",
      "code": "packages/ust-protocol/index.mjs :: noEventBacking — coversNoSlot/observedSubject are class-first and fail-CLOSED, so a gap-covered slot is blind and a checkpoint covering no slot is never asked",
      "vector": "noevent-gap-record-is-blind",
      "test": "F.5v a slot covered ONLY by a signed gap record is BLIND — coverage is not observation, and the record IS the disclaimer"
    },
    {
      "round": 147,
      "math": "A predicate has a DOMAIN on which it is non-trivial, and key-form is where BINDING is not one (#119, carried up from round 145)",
      "spec": {
        "excluded": "The spec already fixes key-form as self-certifying (§3.1); the missing piece was the CONSEQUENCE for coverage, which is a statement about verification and belongs to the model, not to the wire format."
      },
      "code": "packages/ust-protocol/conformance.mjs :: the identical impersonation built in BOTH identity modes, so the verdict names which guard answered — the realization of the vacuity claim itself",
      "vector": "sa-02-impostor-eauthority",
      "test": "F.5w name-form: the SAME impostor keeps the victim name, reaches the binding guard and is refused BY IT (dom(Bound) is non-empty only here)"
    },
    {
      "round": 148,
      "math": "Corollary F.5r-g.2 (head-recovery is the FRAME EVENT, not the repair of one field)",
      "spec": {
        "excluded": "The wire format does not change: recovery is operator-side state, and §9's interval-integrity rule it was violating is already normative. Adding spec text would restate an existing requirement as if it were new."
      },
      "code": "packages/ust-operator/index.mjs :: recoverHead goes through recordFrame — the same door an ordinary append uses — instead of advanceHead alone",
      "vector": {
        "excluded": "Vectors pin document bytes and verdicts. This is the operator's STORE state after a recovery; no document differs, which is precisely why it was invisible until two members of the group were read together."
      },
      "test": "#115 F.5r-d recovery moves the WHOLE GROUP — the interval bound follows the adopted document, not only the head"
    },
    {
      "round": 149,
      "math": "The normative class-sets, without which the predicate cannot be evaluated (rev89)",
      "spec": "the role against a NORMATIVE class-set: **`name-binding-root` admits `genesis`/`key`/`cadence`; `data` admits",
      "code": "packages/ust-protocol/index.mjs :: ROLE_CLASSES + admits(k,c) enforced in resolveAuthority as E-KEY, refusal rather than downgrade",
      "vector": "admits-issuance-observation-refused",
      "test": "admits-issuance-observation-refused"
    },
    {
      "round": 150,
      "math": "F.7c A normative statement is DETERMINED, or it is unfinished — a preference is not an answer",
      "spec": "known, so its arguments must be readable from that document — and a document's identity coordinates are",
      "code": {
        "excluded": "No code changes: round 149 already shipped the only evaluable form. This round states WHY it is the only one, which is what the operator asking 'class or tier?' needed and what the next such question will need."
      },
      "vector": {
        "excluded": "The theorem is about which arguments a predicate CAN take, not about a verdict on bytes. Its consequences are already pinned by the round-149 matrix; a vector here would restate them."
      },
      "test": "admits-issuance-observation-refused"
    },
    {
      "round": 151,
      "math": "The root is in the table (rev91), and F.7c is why",
      "spec": "because a closed vocabulary with one member left unstated has, for that member, the same defect an open field",
      "code": "packages/ust-protocol/index.mjs :: ROLE_CLASSES gains name-binding-root, and the declared-regime condition returns as load-bearing because the root carries its role always",
      "vector": "admits-root-signs-data-under-declared-regime-refused",
      "test": "admits-root-signs-data-undeclared-unaffected"
    },
    {
      "round": 152,
      "math": "F.5y A re-rooting is a CROSSING of every genesis-rooted structure the publisher instantiated, not an event (#131)",
      "spec": "A re-rooting is a CROSSING, and it is not one event (F.5y)",
      "code": "packages/ust-cli/index.mjs :: runRerootCeremony + acceptReroot, acceptance indexed by the PRE-STATE rather than by the artifacts produced (F.5y.3)",
      "vector": "reroot-01-stream-not-crossed-refused",
      "test": "F.5y an uncrossed witness log makes the publisher its OWN fork"
    },
    {
      "round": 153,
      "math": "F.5z A supersession is a TERMINAL KEY-LOG ACT — the carrier is forced, not chosen (#133)",
      "spec": "The SIGNED half of a supersession is a terminal key-log `reroot` (F.5z)",
      "code": "packages/ust-protocol/index.mjs :: OP_FIELDS gains `reroot`, resolveKeys reports supersededBy, and an entry after a reroot is refused",
      "vector": "reroot-11-terminal-later-entry-refused",
      "test": "F.5z a root-signed `reroot` resolves and REPORTS the successor"
    },
    {
      "round": 154,
      "math": "Theorem F.5z.5 (the courier is the witness log, and it remains an index)",
      "spec": "Delivery is the publisher's burden, not the protocol's (F.5z.4/.5)",
      "code": "packages/ust-protocol/index.mjs :: witnessSuccessor carries the signed half, witnessNoShrink protects it, resolveSupersession verifies it through resolveKeys",
      "vector": "reroot-20-courier-proves-successor",
      "test": "F.5z.4 a supersession claimed with NO signed half is refused, not followed"
    },
    {
      "round": 155,
      "math": "Theorem F.5z.5 (the courier is the witness log, and it remains an index)",
      "spec": "The SIGNED half of a supersession is a terminal key-log `reroot` (F.5z)",
      "code": "packages/ust-cli/index.mjs :: runRerootCeremony emits the terminal reroot and hands the CLOSED log to the courier; askHidden hands back the unused remainder of a chunk",
      "vector": "reroot-20-courier-proves-successor",
      "test": "F.5z.5 the courier carries the WHOLE closed log, so an identity with prior entries still proves continuity"
    },
    {
      "round": 156,
      "math": "F.5y A re-rooting is a CROSSING of every genesis-rooted structure the publisher instantiated, not an event (#131)",
      "spec": "A re-rooting is a CROSSING, and it is not one event (F.5y)",
      "code": "packages/ust-cli/index.mjs :: ensureOutDir proves the output target writable at ENTRY, called by all five ceremonies before any passphrase",
      "vector": "reroot-20-courier-proves-successor",
      "test": "tools/ceremony-selfcheck-gate.mjs"
    },
    {
      "round": 157,
      "math": "F.5w A predicate has a DOMAIN on which it is non-trivial, and key-form is where BINDING is not one (#119, carried up from round 145)",
      "spec": "the same theorem as un-backdatable time (formal model F.3.1), now on the NAME axis (F.5a)",
      "code": "packages/ust-cli/index.mjs :: rootSignerFrom derives the public key FROM the private key instead of re-reading the supplied one",
      "vector": "reroot-20-courier-proves-successor",
      "test": "F.5w a cold backup is bound by DERIVING its pub from the private key, never by re-reading the given one"
    },
    {
      "round": 158,
      "math": "F.5w A predicate has a DOMAIN on which it is non-trivial, and key-form is where BINDING is not one (#119, carried up from round 145)",
      "spec": "the same theorem as un-backdatable time (formal model F.3.1), now on the NAME axis (F.5a)",
      "code": "packages/ust-cli/index.mjs :: proveWrittenKey re-reads every key file a ceremony writes and binds it to what the documents name",
      "vector": "reroot-20-courier-proves-successor",
      "test": "tools/ceremony-selfcheck-gate.mjs"
    },
    {
      "round": 159,
      "math": "Theorem F.5z.5 (the courier is the witness log, and it remains an index)",
      "spec": "The SIGNED half of a supersession is a terminal key-log `reroot` (F.5z)",
      "code": "packages/ust-cli/index.mjs :: collectServed prefers the ceremony's witness log over a derivation that cannot know the signed half",
      "vector": "reroot-20-courier-proves-successor",
      "test": "tools/deploy-completeness-gate.mjs"
    },
    {
      "round": 160,
      "math": "F.5y A re-rooting is a CROSSING of every genesis-rooted structure the publisher instantiated, not an event (#131)",
      "spec": "A re-rooting is a CROSSING, and it is not one event (F.5y)",
      "code": "packages/ust-cli/index.mjs :: the stream-crossing line names its destination and the destination to avoid",
      "vector": "reroot-01-stream-not-crossed-refused",
      "test": "F.5y an uncrossed frame stream is REFUSED under the successor genesis (M4)"
    },
    {
      "round": 161,
      "math": "A profile BINDS and DESCRIBES, and one extension rule cannot serve both",
      "spec": "The profile has TWO halves, and the boundary is POSITIONAL",
      "code": "packages/ust-protocol/index.mjs :: parseProfile returns the CLOSED half and is TOTAL on a hostile object",
      "vector": "profile-unknown-in-declares-refused",
      "test": "profile-unknown-at-top-level-ignored"
    },
    {
      "round": 162,
      "math": "A profile BINDS and DESCRIBES, and one extension rule cannot serve both",
      "spec": "Where a copy is NAMED",
      "code": "packages/ust-cli/index.mjs :: query-robustness probes every served surface, and the profile route carries a wildcard",
      "vector": "profile-copy-locator-accepted",
      "test": "profile-copy-stray-field-refused"
    },
    {
      "round": 163,
      "math": "The ladder is REPORTABLE, and each rung names WHICH TERM of R2 its missing input belongs to",
      "spec": "A rung the report did NOT ATTEMPT MUST be distinguishable from one it attempted and found met",
      "code": "packages/ust-protocol/index.mjs :: explainLadder reports coordinates and the inputs never supplied, each with its R2 term",
      "vector": "ladder-02-consumer-faculty-never-attributed-to-publisher",
      "test": "ladder-04-report-grants-nothing"
    },
    {
      "round": 164,
      "math": "TWO RULES THIS MODEL DOES NOT TRADE AWAY",
      "spec": "TWO RULES THIS PROTOCOL DOES NOT TRADE AWAY",
      "code": {
        "excluded": "This round writes a CONTRACT into every documentation surface and bumps the four published packages whose bytes it changed. No behaviour was altered — the reporting side that would change behaviour is #138 and is deliberately not conflated with stating the rule."
      },
      "vector": {
        "excluded": "Nothing new is decidable from bytes here: the round records an obligation on future changes to this specification, and the vector that would pin it belongs to #138 where the behaviour lands."
      },
      "test": "ladder-04-report-grants-nothing"
    },
    {
      "round": 165,
      "math": "Corollary F.5.1a-bis (a version gap is a rung, and its term is the RULESET)",
      "spec": "THE LINE THIS DRAWS, and it governs every verdict in §14",
      "code": "packages/ust-protocol/index.mjs :: a newer minor and a foreign major answer INDETERMINATE; INVALID is reserved for rules this verifier holds and found violated",
      "vector": "version-01-future-minor-is-indeterminate-not-invalid",
      "test": "version-05-invalid-means-my-rules-were-violated"
    },
    {
      "round": 166,
      "math": "F.5t-a The obligation is a property of a SET, and a verifier is a function of ONE document",
      "spec": "therefore MUST expose the classification as an operation a publisher can run over its own published set, not",
      "code": "packages/ust-protocol/index.mjs :: classifyNamed + nameSetReport — four outcomes, total at the directory-walk seam; packages/ust-cli/index.mjs :: cmdNames points them at an operator's set offline; tools/protocol-name-gate.mjs now CALLS the export instead of holding its own copy",
      "vector": "name-set-01-labelled-non-document-in-a-set-is-reported",
      "test": "F.5t-a examining NOTHING is not a pass — an empty target set is its own outcome, never folded into compliance"
    },
    {
      "round": 167,
      "math": "F.5t-a The obligation is a property of a SET, and a verifier is a function of ONE document",
      "spec": "therefore MUST expose the classification as an operation a publisher can run over its own published set, not",
      "code": "packages/ust-cli/index.mjs :: collectArtifacts admits every file and applies a REPORTED size bound; packages/ust-protocol/index.mjs :: classifyNamed judges a parsed artifact from its top level and reserves the byte scan for the unparseable",
      "vector": "name-set-06-a-parsed-array-makes-no-top-level-claim",
      "test": "tools/protocol-name-gate.mjs"
    },
    {
      "round": 168,
      "math": "F.5t-a The obligation is a property of a SET, and a verifier is a function of ONE document",
      "spec": "therefore MUST expose the classification as an operation a publisher can run over its own published set, not",
      "code": {
        "excluded": "A reference correction changes no behaviour: the labels, the Binding citations and the registry rows were repointed, and every predicate and verdict is byte-identical to round 167."
      },
      "vector": "name-set-01-labelled-non-document-in-a-set-is-reported",
      "test": "tools/protocol-name-gate.mjs"
    },
    {
      "round": 169,
      "math": "F.5q-c The WINDOW family is an index set, not a quantified one — and the chain already settles most of it",
      "spec": "never operator-optional; name-authority (HIGH) and anchored time (TOP) are document tiers; completeness is a SEPARATE range verdict over a stream, not a document tier",
      "code": "packages/ust-protocol/index.mjs :: commitmentCoverage reads each ust_id's half-open interval from its own precision, so coverage and gaps are computed with no declaration present",
      "vector": "coverage-03-adjacent-windows-are-not-a-gap",
      "test": "coverage-05-an-empty-chain-answers-unknown-not-uncovered"
    },
    {
      "round": 170,
      "math": "F.5p.3 A declaration must be AUTHENTICATED exactly when it can earn a pass",
      "spec": "**That refusal MUST name the READER as the party whose reach ran out.**",
      "code": "packages/ust-protocol/index.mjs :: parseProfile refuses an unimplemented member and attributes it to the verifier; commitment_rhythm parses to a settled null floor; packages/ust-cli/index.mjs :: the profile check reads skip rather than fail",
      "vector": "rhythm-04-an-unimplemented-member-is-the-readers-gap",
      "test": "rhythm-05-a-malformed-known-member-stays-the-publishers"
    },
    {
      "round": 171,
      "math": "F.9.5-c The ROOT does not compose, so the ENUMERATION is what splits",
      "spec": "**State a structural escape by its CEILING, not by an example:**",
      "code": {
        "excluded": "A presentation correction changes no behaviour: the depth law, the bounds and every verdict are byte-identical to round 170. What changed is which end of the range the normative text names first."
      },
      "vector": "depth-law-covers-whole-ustid-space",
      "test": "breadth-flat-over-law-reject"
    },
    {
      "round": 172,
      "math": "F.9.5-c.1 (the §7 root is not compositional, and compositionality is a property of the TREE, not of\nthis protocol)",
      "spec": "given the procedure as a connector; **absent one, the REFERENCE construction applies**",
      "code": {
        "excluded": "The correction is to a CLAIM, not to behaviour: §11.2 has admitted a substrate tree through opts.inclusionVerify since the connector split, and every byte both trees produce is unchanged. What moved is which tree the model says the property belongs to."
      },
      "vector": "breadth-flat-over-law-reject",
      "test": "depth-law-covers-whole-ustid-space"
    },
    {
      "round": 173,
      "math": "F.5u.1 (enumeration is neither necessary nor sufficient for membership)",
      "spec": "REQUIRED, `constituents` ABSENT, `prev` REQUIRED. A commitment asserts NO membership — membership is",
      "code": {
        "excluded": "No behaviour changed: the commitment shape, the root recomputation and every verdict are what rc.66 shipped. What this round produced is the MEASUREMENT that the composition item was already satisfied by not applying, and checks pinning where tree freedom begins and ends."
      },
      "vector": "breadth-flat-over-law-reject",
      "test": "depth-law-covers-whole-ustid-space"
    },
    {
      "round": 174,
      "math": {
        "excluded": "The report shape is a RECORDING convention, not a protocol claim: it changes no verdict, no byte and no invariant. The formal model is silent on it deliberately — a theorem about how we write reports would be a theorem about us."
      },
      "spec": {
        "excluded": "Same reason: the specification governs what a transcript means to a stranger, and a report is our own artifact about our own work."
      },
      "code": "tools/recap-compose.mjs :: § 6 carries four fates (DONE/MOVED/OPEN/BLOCKED) and the header carries RECORDED/DIARY, so a retro closing declares itself and refuses a retroactive diary",
      "vector": {
        "excluded": "No byte-vector: the subject is a generated skeleton, and its contract is enforced by --check refusing an unfilled marker rather than by a corpus entry."
      },
      "test": {
        "excluded": "MEASURED while writing this record: tools/recap-compose.mjs is invoked by NEITHER the workflow nor any package script — grep finds it in neither. So its --check, the refusal that makes the shape binding, runs only when an author chooses to type it. The ladder gate refused a test reference to it and was RIGHT: pointing the layer at an unrun command would have been the exact defect this repository spends its gates on. Recorded as an exclusion rather than papered over; wiring the generator's own check into the pipeline is the next round, not this one. CLOSED BY ROUND 195 (2026-08-10): the wiring this exclusion promised is done — tools/recap-compose-selfcheck.mjs runs the generator and observes BOTH directions of its refusal (rejects a skeleton, accepts a written report), and CI calls it as test:recap-selfcheck. The exclusion is kept rather than replaced: it was correct when written, and the record of WHY the layer was empty is worth more than a tidy reference."
      }
    },
    {
      "round": 175,
      "math": {
        "excluded": "Gate integrity is not a protocol claim: no verdict, byte or invariant of UST changes. The formal model is silent on how this repository checks itself, deliberately — a theorem about our instruments would be a theorem about us."
      },
      "spec": {
        "excluded": "Same reason. The specification governs what a transcript means to a stranger; the compile gates guard the declarations we ship, which is a packaging concern."
      },
      "code": "tools/consumer-probe-gate.mjs :: --pretty false plus an exit-code fallback so a non-zero exit matching no pattern still counts as failure; tools/types-integrity-gate.mjs :: the same repair",
      "vector": {
        "excluded": "No byte-vector: the subject is a build-time instrument, not a document. Its contract is enforced by the CONTROL leg each gate already carries — which is what detected the defect."
      },
      "test": "tools/consumer-probe-gate.mjs"
    },
    {
      "round": 176,
      "math": {
        "excluded": "The subject is how this repository narrates its own history, not what a transcript means. A theorem about our record-keeping would be a theorem about us — the same exclusion rounds 174 and 175 carry."
      },
      "spec": {
        "excluded": "Same reason: the specification governs what a stranger reads off a document, and these narrations are commentary in our own sources."
      },
      "code": "tools/record-closure-gate.mjs :: enumerates every dated narration and ratchets the unmarked count; tools/protocol-name-gate.mjs and spec/UST-1.0-formal-model.md :: the four live records now carry CLOSED IN PART with dates and the note that the note came later",
      "vector": {
        "excluded": "No byte-vector: the subject is prose in our sources, and its contract is enforced by the gate's own CONTROL pair — a marked narration must be recognised, an unmarked one must not."
      },
      "test": "tools/record-closure-gate.mjs"
    },
    {
      "round": 177,
      "math": {
        "excluded": "The subject is our own commentary, not the protocol. Same exclusion rounds 174-176 carry: a theorem about how we annotate ourselves would be a theorem about us."
      },
      "spec": {
        "excluded": "Same reason — the specification governs what a stranger reads off a document, and these are notes in our sources."
      },
      "code": "tools/record-closure-gate.mjs :: the pin fell 75 to 60; seventeen gate headers in tools/ now carry CLOSED with a verified commit and date",
      "vector": {
        "excluded": "No byte-vector: the subject is prose. The gate's CONTROL pair is the contract — a marked narration must be recognised, an unmarked one must not."
      },
      "test": "tools/record-closure-gate.mjs"
    },
    {
      "round": 178,
      "math": {
        "excluded": "Our own commentary, not the protocol — the same exclusion rounds 174-177 carry."
      },
      "spec": {
        "excluded": "Same reason: the specification governs what a stranger reads off a document."
      },
      "code": "tools/record-closure-gate.mjs :: PINNED_UNMARKED is 0, so the count asserts a property instead of tolerating a backlog; 44 narrations across 21 files carry a blame-established CLOSED",
      "vector": {
        "excluded": "No byte-vector: prose in our sources, contracted by the gate's CONTROL pair."
      },
      "test": "tools/record-closure-gate.mjs"
    },
    {
      "round": 179,
      "math": "Corollary F.5.1d (F.5.1b binds in BOTH directions, and the older copy is the VERDICT's)",
      "spec": "A diagnostic MUST NOT name, as a remedy, an input the call already supplied",
      "code": "packages/ust-protocol/index.mjs :: suppliedInput + supplyRemedy — one decision point for 'did this call hand in the input', so a branch and a sentence cannot disagree; both prescriptive sites routed through it",
      "vector": "remedy-01-genesis-not-prescribed-when-supplied",
      "test": "tools/remedy-guard-gate.mjs"
    },
    {
      "round": 180,
      "math": "The option roster is where this first shipped incomplete, and the failure is the theorem's own shape",
      "spec": {
        "excluded": "No normative change: F.5.1d already binds every supply remedy. This round closes an instance the GATE could not see, which is a realization defect, not a rule change."
      },
      "code": "packages/ust-protocol/index.mjs :: the stale-keylog diagnostic splits SUPPLY from REPLACE — its guard freshness === 'unverified' holds equally for an anchor supplied and not verified",
      "vector": "remedy-03-keyloghead-not-prescribed-when-supplied",
      "test": "tools/remedy-guard-gate.mjs"
    },
    {
      "round": 181,
      "math": "Theorem F.9.5-c.3 (the inclusion predicate has a constructive DUAL, and it belongs to the producer)",
      "spec": {
        "excluded": "No normative change: F.9.5-c.1 already made §11.2 inclusion a CONNECTOR, and this adds a PRODUCER for one connector's tree. A spec line would be the base naming a construction, which is the normativity that corollary dropped."
      },
      "code": "packages/ust-operator/index.mjs :: rfc6962AuditPath — the dual, in the operator layer beside sealTree, naming its scheme so a connector claims only what it implements",
      "vector": {
        "excluded": "No byte-vector: the assertion is a PAIR (build then verify) over a generated leaf list, contracted by the operator suite's own checks including the full mutation battery and a live 120-leaf tree."
      },
      "test": "F.9.5-c.3 EVERY single-step mutation breaks the proof (the load-bearing half)"
    },
    {
      "round": 182,
      "math": {
        "excluded": "No formal-model content: the invariant in play — a fetched continuation is a CANDIDATE until corroborated — is claim-vs-proof, already carried by F.5c.1 and the #71 trust-termination note. A theorem restating it would be prose in the model written to satisfy a column."
      },
      "spec": "`upgrade` now defaults OFF, which is the substantive half.",
      "code": "packages/ust-ots-verify/ots-codec.mjs :: parseOts / serializeOts / upgradeOts — zero-dependency, and makeSubstrateVerify defaults upgrade OFF",
      "vector": {
        "excluded": "No byte-vector: the contract is a BYTE-IDENTICAL round trip over real calendar output — an equality on the artifact itself rather than a case a vector could enumerate."
      },
      "test": "tools/ots-codec-gate.mjs"
    },
    {
      "round": 183,
      "math": {
        "excluded": "No formal-model content: the seam already has its theorem. F.5.0/C3 fixes the substrate answer as a CLOSED TYPED verdict and the module-seam law (UST-5tm) fixes its totality; what was missing was a second decoded shape for the NEGATIVE answer, which is an arity of the same contract rather than a new one. Writing a theorem to say \"and also decode false\" would be prose in the model to satisfy a column."
      },
      "spec": "a connector that answers NO is not a connector that could not be",
      "code": "packages/ust-protocol/index.mjs :: decodeSubstrateRefusal — the negative answer gets its own door, decoded inside the same guarded try, with `reason` admitted as a bounded slug",
      "vector": {
        "excluded": "No byte-vector: the subject is the SEAM, not a document. A vector fixes bytes a verifier must judge; this fixes how a not-ours module return is decoded, which no byte can express. Contracted instead by five conformance checks driving the seam with each shape."
      },
      "test": "a STATED substrate refusal is distinguishable from a malformed receipt"
    },
    {
      "round": 184,
      "math": {
        "excluded": "No formal-model content: nothing this round decides is a verdict. Portability moves WHERE code can run, never WHAT it concludes, and that is exactly what the gate pins — both builds must recompute canon, H, keyId, contentHash and both strictB64url verdicts to identical bytes. A theorem about the model would have nothing to say about a platform global, and inventing one to fill this layer would be the padding the law exists to prevent."
      },
      "spec": "The cryptographic faculty moved behind ONE internal module chosen",
      "code": "packages/ust-protocol/_crypto.mjs :: sha256Hex / ed25519Verify / aesGcmDecrypt — the platform faculty behind one internal module, selected at BUILD time and unreachable from the data path",
      "vector": "content-hash-minimal-document",
      "test": "tools/browser-portability-gate.mjs",
      "_vector_was": "No byte-vector: the subject is a BUILD, not a document. A vector fixes bytes a verifier must judge; nothing here changes a byte a verifier sees — the existing corpus passing unchanged IS the evidence, and a new vector would assert the same thing with a weaker instrument. The cross-build agreement check plays the vector role: it re-derives the same five values under a different platform and requires identity."
    },
    {
      "round": 185,
      "math": {
        "excluded": "No formal-model content: the subset relation between a floor and a ceiling is already fixed — a floor admits a SUBSET of what the ceiling admits at the ceiling's tier, and the model says so in both directions. Nothing here changes that statement; what changed is that one implementation did not satisfy it and the corpus could not show it. A theorem cannot fix a corpus, and adding one to fill this layer would be padding."
      },
      "spec": "two copies of the canonical form claimed to agree, and both claims were",
      "code": "packages/ust-light/index.mjs :: DATA_CLASSES / PREV_ONLY_SUBTYPES / merkleRoot — the floor accepts every data class and recomputes a set attestation's root, while the authority layer keeps its principled refusal",
      "vector": "role-attestation-in-data",
      "test": "packages/ust-light/test.mjs :: round-185 floor verdict for role-attestation-in-data (ceiling says admitted) — expect_floor across the six data-role class-role vectors, executed BY the floor",
      "_vector_was": "No byte-vector: the subject is a VERDICT DISAGREEMENT between two implementations over documents that already have vectors. A vector fixes bytes for one verifier to judge; what was missing is a corpus spanning the classes, which is a differential and lives in the parity gate. Adding a vector would pin one more document while leaving the domain question untouched."
    },
    {
      "round": 186,
      "math": {
        "excluded": "No formal-model content: nothing here decides a verdict. This is the mechanism that moves artifacts from the tree to the registry, and what it publishes was already fixed by the rounds that produced it. A theorem has nothing to say about a gitignored lockfile or a token scope, and inventing one to fill this layer would be the padding the law exists to prevent."
      },
      "spec": {
        "excluded": "No specification content: nothing normative moved. The protocol says what a document must be, and this round changed how the packages that implement it reach a registry — a stranger installing today gets the same bytes the spec already describes. Naming a spec section here to fill the layer would point at text this round did not touch."
      },
      "code": "tools/publish-line.mjs :: the line publishes straight to `latest` and there is no promote — the two-step design assumed both steps authenticate alike, and OIDC authorises publish and not dist-tag",
      "vector": {
        "excluded": "No byte-vector: the subject is a PROCESS, not a document. Nothing a verifier judges changed shape. The evidence that belongs here is an install from the registry followed by an import, which is what verified this round and is stronger than a vector for this claim."
      },
      "test": "tools/npm-drift-check.mjs"
    },
    {
      "round": 187,
      "math": "The obligation is universal over trees, and was realized on one (#139).",
      "spec": {
        "excluded": "No normative change: §11.2 already fixes the proof SHAPE and delegates the walk to a connector, with the reference construction applying absent one. This round asserts nothing new about the wire or about what a verifier must accept — it instantiates an existing obligation on the second tree. Adding spec text to fill this layer would restate §11.2 in weaker words."
      },
      "code": "packages/ust-protocol/model-correspondence.mjs :: roster — the citation domain spans BOTH executed rosters, because a theorem is quantified over the protocol and its realization lands wherever the mechanism lives; digest-bound exactly as ladder-completeness-gate binds the same two files",
      "vector": {
        "excluded": "No byte-vector: nothing about the WIRE changed. The subject is a producer/verifier PAIR over the reference tree, and its evidence is a mutation battery that must be executed to mean anything — a frozen byte artifact would record one path and could not express the universal negative leg, which is the half the theorem calls load-bearing."
      },
      "test": "F.9.5-c.3 REFERENCE tree: EVERY flipped direction breaks the proof (the load-bearing half)"
    },
    {
      "round": 188,
      "math": "The sizes are the PROTOCOL's, and the first version of this battery got that wrong",
      "spec": {
        "excluded": "No normative change: §13 already fixes breadth 64, array length 4096 and the depth ceiling. The round stops substituting one operator's hour width for those numbers — it reads the bounds the spec already states rather than adding any. Writing new spec text to fill this layer would duplicate §13 in a second place, which is how two ceilings come to disagree."
      },
      "code": "packages/ust-operator/conformance.mjs :: indices/SWEEP_CAP — the leaf sweep is exhaustive to 1024 and sampled at named structural indices above it, because the builder recomputes the tree per query and the full sweep at the 4096 ceiling costs 38 s in every CI run",
      "vector": {
        "excluded": "No byte-vector: nothing about the wire changed. The subject is which SIZES a producer/verifier pair is exercised at, and a frozen artifact records one tree — it cannot express a set of sizes, which is the whole content of this round."
      },
      "test": "F.9.5-c.3 REFERENCE tree: build-then-verify holds at the PROTOCOL sizes — 1,2,3,7,8, breadth 64, 65 where composition begins, and the 4096 ceiling (the positive half)"
    },
    {
      "round": 189,
      "math": "an inability that is not carried as a typed condition through EVERY consumer of a verdict becomes a",
      "spec": "inability is not guilt, and it is not silence either",
      "code": "packages/ust-protocol/index.mjs :: cannotDecide + the enumerated 16 consumers of a verdict; _crypto.browser.mjs :: the refusal carries code E-UNSUPPORTED instead of naming itself in prose. Seven consumers turned inability into an accusation; verifyStream tested v.error, which INDETERMINATE does not carry, so a build that checked NO signature had its frames counted as intact — the first fix created that silent confirmation, and it is the worse direction.",
      "vector": "faculty-absent-ed25519",
      "test": "packages/ust-protocol/browser-build.test.mjs :: the browser build is EXECUTED and must answer INDETERMINATE over a real VALID document"
    },
    {
      "round": 190,
      "math": "does not implement it is a rule with no realization",
      "spec": "reject a non-canonical scalar `S ≥ L`, reject small-order / non-canonical `A` and `R` encodings, and use",
      "code": "packages/ust-protocol/index.mjs :: admitEd25519Point / SMALL_ORDER — the two rejections §7/N6 demands and no library performs, decided on the wire bytes: a finite eight-element set of encodings, and y < p plus the negative-zero case (measured: vectors 8-11 are non-canonical in the SECOND sense only, so a y >= p check alone misses all four)",
      "vector": "ed25519-edge-00",
      "test": "packages/ust-protocol/conformance.mjs :: ed25519-edge-00 A — the reference edge-case corpus, executable here for the first time because the admission predicate takes bytes rather than a string"
    },
    {
      "round": 191,
      "math": "verifiers given the same inputs compute the same value; the practical use made of it — differential testing,",
      "spec": {
        "excluded": "No normative change. §7/N6 and the verdict rules are untouched; what moved is what the PROJECT says about its own artifacts. PORTING.md is the document a port reads and it is corrected here, but a spec clause asserting \"our documents are true\" would be a rule the protocol cannot enforce on anyone else — the enforcement belongs in a gate over this repository, which is where it went."
      },
      "code": "tools/promised-artifacts-gate.mjs :: a roster of claimable artifacts, each with its own detector derived from the tree rather than a declaration, checked against every git-tracked document",
      "vector": {
        "excluded": "No wire vector: nothing about a document, canon, hash or verdict changed. The subject is whether this repository's prose matches this repository's contents, which is not a property any byte-level vector can carry — a port reading our documents is affected, but a port's OWN documents are not ours to pin."
      },
      "test": "tools/promised-artifacts-gate.mjs :: a document may not claim an artifact this tree does not contain — mutation-checked (restoring the old CLI sentence reddens it by file and line) and vacuity-checked (a corpus mentioning nothing rostered reports VACUOUS rather than passing)"
    },
    {
      "round": 192,
      "math": {
        "excluded": "No mathematical content. The round moves an INSTRUMENT into the tree and makes a corpus self-contained; the theorem it serves (rev94, a rule delegated to a component that does not implement it) was written in round 190 and is unchanged. Writing model text to fill this layer would restate rev94 in a second place, which is how two statements of one rule come to disagree."
      },
      "spec": {
        "excluded": "No normative change. §7/N6 is untouched; what changed is whether OUR evidence for a claim about §7/N6 can be re-run by a stranger. A spec clause about the reproducibility of our own measurements would be a rule this protocol cannot impose on anyone else."
      },
      "code": "tools/platform-agreement/main.go :: the instrument, reading our own vectors rather than an upstream corpus; and the Go detector in tools/promised-artifacts-gate.mjs, which counted any .go file and was flipped to exists by the instrument itself",
      "vector": "ed25519-edge-00",
      "test": "tools/promised-artifacts-gate.mjs :: a document may not claim an artifact this tree does not contain — the detector fix is covered by the same mutation control (restoring the old CLI sentence reddens it), and the instrument itself is manual: no Go toolchain runs in CI, which is stated rather than implied"
    },
    {
      "round": 193,
      "math": {
        "excluded": "No mathematical content. The subject is what OUR corpus pins and how a round may account for it — a property of this repository's bookkeeping, not of the protocol. Writing model text here would restate the bookkeeping as theory, which is the patch this repository rejects."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently and no verifier accepts differently. The corpus gained a header describing itself; the vectors it describes are unchanged, byte for byte."
      },
      "code": "packages/ust-protocol/conformance.mjs :: the four UST#147 checks that re-derive the definition from the corpus on every run",
      "vector": {
        "excluded": "The subject has no fixed input FOR AN IMPLEMENTATION — it is the agreement between a header and the data beside it, not behaviour of a verifier on an input. This is the criterion the round itself introduces, applied to itself: a vector pins input to result, and there is no input here to give a port. Note the shape deliberately: the exclusion names WHICH of the six input forms it fails, which is exactly what the new rule demands of every future exclusion."
      },
      "test": "packages/ust-protocol/conformance.mjs :: UST#147 the definition's vector count matches the corpus it describes — mutation-controlled (stating 999 reddens that check alone), and the citation check caught a real error on its first run (vector IDs used where kinds were meant)"
    },
    {
      "round": 194,
      "math": {
        "excluded": "No mathematical content: the subject is whether OUR parity table describes OUR packages truthfully. The protocol property behind it — that finality and membership are different claims about a substrate — is already carried by §11.2 and by the combineSubstrates/combineInclusion split; restating it as theory would duplicate a rule that already has a home."
      },
      "spec": {
        "excluded": "Nothing normative moved. §11.2 already separates finality from membership, and no producer emits differently and no verifier accepts differently after this round. What changed is that our own capability table stopped claiming a connector implements a half it does not."
      },
      "code": "tools/capability-parity.mjs :: the connector probe now splits substrate-registry into finality AND membership, and ust-ots-verify declares that capability na with a physical reason rather than full",
      "vector": {
        "excluded": "No fixed input for an implementation — the subject is agreement between a declaration table and the exports beside it, which is a property of this repository rather than behaviour of a verifier on an input. Named against the criterion introduced in round 193: of the six input forms a vector may take, this subject provides none, because there is nothing to hand a port."
      },
      "test": "tools/capability-parity.mjs :: REALITY: ust-ots-verify declares substrate-registry — mutation-controlled in both directions (restoring full reddens by name; declaring subset for an absent half also reddens, which is how the na was found)"
    },
    {
      "round": 195,
      "math": {
        "excluded": "No mathematical content, and for the reason round 174 already gave about the same subject: the report shape is a RECORDING convention of ours, not a protocol claim. A theorem about how we write reports would be a theorem about us."
      },
      "spec": {
        "excluded": "Nothing normative moved: the specification governs what a transcript means to a stranger, and a report generator is our own artifact about our own work. No producer emits differently, no verifier accepts differently."
      },
      "code": "tools/recap-compose-selfcheck.mjs :: composes all three forms, counts their sections rather than naming them, and observes both directions of the --check refusal",
      "vector": {
        "excluded": "No fixed input for an implementation. Measured against the criterion round 193 introduced — of the six input forms a vector may take, this subject provides none: the input is our own generator and the result is its exit code, neither of which can be handed to a port. The corpus pins what a verifier must conclude, not whether our tooling runs."
      },
      "test": "tools/recap-compose-selfcheck.mjs :: the check ACCEPTED a skeleton that still carries FILL markers — mutation-controlled by disabling the marker-refusal branch specifically (the first attempt flipped an unrelated exit and stayed green, which is recorded because aiming NEAR a claim is a repeating class here)"
    },
    {
      "round": 196,
      "math": {
        "excluded": "No mathematical content: what a vector may pin is a property of this repository's corpus and bookkeeping, not a claim about documents or verdicts. Round 193 gave the same reason for the same subject, and nothing about the protocol moved when the seventh form was found."
      },
      "spec": {
        "excluded": "Nothing normative moved. The specification governs what a transcript means to a stranger; the form list governs how WE account for a round. No producer emits differently and no verifier accepts differently."
      },
      "code": "packages/ust-protocol/conformance.mjs :: the fifth UST#147 check, asserting the stated form count equals the forms listed",
      "vector": "stream-fork-visible-only-with-both-branches",
      "test": "packages/ust-protocol/conformance.mjs :: UST#147 the definition's form count matches the forms it lists — mutation-controlled (stating 3 while listing 8 reddens that check alone)"
    },
    {
      "round": 205,
      "math": "a construction is a TRIPLE, and the body belongs to it",
      "spec": "A construction is a leaf rule, a node rule AND a body grammar",
      "code": "packages/ust-operator/index.mjs :: rfc6962AuditPath returns an AnchorProof in the normative shape, with `anchor` absent because the Locator does not exist yet when the root is being computed",
      "vector": "ic-01-rfc6962-raw-n3-i1",
      "test": "F.9.5-c.3 the proof NAMES its tree, in the NORMATIVE carrier (F.9.5-c.6 — `inclusion.construction`, not a member inside the substrate Locator)"
    },
    {
      "round": 204,
      "math": "a construction is a TRIPLE, and the body belongs to it",
      "spec": "A construction is a leaf rule, a node rule AND a body grammar",
      "code": "packages/ust-rekor-verify/index.mjs :: inclusionVerify reads the NORMATIVE carrier `proof.inclusion.construction` instead of a member of its own inside the substrate Locator",
      "vector": "ic-01-rfc6962-raw-n3-i1",
      "test": "tools/inclusion-connector-gate.mjs"
    },
    {
      "round": 203,
      "math": "input under which the two branches are reported AS A FORK",
      "spec": {
        "excluded": "No normative change: the subject is how THIS repository ANCHORS a registry record to the model section it was written for. A locus is our own bookkeeping, not a protocol object, and a port reads neither the registry nor the anchoring rule. The protocol FACT underneath — that a claim must be bound to evidence — is already normative and unchanged."
      },
      "code": "tools/model-domain-totality.mjs :: the registry walk REFUSES a model_locus that occurs more than once, instead of anchoring at the first hit",
      "vector": {
        "excluded": "No byte-vector: the input is this repository's own registry file and its model text, and the output is a gate verdict about bookkeeping. A vector fixes bytes a VERIFIER must judge; no verifier, ours or a port's, ever sees a model_locus. The `test` layer is the gate itself, which is executed by CI."
      },
      "test": "tools/model-domain-totality.mjs"
    },
    {
      "round": 202,
      "math": "Domain (enumerated, not sampled)",
      "spec": "The withheld answer is carried in the proof record by OMITTING the",
      "code": "packages/ust-protocol/index.mjs :: anchoredByProofs — the third of the three call sites the theorem quantifies over, and the one whose answer decides authoritative against consumer-override",
      "vector": "ai-09-declared-foreign-construction-withheld",
      "test": "F.9.5-c.5 THIRD call site (witness): a witness anchor whose construction this build cannot compute leaves no-fork UNPROVEN, never confirmed"
    },
    {
      "round": 201,
      "math": "evaluating a construction one was not asked for is not evidence",
      "spec": "A proof DECLARES the construction it was built under, and the declaration BINDS THE READER",
      "code": "packages/ust-protocol/index.mjs :: verifyAnchorCore reads the declared construction and WITHHOLDS by omitting `inclusion`; verify() maps the omission to INDETERMINATE rather than E-ANCHOR (round-235: to the ANCHOR COORDINATE's unknown, not the document's verdict); anchoredByProofs tracks an unread proof so no-fork is not claimed from an unreadable one",
      "vector": "ai-09-declared-foreign-construction-withheld",
      "test": "packages/ust-protocol/conformance.mjs :: 'F.9.5-c.5 THROUGH verify(): a foreign construction withholds the ANCHOR coordinate and nothing else (round-235)' — RENAMED at round 235, and the rename IS a finding: the check read result === 'INDETERMINATE' over the WHOLE verdict, so its name described a symptom and its assertion began defending it. Round 201's claim survives intact (inability is not guilt: never INVALID, and E-ANCHOR still stands for a readable proof that misses its root); only the SCOPE it pins changed."
    },
    {
      "round": 200,
      "math": {
        "excluded": "No formal-model content, and the reason is the same one round 184 gave for its own math layer: a build decides WHERE code runs, never WHAT it concludes. What this round changes is which INPUTS the agreement is measured over — an evidence question, not a decision one. The model already states that two implementations agree by reproducing the corpus; nothing here adds to that, it makes one gate obey it."
      },
      "spec": "The cryptographic faculty moved behind ONE internal module chosen",
      "code": "tools/browser-portability-gate.mjs :: CASES/EXEC_SRC — the inputs come from the corpus and ONE executor text is evaluated on both sides, so the gate cannot compare two executors while claiming to compare two builds",
      "vector": "domainsep-leaf",
      "test": "tools/browser-portability-gate.mjs"
    },
    {
      "round": 199,
      "math": "a discriminator is only as good as its TOTALITY",
      "spec": "A fork-choice record carries `kind: \"fork-choice\"`, on EVERY",
      "code": "packages/ust-protocol/index.mjs :: REGISTRY.forkChoiceKind — the discriminator registered rather than repeated as a string literal, so the gate compares against a declared value instead of a hand-typed one",
      "vector": "fc-02-suspect-key-reaches-predicate-and-is-refused",
      "test": "tools/spec-code-sync.mjs"
    },
    {
      "round": 198,
      "math": "a stream verifier DOES distinguish — and why this guard stands alone",
      "spec": "a frame after the first that carries NO `prev` is likewise E-PREV",
      "code": "packages/ust-protocol/index.mjs :: verifyStream — the presence branch that refuses a frame with no `prev` past position 0; unchanged by this round, which is the point: the code already refused what neither the model nor the spec required of a consumer",
      "vector": "sa-05-midstream-origin-frame-refused",
      "test": "sa-05-midstream-origin-frame-refused"
    },
    {
      "round": 197,
      "math": {
        "excluded": "No mathematical content. That a floor must admit whatever the ceiling admits at the floor's own tier is stated by the subset relation the packages already carry; this round moves WHERE that claim is executed, not what it says."
      },
      "spec": {
        "excluded": "Nothing normative moved: no producer emits differently, no verifier accepts differently. The class × role domain is unchanged — only its floor half stopped being asserted from a locally built sample."
      },
      "code": "vectors/conformance-vectors.json :: expect_floor added to the six data-role class-role vectors, with the reason the six key-role ones must NOT carry it recorded beside them",
      "vector": "role-derivation-in-data",
      "test": "packages/ust-light/test.mjs :: round-185 floor verdict for role-derivation-in-data (ceiling says admitted) — mutation-controlled by narrowing DATA_CLASSES to ['observation'], which reddens exactly the attestation and derivation rows"
    },
    {
      "round": 206,
      "math": "The partition-kind domain `K` is one object, and fail-closed is only sound relative to it",
      "spec": "A verifier's admitted set MUST EQUAL it: admitting more accepts a tag",
      "code": "packages/ust-protocol/index.mjs :: REGISTRY.partitionKinds — the single definition; docs/ust-verify.mjs and extension/lib/ust-verify.mjs admit `absence` and name the offending kind in the error, both clean-room copies keeping their own literal by design",
      "vector": "partition-kind-absence-admitted",
      "test": "packages/ust-protocol/conformance.mjs :: #154 the cross-implementation corpus exercises every element of K — mutation-controlled by removing the absence vector (parity COVERAGE leg goes red) and by narrowing either clean-room literal (spec-code-sync names the missing element and the direction of the flip)"
    },
    {
      "round": 207,
      "math": "a report may name only a term it MEASURED, and a defect in `v` has no row",
      "spec": "a connector answering `final: false` MUST name WHICH check failed",
      "code": "packages/ust-protocol/index.mjs :: REGISTRY.anchorRefusalReasons — the closed set with the term that says WHO CAN ACT, realized once; docs/ust-resolve.mjs carries the clean-room literal and the BigInt climb, packages/ust-rekor-verify/index.mjs names the six refusals it had left bare",
      "vector": "anchor-refusal-inclusion-failed",
      "test": "packages/ust-protocol/conformance.mjs :: #155 the web witness inclusion climb verifies an index above 2^31 — the leg it replaced PINNED the browser copy to the 32-bit literal; mutation-controlled by reverting the climb (this leg plus three in tools/unbounded-index-gate.mjs go red) and by dropping, mis-terming or straying a reason (spec-code-sync and the coverage leg name which)"
    },
    {
      "round": 208,
      "math": "A DEFERRAL IS A CLAIM ABOUT SOMETHING THIS DOCUMENT CANNOT SEE.",
      "spec": {
        "excluded": "Round 208 changed nothing normative. The deferral rule governs how the FORMAL MODEL records its own obligations — it binds this repository’s bookkeeping, not an implementation of UST, and putting it in the normative spec would oblige every porter to a discipline that is ours."
      },
      "code": "tools/pending-binding-gate.mjs :: resolves every `Binding: pending — #N` against a committed tracker state that `--write` regenerates; fail-closed on an unreachable tracker; tools/model-domain-totality.mjs gains the duplicate-corollary-label leg",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an INPUT and a declared RESULT a second implementation must reproduce; the subject here is whether OUR model’s deferrals resolve against OUR tracker, which no other implementation has and none should be held to."
      },
      "test": "tools/pending-binding-gate.mjs :: five mutations — a closed tracker, a prose deferral, a marker naming no tracker in its span, an issue absent from the state, and a duplicate corollary label — each red with its own message; the closed-tracker leg re-proven after the first attempt reported `absent from the state` instead"
    },
    {
      "round": 209,
      "math": {
        "excluded": "F.5.1e already states the obligation — a rung decided by a conjunction reports WHICH conjunct failed, from a closed reason set. This round is a REALIZATION gap in verifyStream, not a new theorem, and inventing one for work the model already covers would be the vacuity this repository spends its gates on."
      },
      "spec": {
        "excluded": "Nothing normative moved. The shortfall slugs are the reference report naming its own missing inputs; a second implementation is held to the VERDICT (complete / chain-consistent / provisional), which is unchanged, not to the prose beside it."
      },
      "code": "packages/ust-protocol/index.mjs :: verifyStream — every shortfall return carries a slug naming its missing input (`unbounded`, `open-tail`, `no-grid`, `unbounded-interval`, `cadence-change`) beside the two that already spoke (`origin-unbound`, `hole`)",
      "vector": {
        "excluded": "There is nothing language-neutral to pin that is not already pinned. The completeness VERDICTS have vectors; the slugs are the reference implementation explaining its own answer, and holding a porter to our wording would make conformance turn on prose rather than on the decision."
      },
      "test": "packages/ust-protocol/conformance.mjs :: #161 the shortfall DOMAIN is enumerated from the source, not sampled — reads verifyStream and requires every non-`complete` return to carry a reason or a named hole; mutation-proven by ADDING a seventh silent return (6 found, 1 silent) and by stripping a reason from an existing one"
    },
    {
      "round": 210,
      "math": "bring a trust root, a connector, an accepted issuer | the CONSUMER only |",
      "spec": "anchor refusal reason",
      "code": "packages/ust-protocol/index.mjs :: REGISTRY.inclusionConstructions — construction → claimant as DATA, with the unsupported_construction detail generated from it; tools/spec-code-sync.mjs refuses a map missing the bundled construction or naming a package this tree does not publish",
      "vector": {
        "excluded": "The VERDICT is unchanged and already vectored — INDETERMINATE(unsupported_construction) in every case, which is the half a porter must reproduce. The route is this implementation explaining its own answer; holding a second implementation to our package names would make conformance turn on our distribution rather than on the decision."
      },
      "test": "packages/ust-protocol/conformance.mjs :: #153 every registered construction is reachable from the refusal it explains — mutation-proven by dropping the route, by inventing a claimant for an unregistered name, and by ADDING a construction the message cannot reach, which reddens the coverage leg alone"
    },
    {
      "round": 211,
      "math": "the floor is decidable offline by anyone",
      "spec": {
        "excluded": "Nothing normative moved. Which exports of OUR core reach the network is a property of this repository, not an obligation on an implementation — a porter may put its transport wherever it likes, provided its LIGHT floor stays offline-decidable, which F.5 already requires."
      },
      "code": "tools/core-network-boundary-gate.mjs :: discovers every acquisition of the global fetch in packages/ust-protocol/index.mjs, attributes each to its owning export by brace depth, and requires a declared reason; a declared name that no longer reaches also fails",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an input and a declared result a second implementation must reproduce; this measures where OUR module puts its transport, which no other implementation has and none should be held to."
      },
      "test": "tools/core-network-boundary-gate.mjs :: mutation-proven by ADDING a third reach, by a reach at module scope, and by renaming a declared export — the adding direction fixed in the card before the gate was written, because a boundary gate passes forever on the entries its author had in mind"
    },
    {
      "round": 212,
      "math": {
        "excluded": "The class is one the model already names — an enforcement must enumerate its domain, and a claim over an empty set is free — but the model governs what a VERIFIER owes, not what this repository’s report tooling owes. Restating it there to cover a composer would widen the formal model into our workflow, which is the one place it has always refused to go."
      },
      "spec": {
        "excluded": "Nothing normative moved. A porter writes no reports in this shape and is held to no part of it; the composer is how THIS tree records rounds, and the normative spec says nothing about that on purpose."
      },
      "code": "tools/recap-compose.mjs :: slot() and drawn() both register a section identifier — a number where the form numbers headings, a name where it does not; the declaration separates with | so a name can be an id, the lookup keys both styles from one map, and an EMPTY declaration is itself refused",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an input and a declared result a second implementation must reproduce; this governs the shape of OUR round reports, which no other implementation writes and none should be held to."
      },
      "test": "tools/recap-compose-selfcheck.mjs :: four legs per FORM — the form declares a non-empty slot domain, a DELETED slot is refused, a STATED refusal is accepted, and an emptied declaration is refused; leg 4 also had to stop filling a diagram slot with prose, because prose is not a decision"
    },
    {
      "round": 213,
      "math": {
        "excluded": "The class is one the model already states — a rule quantified over an empty set is unasked, not satisfied — and it states it about a VERIFIER's domain. This round is about a repository gate reading git, which the formal model has always declined to cover; widening it to reach our tooling would make the model a description of our workflow."
      },
      "spec": {
        "excluded": "Nothing normative moved. A porter runs no round-bookkeeping gate and is held to none of this; how this tree records which round closed which card is not an obligation on an implementation."
      },
      "code": "tools/recap-per-issue-gate.mjs :: the guard moved from a CAUSE of an empty domain to the domain itself — an empty closedBy is refused with its own message, sh() returns null on failure so a failed read is distinguishable from an empty one, and each caller refuses separately",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an input and a declared result a second implementation must reproduce; this measures whether OUR gate can see its own domain, which no other implementation has."
      },
      "test": "tools/recap-per-issue-gate.mjs :: mutation-proven in two directions — restoring the historical catch and the 1 MiB buffer yields the empty-domain refusal rather than a green rule, and clearing the domain past the guard reddens the pin and the new control; two controls exercise the unreachable branch and state the history actually read"
    },
    {
      "round": 214,
      "math": {
        "excluded": "Nothing mathematical is at stake. This is the rendered geometry of a report page — how an HTML table distributes column width — and the formal model governs verdicts over documents, not the layout of the prose in which a round is recorded."
      },
      "spec": {
        "excluded": "Nothing normative moved. A porter writes no reports in this shape; the composer is how THIS tree records rounds, and the normative spec says nothing about that on purpose."
      },
      "code": "tools/recap-compose.mjs :: tbl takes the column count as the max over rows and a shorter row spans the remainder, so one table can mix a four-up factual row with a full-width evidence row; § 5's evidence cells became full-width rows; and --check now reads the RENDER — a fence opening inside a list item without being indented into it, and a table whose rows span different totals, are both refused",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an input and a declared result a second implementation must reproduce; this governs the layout of OUR round reports, which no other implementation writes."
      },
      "test": "tools/recap-compose-selfcheck.mjs :: six legs — every emitted table row spans the same total and an incident table must actually MIX the two widths (or the padding is unexercised), a fence inside a list item is refused, and the same fence indented into the item is accepted; removing the padding reddens the grid leg by name"
    },
    {
      "round": 215,
      "math": {
        "excluded": "The class the model already names — an enforcement must enumerate its domain — and it names it about a VERIFIER. This is a repository gate reading commit conventions, which the formal model has always declined to cover; stating it there would make the model a description of our bookkeeping."
      },
      "spec": {
        "excluded": "Nothing normative moved. A porter runs no round gate and follows no commit convention of ours; which channel declares that a round closed a card is not an obligation on an implementation."
      },
      "code": "tools/recap-per-issue-gate.mjs :: the domain is the union of three declaration channels (commit subject, closing trailers, recap registry) read per round; disagreement between channels is a finding; every round git carries must name an issue or record a no_recap reason; and every declared channel is asserted live so the domain cannot narrow back in silence",
      "vector": {
        "excluded": "There is nothing language-neutral to pin. A vector fixes an input and a declared result a second implementation must reproduce; this measures whether OUR gate can see the rounds this tree writes."
      },
      "test": "tools/recap-per-issue-gate.mjs :: three controls, one per previously unread channel — two issues named only in the subject, two channels naming different issues for one round, and a round naming nothing and declaring nothing; mutation-proven twice, once by declaring a second issue through the subject and once by deleting the subject channel, which is the mutation that found the fix did not protect itself"
    },
    {
      "round": 216,
      "math": "Prefix-uniqueness supplies the first and is silent on the second",
      "spec": "a verifier MUST report the basis on which currency is held (`map_root_currency`) and MUST NOT",
      "code": "packages/ust-protocol/index.mjs :: `mapRootAdmitted` becomes `mapRootBasis` and returns a currency BASIS instead of a boolean, carried into the verdict as `map_root_currency` at BOTH admission sites — the name-map identity rung and the checkpoint-map freshness rung — because one mechanism admitted both typed spaces through one silent boolean",
      "vector": "map-currency-01-pinned-root-is-consumer-asserted",
      "test": "F.5a.2b a consumer-pinned map root reports map_root_currency consumer-asserted"
    },
    {
      "round": 217,
      "math": "a refusal carries every coordinate of `A` and no member of `F`",
      "spec": "a refusal MUST carry the coordinates it MEASURED",
      "code": "packages/ust-protocol/index.mjs :: one refusal SEAM over verifyCore's thirteen INDETERMINATE exits attaches `absent`, `verifier` and `registry_digest` and strips any `tier`; `absentInputs` is extracted so the refusal and the ladder report read ONE derivation of what the call did not bring",
      "vector": "refusal-shape-01-measured-facts-ride",
      "test": "F.5.1f a refusal carries NO tier — the field a consumer branches on to proceed"
    },
    {
      "round": 218,
      "math": "A rung\nis a strength of the fact ESTABLISHED, not of the manner of establishing it",
      "spec": "A tier rung records the strength of what was",
      "code": {
        "excluded": "Nothing changed and nothing needed to. Measured first: `projectTier` reads the four axes and has never seen a basis, the verdict already carries `basis`/`noFork` beside the strength, and `ULTRA` appears nowhere in the tree. Writing code here would have been building a duplicate of what the round was sent to verify."
      },
      "vector": {
        "excluded": "The proposition is about the PROJECTION, whose inputs are already pinned by the twelve exhaustive tier-projection vectors over the lattice. A vector asserting that a field the function cannot read does not change its output would fix an input no conforming implementation can accept in the first place."
      },
      "test": "F.5a.3 two verdicts differing ONLY in evidence basis project to the SAME tier"
    },
    {
      "round": 219,
      "math": "claim whose domain the claimant delimits is not bounded at all",
      "spec": "such a chain earns a CANDIDATE, and the floor is ESTABLISHED only",
      "code": "packages/ust-protocol/index.mjs :: `deriveStreamFloor` walks the supplied epoch transitions to a candidate root, binds a supplied root genesis to it by `genesisEpoch(contentHash(·))`, and returns `established` ONLY when that root is consumer-admitted — a publisher-supplied chain earns a candidate under its own name, because the chain's completeness is the publisher's to withhold",
      "vector": "stream-floor-01-withholding-moves-it-later",
      "test": "F.5s withholding the earliest transition moves the derived floor LATER (suppression forges)"
    },
    {
      "round": 220,
      "math": "epoch-varying coordinate moved from `C` into `Fₜ`, the `C`-part became one entry per AUTHORITY rather than one",
      "spec": "`anchored-authority` — the root arrives as a",
      "code": "packages/ust-protocol/index.mjs :: the `nameMapRootClaim`/`buildNameMapRoot`/`verifyNameMapRoot` trio mirrors the audited no-fork-evidence shape, and `proveMapRootAnchor` mints an unforgeable token only when the authority signature AND the anchor inclusion both hold — enlisted in the admitDeep preservation registry, without which the branch is silently dead",
      "vector": "map-root-anchor-01-signed-and-anchored-mints",
      "test": "#42 anchored-authority earns authoritative with NO consumer mapRoots — per-epoch config becomes one-time"
    },
    {
      "round": 221,
      "math": "ABSENCE of an assigned domain is UNESTABLISHED, never independent",
      "spec": "A verifier reports the assigned domain as `map_authority_domain` and MUST NOT infer independence from",
      "code": "packages/ust-protocol/index.mjs :: the consumer's `trust_domain` for a map authority is read, rides the minted token into the verdict as `map_authority_domain`, and `requireIndependentAuthority` refuses `authoritative` on an unassigned domain or one equal to the publisher's — reading whichever coordinate the route supplied, so both routes to authoritative answer the same question",
      "vector": {
        "excluded": "The claim is about what a CONSUMER CONFIGURATION does to a verdict, and a vector fixes bytes plus a declared result. The admissible half is already pinned by `map-root-anchor-02` (an authority the consumer does not hold mints nothing); what is new here is a policy floor, whose input is not part of any document a second implementation would be handed."
      },
      "test": "#42 requireIndependentAuthority: no assigned domain ⇒ INDETERMINATE, never an inferred independence"
    },
    {
      "round": 232,
      "math": {
        "excluded": "No theorem moved. Whether a host delivers a file decides no verdict; a document that reaches a verifier earns exactly what it earned before. This is the boundary between the repository and the deployment, which the model does not quantify over and should not."
      },
      "spec": {
        "excluded": "Nothing normative moved. How an implementation is HOSTED is not a protocol question, and a porter serving the same bytes from anywhere is unaffected. The obligation broken here was ours to our own readers, not the protocol's to anyone."
      },
      "code": "docs/.nojekyll :: the one byte that stops the host's publisher from dropping every underscore-prefixed module the vendored build imports",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. 'Our host serves the files we committed' is neither an input nor a result — it is a property of one deployment, and the check that holds it belongs where the vendoring happens."
      },
      "test": "tools/vendor-browser-core.mjs :: nothing is served that the package no longer ships"
    },
    {
      "round": 231,
      "math": {
        "excluded": "No theorem moved. The round measures that two implementations of a CONNECTOR agree on one live anchor; the decision relation reads a receipt and does not care which connector produced it. Agreement between two suppliers of one faculty is an engineering fact about this tree, not a property of the wire."
      },
      "spec": {
        "excluded": "Nothing normative moved. §11.2 keeps the substrate delegated and the receipt shape is unchanged; which connectors exist, and how strong each honestly claims to be, is exactly what the spec leaves to implementations."
      },
      "code": "tools/primitive-parity-gate.mjs :: the owed comparison is replaced by the measurement, with the assurance difference named to the word rather than smoothed over",
      "vector": {
        "excluded": "A vector fixes an input and a declared result reproducible offline. This claim is about two connectors reaching the same answer over LIVE third-party logs — Rekor and a block explorer — which no offline vector can carry, and which is why the record lives beside the exports rather than in a running leg."
      },
      "test": "tools/primitive-parity-gate.mjs :: every shared entry point agrees; the entry-point SET is pinned"
    },
    {
      "round": 230,
      "math": {
        "excluded": "No theorem moved. F.5.1 already states that inability is a THIRD reading and may not be reported as a verdict; this round applies that existing rule to a second implementation which had never obeyed it. A model section for 'the other copy should also follow the model' would be bookkeeping, not mathematics."
      },
      "spec": {
        "excluded": "Nothing normative moved. The spec already says a verifier that cannot implement a primitive answers INDETERMINATE(unsupported_alg) rather than guessing — the clean-room was simply not doing what the spec already required."
      },
      "code": "docs/ust-verify.mjs :: edVerifyRaw re-throws a missing FACULTY as E-UNSUPPORTED (DOM NotSupportedError) while a bad INPUT stays false, and the outer catch routes it to INDETERMINATE instead of INVALID",
      "vector": {
        "excluded": "The language-neutral half is already pinned by `faculty-absent-ed25519`, which fixes an input and its declared result for a build lacking the primitive. What this round adds is that a SECOND implementation obeys it, and 'our other copy also behaves' is not something a porter reproduces — it is a property of this tree, held by a gate."
      },
      "test": "tools/browser-portability-gate.mjs :: #144 clean-room WITHOUT the faculty WITHHOLDS on an honest document"
    },
    {
      "round": 229,
      "math": {
        "excluded": "No theorem moved. The substrate was always the consumer's faculty and the decision relation always read its receipt; this round supplies that faculty in one more environment. A document earns exactly the verdict it earned before, on the same evidence."
      },
      "spec": {
        "excluded": "Nothing normative moved. §11.2 keeps the substrate delegated on purpose, and which connectors an implementation ships — and for which hosts — is precisely what the spec leaves to implementations. A porter owes a receipt of the declared shape, not our adapter."
      },
      "code": "docs/substrate-browser.mjs :: substrateVerify for a page, mapping the existing browser rekor/OTS checks onto the contract the core asks for, at the assurance they actually hold",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. The claim here is that a receipt obtained in a BROWSER matches the one the Node connector produces for the same anchor — a cross-connector agreement over live substrate answers, which no offline vector can carry. It is named as owed in the parity gate rather than left implied."
      },
      "test": "tools/primitive-parity-gate.mjs :: every shared entry point agrees; the entry-point SET is pinned"
    },
    {
      "round": 228,
      "math": {
        "excluded": "No theorem moved. Two public entries were made to return one shape and a gate stopped being asked a question a browser cannot answer; the decision relation is untouched, and every document earns the verdict it earned before. The model quantifies over verdicts, not over which door a caller knocked on."
      },
      "spec": {
        "excluded": "Nothing normative moved. Which entry point a consumer calls, and how a page is wired to it, are implementation questions the spec deliberately leaves alone; a porter reproduces verdicts, and the verdicts are unchanged."
      },
      "code": "packages/ust-protocol/index.mjs :: finishVerdict shared by both public doors, and resolveByDiscovery gating on the ASYNC verdict so a browser reaches discovery at all",
      "vector": {
        "excluded": "A vector fixes an input and a declared result. Both defects here are about REACHABILITY — whether a build can get to an answer it already agrees with — which a vector cannot express: on any platform with the synchronous faculty the corpus answers identically before and after."
      },
      "test": "web: the no-fork checkbox is suppressed on the CORE's corroborated bases, not on page-local prose"
    },
    {
      "round": 227,
      "math": {
        "excluded": "No theorem moved. Which bytes a host serves decides no verdict; the same document verifies identically whether the verifier that read it was installed from a registry or fetched from a page. The model quantifies over evidence, and provenance of the verifier binary is not evidence about a document."
      },
      "spec": {
        "excluded": "Nothing normative moved. A conforming implementation may be distributed any way its author likes; the spec's only relevant sentence — resolve the verifier by name, never trust one a sender links you to — is unchanged, and this round is us finally obeying it on our own deployment rather than a new obligation on anyone else."
      },
      "code": "docs/lib/ust-protocol/index.mjs :: the vendored browser build the page serves, produced by the package's own browser map and compared to it byte for byte",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. The claim here is that two copies of ONE implementation are identical, which is a byte comparison rather than a conformance question; a porter reproduces verdicts, not our distribution."
      },
      "test": "tools/vendor-browser-core.mjs :: nothing is served that the package no longer ships"
    },
    {
      "round": 226,
      "math": {
        "excluded": "No theorem moved. An address decides no verdict: the same bytes verify identically whichever host served the verifier that read them. The model quantifies over evidence a verifier holds, and a hostname is not evidence."
      },
      "spec": {
        "excluded": "Nothing normative moved. The spec's advice — resolve the verifier BY NAME, never trust one a sender links you to — is unchanged and is exactly what this round makes true of our own deployment. WHICH name is ours is a fact about this operator, and the spec is deliberately free of it."
      },
      "code": "packages/ust-cli/index.mjs :: the clipboard HEADER a publisher stamps into every artifact now names the live verifier, and so do the extension's and release-evidence's copies of it",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. Where OUR verifier is hosted is neither; a porter reproduces verdicts, not our deployment."
      },
      "test": "tools/verifier-url-gate.mjs :: no tracked file points at the retired origin"
    },
    {
      "round": 225,
      "math": {
        "excluded": "No theorem moved, and the distinction is the point. The decision relation is unchanged: the same document under the same trust configuration earns the same verdict. What changed is which PLATFORMS can evaluate it. The model quantifies over what a verifier may conclude from evidence it holds, never over the faculties of the host it runs on."
      },
      "spec": {
        "excluded": "Nothing normative moved. The spec already says a verifier that does not implement a primitive answers INDETERMINATE(unsupported_alg) rather than guessing, and that sentence is what the browser was correctly obeying. How an implementation obtains a faculty is its own business; binding a porter to our async door would make an engineering choice normative."
      },
      "code": "packages/ust-protocol/_sigmemo.mjs :: the verifier-owned signature memo, plus verifyCoreWithSignatureFaculty in index.mjs — the fixpoint that resolves what the core asked about and runs it again with real answers",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. This round's subject is a BUILD's faculty and the door that supplies it — not bytes. The half a vector CAN express, the honest refusal without the primitive, is already pinned by `faculty-absent-ed25519`, and this round leaves that answer exactly as it was."
      },
      "test": "tools/browser-portability-gate.mjs :: #144 verifyAsync REACHES a verdict in the browser build"
    },
    {
      "round": 224,
      "math": {
        "excluded": "No theorem moved. Whether a PACKAGE runs in a browser is a property of an artifact's host environment, not of the decision relation: the same document, the same trust configuration and the same evidence yield the same verdict whether the code executing them was installed in Node or bundled for a page. The model quantifies over what a verifier may conclude, and nothing here changes that set."
      },
      "spec": {
        "excluded": "Nothing normative moved, and the distinction is worth stating because the card sounds like it should. The spec already requires a verifier to REPORT what it cannot evaluate rather than pass it silently - that obligation is discharged in verdicts, per document. Which environments a given implementation's packages happen to run in is a fact about this tree's distribution, binding on us and on nobody porting the protocol."
      },
      "code": {
        "excluded": "No library code changed. The round adds a DECLARATION to nine manifests and the same statement to nine READMEs, and moves two published versions because a manifest field and a README line are shipped bytes. Nothing a verifier executes moved; the enforcement that reads the declarations is the test layer below."
      },
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. A package's browser stance is neither: there is no input, and the result a porter would have to reproduce would be a fact about OUR distribution rather than about the wire."
      },
      "test": "tools/browser-portability-gate.mjs :: the portability domain is READ from packages/, not listed here"
    },
    {
      "round": 223,
      "math": {
        "excluded": "No theorem moved and none should. A request label decides no verdict: strip it and every document in the corpus verifies identically, because nothing in the decision relation reads a transport header. The formal model quantifies over what a verifier concludes from bytes it holds, and this round changes only how those bytes were asked for."
      },
      "spec": {
        "excluded": "Nothing normative moved. A conforming implementation is free to label its requests or not, and a porter who omits this reaches exactly the same verdicts; making it normative would bind a second implementation to our operational courtesy and would be unenforceable from a document anyway. The obligation this round creates is on THIS tree, so it lives in a gate rather than in the spec."
      },
      "code": "packages/ust-protocol/index.mjs :: userAgent + labelledFetch, one seam per package with the DEFAULT fetchImpl labelled, adopted across all eight network surfaces (core, cli, mcp x2, ots-verify x2, rekor-verify, and the mcp server)",
      "vector": {
        "excluded": "A vector fixes an input and a declared result a second implementation must reproduce. This round produces neither: the label rides on an HTTP request, not in a document, and a conformance vector that asserted a header would be asserting a property of our transport rather than of the wire a porter has to agree with."
      },
      "test": "tools/user-agent-gate.mjs :: every shipped module that reaches the network is DISCOVERED from source, never listed"
    },
    {
      "round": 222,
      "math": {
        "excluded": "No theorem moved. This round states no property of the protocol; it records what six earlier rounds established and closes the card. Adding a model section for a closing act would make the formal model a description of our bookkeeping, which it has always declined to be."
      },
      "spec": {
        "excluded": "Nothing normative moved. A porter runs no recap and closes no card of ours; which report accompanies a closing is not an obligation on an implementation."
      },
      "code": {
        "excluded": "No code changed. The round's artifacts are a sealed diary entry, a rendered page derived from it, and two registry rows — none of which any verifier reads."
      },
      "vector": {
        "excluded": "There is nothing language-neutral to pin: a vector fixes an input and a declared result a second implementation must reproduce, and this round produced neither an input nor a result."
      },
      "test": "tools/recap-per-issue-gate.mjs :: the round is accounted by the gate that refused it first — a recap belongs to work recorded in a version line, and the CHANGELOG row for 222 is what discharges that; the diary half is held by test:diary, which fails if the rendered page disagrees with the signed store"
    },
    {
      "round": 233,
      "math": "withholding the identity verdict because the cadence is unknown is the category error rev92 named,",
      "spec": "A verifier MUST NOT withhold a verdict on the strength",
      "code": "packages/ust-protocol/index.mjs :: resolveByDiscovery — the early return on an unreadable cadence is replaced by a carried `cadenceUnknown`; resolveCadence is NOT called in that state, because resolveCadence(genesis, [], t) would hand back the genesis value as though it were in force, which is exactly the substitution the F.4 closure forbids; the resolution reports `cadence_unknown` positively and omits `cadence` entirely, so the third state can never be read as the publisher's `null`. docs/index.html renders the three states apart, naming which is permanent and which is a retry.",
      "vector": {
        "excluded": "Nothing language-neutral to pin. A vector fixes bytes in and a declared result out; this round's subject is the TRANSPORT's answer — a determinate 404 versus a failure that carries no status — which is not expressible as an input document. The behaviour is held instead by two executable gates that drive a scripted fetch, and the byte-level cadence resolution they sit on is already pinned by the cadence-parity vectors, unchanged here."
      },
      "test": "packages/ust-protocol/conformance.mjs :: '#169/F.4 an UNREADABLE cadence lands on the RANGE only' asserts identity by EQUALITY against the 404 run, so a change to what identity resolves to cannot leave it green; the rewritten '#95/F.4 … reported UNKNOWN' holds the non-substitution half. tools/cadence-discovery-gate.mjs :: section 4 asserts the same pair over 500/503/403/429 plus a fetch REJECTED with no status, the live browser shape. Both proven able to fail in both directions — restoring the early return reddens both; collapsing unknown into null reddens only the substitution check, so the two discriminate rather than duplicate."
    },
    {
      "round": 234,
      "math": {
        "excluded": "The round MEASURES the closure rather than moving it. F.4 rev92's disjoint-carrier algebra and round-233's Realization already state that an unknown c_n(t) lands on the RANGE carrier alone; a live run either agrees with that statement or refutes it, and this one agreed. A measurement that confirms a theorem adds no theorem."
      },
      "spec": {
        "excluded": "Nothing normative changed. §20.1 already carries the scope round 233 gave it, and the live publisher turned out to be exactly the case the section describes — cadence signed into the genesis, no log served, a determinate 404. The one spec-shaped finding the run produced (that 404 is unreadable cross-origin, and no serving property requires otherwise) belongs to #168, whose consequence table this round corrects rather than whose rule it writes."
      },
      "code": {
        "excluded": "No source line changed. The round's artifacts are a live A/B across two existing commits, six issue measurements, a version-line row and a sealed diary entry — none of which any verifier reads. The behaviour under test is b383bb0's, unmodified."
      },
      "vector": {
        "excluded": "A vector fixes bytes in and a declared result out, reproducible by a second implementation. This round's subject is a LIVE publisher's transport and stream — a 404 whose CORS header is absent, a hour of slots that will not be re-served identically forever, and a genesis that may be re-rooted. Pinning today's bytes would freeze one operator's current state into the language-neutral corpus and call it the protocol. The behaviour those bytes exercise is already pinned by round-233's two gates, unchanged and green here."
      },
      "test": {
        "excluded": "The round adds no runnable check, and that is a REAL gap rather than a clean exclusion. A live measurement cannot be a gate: it depends on a third party's server, its network and its current state, so wiring it into CI would buy a flake and sell it as coverage. What holds the behaviour is round-233's pair — conformance.mjs '#169/F.4 an UNREADABLE cadence lands on the RANGE only' (identity asserted by EQUALITY against the 404 run) and cadence-discovery-gate section 4 — both re-run green here, and both proven able to fail in two directions. What no check holds is the round's own finding: that a hermetic proof cannot raise the question it was not asked. That is a procedural rule (§6 of the report), and it has no executor."
      }
    },
    {
      "round": 235,
      "math": "is a statement about the TIME carrier, and the document's verdict is not that carrier",
      "spec": "The withheld answer is the ANCHOR COORDINATE's, not the document's",
      "code": "packages/ust-protocol/index.mjs :: verifyCoreInner's anchor step — the early return on a withheld inclusion answer is replaced by a CARRIED timeField; `status:'unavailable'` plus an `unknown` member naming the reason and the connector that claims the construction, which is unmistakable for the `status:'none'` of a document carrying no proof. The two exceptions are preserved rather than swept: `requireAnchored` still refuses (it sees tier !== TOP and names the unavailable substrate), and a READABLE proof that misses its root is still E-ANCHOR. docs/lib/ust-protocol re-vendored so the page serves the same core.",
      "vector": "aca-01-foreign-construction-withholds-only-the-anchor",
      "test": "packages/ust-protocol/conformance.mjs :: 'F.9.5-c.5 THROUGH verify(): a foreign construction withholds the ANCHOR coordinate and nothing else (round-235)' asserts the scope by EQUALITY against the same document with NO proof, so a change to what these documents resolve to cannot leave it green; 'F.9.5-c.5 THROUGH verify(): an UNREADABLE proof is not the same state as NO proof' holds the third-state half. Four checks that pinned the symptom (result === 'INDETERMINATE' over the whole verdict) were re-aimed at the coordinate. Proven able to fail AND to discriminate: restoring the early return reddens 2 registered checks, collapsing unknown into the absent state reddens 1.",
      "_vector_note": "new kind `anchor-construction-absent`. Unlike #169's transport-shaped subject this one IS bytes. Both legs are pinned because the defect lived in their RELATION: the verdict equals the no-proof run, AND the coordinate does not. The path is deliberately one the reference walk would fail, so a runner that walks a tree it was not asked to walk is caught rather than passing."
    },
    {
      "round": 236,
      "math": "the calling MODE rather than the construction",
      "spec": "The connector contract admits BOTH calling modes (round 236)",
      "code": "packages/ust-protocol/index.mjs :: verifyAnchorCore withholds on a promise instead of returning inclusion:false, and verifyAsync pre-resolves the inclusion seam once behind a shim bound to the (content_hash, proof) it was obtained for — the substrate seam's own identity binding, plus the round-17 snapshot so a connector mutating mid-await cannot move the target. packages/ust-rfc6962-verify :: the construction connector lifted VERBATIM out of ust-rekor-verify, which now re-exports it; both plugin loaders and the construction registry learn the name.",
      "vector": {
        "excluded": "Nothing language-neutral to pin. A vector fixes bytes in and a declared result out, and this round's subject is the CALLING MODE of a consumer-supplied function — a JavaScript promise versus a value, which is a property of the host and its faculty, not of any document. The bytes on both sides are identical and already pinned; what changed is whether the verifier can observe an answer about them. The behaviour is held by executable checks that drive both doors over the same document."
      },
      "test": "packages/ust-protocol/conformance.mjs :: '#95 an async connector is NAMED and WITHHELD — a promise is the caller's plumbing, never a refusal of the proof' (re-aimed from an assertion on inclusion===false, which was the symptom), '#95 a connector that RETURNS false has computed and refused — the asymmetry survives the async repair', 'F.9.5-c.5 the ASYNC door reaches the same answer as the sync one — a promise no longer caps a host (round-236)' asserted by EQUALITY between the two doors, and 'F.9.5-c.5 the inclusion await verifies an IMMUTABLE snapshot'. Proven able to fail and to DISCRIMINATE: the sync-refusal mutant reddens exactly one check, the dropped-pre-resolution mutant reddens exactly the other."
    },
    {
      "round": 237,
      "math": "an axiom is a reason to CONCLUDE more, never a reason to OBSERVE less",
      "spec": "A caller assertion does not suppress the witness probe (round 237)",
      "code": "packages/ust-protocol/index.mjs :: resolveByDiscovery — the witness probe is gated on `offline` ALONE, never on what a caller asserts; resolveAuthority's served-list branch carries the caller's axiom beside the earned strength (`override_liftable` on `corroborated`, `independently_verified` still false); the nameAuthoritative test and the π_override projection key on LIFTABILITY rather than on the strength that carries it, so a consumer that was also corroborated can still lift.",
      "vector": {
        "excluded": "Not expressible as bytes. The subject is whether the verifier MAKES an observation — a network act — and the coordinate that follows rides on an unforgeable token minted only by the code that performed the fetch (`VERIFIED_SERVED`). A vector fixes inputs and a declared result; a caller cannot be handed the token, and a document cannot express 'a probe was run'. Held instead by executable checks that count the surfaces fetched and compare two runs differing only in the assertion."
      },
      "test": "packages/ust-protocol/conformance.mjs :: '#171 an added TRUE assertion never LOWERS the verdict' compares two runs differing in nothing else (a named-value assertion would pass for a reason the statement does not make); '#171 the witness probe RUNS under a caller assertion' asserts the observation is still made, which is what keeps the fork refusal reachable; the pre-existing air-gap check is re-aimed at the EARNED basis plus independently_verified:false. Proven able to fail and to discriminate: restoring the skip reddens 2, dropping the axiom from the earned basis reddens 1."
    },
    {
      "round": 238,
      "math": {
        "excluded": "No statement moved. Round 236 already settled the mathematics this depends on — an unobservable connector answer is `⊘`, and the async door widens the OBSERVATION rather than `C_V`. This round supplies a faculty the model already accounts for; a consumer installing a connector is exactly what F.9.5-c.5 ranges over."
      },
      "spec": {
        "excluded": "Nothing normative changed. §11.2 already delegates the construction and already admits both calling modes (round 236). Which connector a given consumer installs is a consumer's choice, not a protocol rule — the spec would be wrong to name one."
      },
      "code": "docs/inclusion-browser.mjs :: the browser CONSTRUCTION connector — reads the declared construction, returns null for anything but rfc6962-raw, computes leaf = SHA256(0x00 ‖ raw32(content_hash)) and binds the existing RFC 6962 walk (docs/ust-resolve.mjs :: rekorInclusion) to the core's seam; never re-implements the climb. docs/index.html passes it as an OPTS member (as a transport capability it is silently ignored).",
      "vector": {
        "excluded": "A vector fixes bytes in and a declared result out. This round adds no rule and changes no verdict function: it supplies a CONSUMER FACULTY, and which faculties a consumer installs is precisely what a language-neutral vector must not pin. The behaviour that IS language-neutral — the rfc6962-raw membership predicate — is unchanged and already pinned."
      },
      "test": "tools/inclusion-connector-gate.mjs :: the browser and Node connectors are driven over a valid path, a wrong root and a foreign construction and must AGREE on each, plus a confirming case so the pair cannot agree only on refusals. Complements unbounded-index-gate, which pins the underlying climb against the Node connector above 2^31 and 2^53."
    },
    {
      "round": 239,
      "math": {
        "excluded": "No statement moved. F-theorem clause 5 already governs this exactly — a verifier holding the inputs and unable to evaluate the function reports INDETERMINATE, never a guessed verdict — and the defect was that one caller reached the faculty while four sites kept the refusal. Supplying a missing INPUT is not a change to the verdict function, and the replay's precondition (a pure, clock-free call) is the invariant this core already states about `verifyCore` rather than a new one."
      },
      "spec": {
        "excluded": "Nothing normative changed. The two refusal shapes are already normative and already separate — INDETERMINATE(unsupported_alg) for a §14 verdict, an E-code in `resolverErrorCodes` for a resolver that has no verdict to give — and this round reads both from those registries instead of restating them. Which primitives a HOST offers is a property of the build, and the spec would be wrong to name one."
      },
      "code": "packages/ust-protocol/index.mjs :: `withSignatureFaculty(runSync, refused, memo)` generalizes the #144 replay loop over the CALL rather than over `verifyCore`, and `resolveByDiscovery` applies it to the three PURE signature-bearing calls it makes after its fetches — genesis `verifyJson`, `resolveCadence`, `resolveAuthority` — sharing one memo. The door itself stays unwrapped: it fetches, and replaying it would replay the network.",
      "vector": {
        "excluded": "A vector fixes bytes in and a declared verdict out, and this round changes neither: the same document under the same rules gets the same answer, in a build that can now perform a step it previously declined. The language-neutral half is already pinned by `faculty-absent-ed25519`, which states BOTH halves — with the faculty and without it — and stays true unchanged."
      },
      "test": "packages/ust-protocol/browser-build.test.mjs :: the browser build is driven through `resolveByDiscovery` over an in-memory transport with a SIGNED cadence entry, and must resolve the publisher, carry the capacity grant and agree with the node build on the same bytes — guarded against vacuity, since two builds that both refuse also agree. Proven able to fail one site at a time: reverting the genesis, cadence or authority wrapper reddens it alone."
    },
    {
      "round": 240,
      "math": {
        "excluded": "No statement moved. F.9.5-c.5 already governs a connector this host cannot reach, and §11.2 already delegates the construction. A connector that is assembled and not delivered is not a different mathematics — it is the same absent-capability case the model already ranges over, reached by a defect in the caller rather than by the world."
      },
      "spec": {
        "excluded": "Nothing normative changed. The spec states that inclusion is delegable and that a verifier without the faculty reports honestly; it does not, and should not, describe how a particular CLI wires its plugins. Naming a surface's plumbing in the protocol text would be the over-reach the spec-neutrality rule exists to prevent."
      },
      "code": "packages/ust-cli/index.mjs :: every site that assembles an optional connector now passes it to the call it was assembled for — 4 assembly sites and 5 calls across this file and packages/ust-mcp/index.mjs, previously zero delivering. Measured before/after on the reference operator's live anchored document: time unproven -> anchored.",
      "vector": {
        "excluded": "A vector fixes bytes in and a verdict out for an implementation that HAS the faculty. What broke here is whether a surface hands its own faculty to the verifier, which is a property of the caller, not of the wire — and a language-neutral corpus must not pin how a port wires its plugins."
      },
      "test": "tools/connector-delivery-gate.mjs :: the domain is read from the source — every `combine*` the core exports, every assembly site in every shipped package — and each assembled name must appear in the call it precedes. Proven able to fail one site at a time; `P.verify` excluded by name because the synchronous door takes no connector."
    },
    {
      "round": 241,
      "math": {
        "excluded": "No statement moved. Self-containment is already the model's premise — a verdict is a function of the document and the verifier's own faculties, with no party in the domain — so the rule adds no obligation the mathematics does not already have. What it adds is a bound on future PROPOSALS, which is a governance object and not a theorem."
      },
      "spec": {
        "excluded": "Deliberately not the spec. The normative text says what a conforming verifier must compute; it does not, and should not, contain a promise about what future versions may be proposed. Putting a self-binding rule in the specification would also make it look like a conformance requirement on implementers, which it is not — it binds this project."
      },
      "code": {
        "excluded": "No code changed and no verdict moves. The property being written down is one the format already has: verification consumes bytes the consumer holds. A code change here would mean the property was NOT already true, which is the opposite of what this round records."
      },
      "vector": {
        "excluded": "A vector pins bytes in and a verdict out. This round adds no rule a document can violate and no verdict a verifier can get wrong — it constrains what may be proposed in future versions, which no corpus can express."
      },
      "test": {
        "excluded": "There is nothing executable to assert: the rule governs proposals, not runs. The only enforcement is a reader holding the text against a future change, and a gate pretending otherwise would be the vacuous kind this repository keeps finding — green because its domain is empty."
      }
    },
    {
      "round": 242,
      "math": {
        "excluded": "No statement moved. The verdict object and its per-axis strengths are unchanged; what changed is which of them a rendering surface consumes. A display that omits a computed axis is not a different function — it is a caller discarding part of the result, the same shape round 240 closed one layer down."
      },
      "spec": {
        "excluded": "Nothing normative changed — and §14 is the SOURCE this round reads rather than edits: it already names identity / time / completeness, and that sentence is now the gate's domain. Adding a display obligation to the specification would bind every implementer to a UI decision that belongs to each surface."
      },
      "code": "docs/index.html :: the page renders the TIME axis it was discarding (strength, status, anchorTime, inclusion, assurance) and completeness beside it; the Proven / Not proven lists are derived from verdict.time and resolution.noFork instead of being literals that contradicted the verdict; content moved last so the axes are above the fold.",
      "vector": {
        "excluded": "A vector fixes bytes in and a verdict out. This round changes no verdict — the same document gets the same answer, displayed differently. What a surface renders is not language-neutral and must not be pinned by a corpus every port would then have to satisfy."
      },
      "test": "tools/verdict-axis-display-gate.mjs :: the axis list is parsed from §14 of the spec, and each axis must be consumed off the verdict by the page; the tier lists must be assembled rather than literal. Proven able to fail on each axis independently, and its own first draft was vacuous through a substring collision that mutation caught."
    },
    {
      "round": 243,
      "math": "Theorem F.7a.2 (a second channel is a second witness of the same value, never a second opinion)",
      "spec": {
        "excluded": "Nothing normative changed and nothing needed to: §10 already required that `ct` be an AEAD encryption of exactly the committed value, that a key-holder verify the decryption reproduces `{nonce,value}` -> commit with E-COMMIT on mismatch, and that AES-256-GCM be used only with a stated unique-nonce-per-key derivation. The model was the layer that had said nothing; the spec had said it all along. The one spec CHANGE is generated, not authored: the hash-domain list renders from REGISTRY and now carries `ust:enc-iv`."
      },
      "code": "packages/ust-protocol/index.mjs :: `encryptPartition` — the producer the mode never had, deriving commitment and ciphertext from ONE `canon` so they cannot drift, with the IV derived as H('ust:enc-iv', commit)[0..12] so IV uniqueness reduces to the commitment uniqueness S10 already demands; `aesGcmEncrypt` in `_crypto.mjs` with a symmetric refusal in the browser build.",
      "vector": "privacy-encrypted-channels-disagree",
      "test": "packages/ust-protocol/conformance.mjs :: the two vectors are executed FROM THE CORPUS so a port runs the same bytes, plus the producer's own obligations — determinism of the derived IV, commitment equality with `blindedCommit`, and a producer↔verifier round-trip."
    },
    {
      "round": 244,
      "math": {
        "excluded": "No theorem changed and none needed to: F.7a.1/F.7a.2 already fixed the admissible disclosure and the two-channel rule. What moved is the BINDING line — #175 had deferred it for want of a producer able to construct the objects the theorems range over, and round 243 built one, so the deferral had no subject left. A binding is an accounting of realization, not a mathematical claim; recording it as this round's math would let a bookkeeping edit stand in for a proof."
      },
      "spec": "**Private partitions — what is NORMATIVE, and what is one build's limit.**",
      "code": {
        "excluded": "No implementation changed, and the round exists BECAUSE none needed to: the four refusals were already correct through both doors. What was wrong was where the corpus stood to watch them — on the object door, which a porting implementation never enters. A round that fixes the observation rather than the behaviour has no code layer, and inventing one would misreport what was repaired."
      },
      "vector": "privacy-encrypted-bytes-wellformed",
      "test": "privacy-encrypted-bytes-enc-not-object: `enc` as a STRING. Carried as BYTES on purpose: a typed decoder (Go struct, serde) meets this before its"
    },
    {
      "round": 245,
      "math": "*Corollary (the inability must be DECLARED, because a failed call and a failed tag are the same observation).*",
      "spec": "**`ct` LAYOUT (normative):**",
      "code": "packages/ust-protocol/_crypto.mjs :: `AEAD_IMPLEMENTED` — the build's declaration, read before any document is, so `⊥` regains one meaning; `hchacha20` derives the XChaCha subkey from one ChaCha20 keystream block by arithmetic (`working = keystream − initial`), leaving no cipher of our own in the TCB; `xchachaEncrypt`/`xchachaDecrypt` over the IETF AEAD Node ships. `index.mjs` decides `enc.alg ∈ Impl` on the declaration and slices `ct` by the registry's per-algorithm nonce width.",
      "vector": "hchacha20-cfrg-2-2-1",
      "test": "faculty-absent-aead-xchacha: with the faculty present the same document verifies — aead-xchacha20-poly1305"
    },
    {
      "round": 246,
      "math": {
        "excluded": "The capability map is a statement about THIS implementation's surfaces, not about the protocol: it says which of our tools expose which act, and a second implementation could expose an entirely different set while conforming perfectly. Nothing here quantifies over documents or verdicts, so there is no theorem to state — and inventing one would put an implementation detail into the model, where the next reader would reasonably take it for an obligation on everyone."
      },
      "spec": {
        "excluded": "For the same reason, one layer down: S16 defines a conforming PRODUCER and VERIFIER, and a command-line tool is neither by virtue of existing. The spec already says what a producer owes; it does not say that our CLI must be one, and it should not — the gap this round exposes is ours to close (#177), not a rule to impose on implementers."
      },
      "code": "tools/capability-parity.mjs :: typed tokens (`cmd:` · `flag:` · `api:` · `tool:` · `arg:`) resolved against the CLI dispatch table, the argument parser, the package exports and the MCP tool schemas — never against the source text; `full` requires EVERY token, matching the predicate round 51 gave the library surfaces.",
      "vector": {
        "excluded": "A vector is a fixed INPUT and a declared RESULT that any runner reproduces. This gate's input is the source of two of our own packages, so a vector would have to carry our CLI and MCP inside the corpus — pinning this repository's shape into the file a PORT reads to check its protocol conformance. The equivalent guarantee lives where it belongs instead: seven negative tokens, including the two bare substrings that produced the false declarations, are refused inside the gate's own controls."
      },
      "test": "tools/capability-parity.mjs :: the gate is a CI step in its own right (`npm run test:parity`), and its controls run on every invocation — each surface form must resolve, and seven tokens naming nothing (among them the bare `seal` and `buildState` that produced the false declarations) must not."
    },
    {
      "round": 247,
      "math": {
        "excluded": "F.7a already fixes what a disclosure is and F.7a.2 what two channels owe each other; this round moves that reach out to two surfaces and changes no theorem. The one thing here that WOULD be a model question — whether a verdict must report which channels it was able to check — is a defect this round FOUND and deliberately did not fix, since it changes the verdict shape for every implementation. It is recorded on #177 rather than smuggled into a surface round."
      },
      "spec": {
        "excluded": "§14 step 8 states the obligation ('if authorized: reproduce its commit … and for encrypted verify AEAD-Decrypt') and §17 states the inputs. Neither says which of OUR tools must accept them — nor should it: the spec binds conforming producers and verifiers, and a CLI flag or an MCP schema property is this implementation's business. The gap was ours to close, not a rule to impose on implementers."
      },
      "code": "packages/ust-mcp/index.mjs :: `decKeys` on `ust_verify` — schema, handler signature and the options object, so ONE call reaches both privacy modes; packages/ust-cli/index.mjs :: a `private` line naming what opened AND what stayed closed (the negative half is the one a reader cannot reconstruct), and `resolveByDiscovery` now receives the consumer's envelope by SPREAD instead of a hand-copied field list that had silently dropped it.",
      "vector": "privacy-encrypted-channels-disagree",
      "test": "packages/ust-mcp/live-test.mjs :: live:decKeys is genuinely USED — channels that disagree give the agent E-COMMIT, not VALID"
    },
    {
      "round": 248,
      "math": "*Corollary (authorization is per-CHANNEL, so a partial disclosure may not be reported as a whole one).*",
      "spec": "**Authorization is PER-CHANNEL, and a partial check MUST NOT be reported as a whole one.**",
      "code": "packages/ust-protocol/index.mjs :: step 8 splits the two channels — a partition whose AEAD could not be examined goes to `disclosed_partial` with `checked`/`unchecked`/`needs_key_id` instead of being pushed into `disclosed`; packages/ust-cli/index.mjs shows the three resulting states; packages/ust-mcp/live-test.mjs asserts the distinction reaches an agent over the wire.",
      "vector": "privacy-encrypted-channels-disagree",
      "test": "privacy-encrypted-channels-disagree: WITHOUT the key the divergence is invisible — VALID, and the value is reported as PARTIAL, never as disclosed"
    },
    {
      "round": 249,
      "math": "*Corollary (an identifier the verdict carries is STRUCTURE, and structure a publisher writes may not forge it).*",
      "spec": "**IDENTIFIERS carry a further restriction, and VALUES do not.**",
      "code": "packages/ust-protocol/index.mjs :: §4.4 splits the two private productions (a `blinded` partition carrying `enc` is E-MALFORMED at admission) and §6 constrains the identifiers the verdict quotes — partition names and `enc.key_id` — against control and bidi-override characters, with `quoteSafe` inside `bad()` covering all 37 diagnostic quotations in one place. The same two rules re-implemented independently in `docs/ust-verify.mjs` (clean-room) and synced byte-for-byte to `extension/lib/ust-verify.mjs`.",
      "vector": "identifier-forges-structure-partition-name",
      "test": "identifier-forges-structure-partition-name: and NO field of the verdict carries such a character — including the diagnostic that quotes the rejected value"
    },
    {
      "round": 250,
      "math": {
        "excluded": "The rule this gate enforces is already a theorem — F.7a.2's unexaminable-channel corollary, stated and bound in round 249. What changed is the DETECTOR: from a vector that pins one case to an enumeration that decides every cell of the product. A change in how thoroughly a stated claim is checked is not itself a claim, and inventing a theorem for it would put a testing decision into the model, where a reader would take it for an obligation on implementers."
      },
      "spec": {
        "excluded": "Same reason one layer down: §4.4 already states the closed envelope per shape and §6 the identifier rule — both written in round 249. A conforming implementation owes those rules; it does not owe our method of checking that it meets them. Writing the enumeration into the spec would confuse a normative obligation with a repository practice."
      },
      "code": "tools/envelope-domain-gate.mjs :: the (kind × privacy × foreign-member) product rebuilt, signed and verified every run against BOTH the reference and the clean-room verifier, with bases read from REGISTRY.partitionKinds; docs/ust-verify.mjs gains the closed envelope it never had (39 of 42 cells diverged on the first cross-implementation run), synced byte-for-byte to extension/lib/ust-verify.mjs.",
      "vector": "privacy-blinded-carrying-enc",
      "test": "tools/envelope-domain-gate.mjs :: the gate is a CI step in its own right (`npm run test:envelope-domain`), and carries its own controls — a legal encrypted partition must be admitted and the exact #177 shape must be refused, so a build refusing everything cannot pass it."
    },
    {
      "round": 251,
      "math": {
        "excluded": "The three states this page now draws are F.7a.1's per-channel corollary, stated and bound in round 248; nothing here changes what a verifier must decide. A rendering decision is not a claim about documents, and putting one in the model would read to an implementer as an obligation on their own surfaces — which it is not: a conforming verifier owes the DISTINCTION, never a particular glyph."
      },
      "spec": {
        "excluded": "Same reason: §14.8 already requires the distinction to be reported and §16 already states which outcomes bind a conforming verifier. How a surface DRAWS it is this implementation's business, and the spec deliberately does not describe our page."
      },
      "code": "docs/index.html :: the disclosure envelope and key map as offline inputs, spread into all three call paths; `privateMd` draws the two §14.8 channels with a fixed-length seal (`#` for blinded, U+25AE for encrypted) so the render cannot leak the hidden value's size; the md/raw toggle extended to private partitions, where raw is the on-wire envelope.",
      "vector": {
        "excluded": "A vector is a fixed input and a declared RESULT a runner reproduces. This round changes no verdict — measured: the same document yields the same verdict, tier and disclosure fields before and after. What changed is what a human SEES, which no runner can assert and which the corpus would be wrong to pin: a port owes the distinction, never our glyphs."
      },
      "test": "tools/docs-verifier-parity.mjs :: the page's verifier is checked against the reference on every vector in the corpus, so a rendering change that quietly altered a verdict would be caught there; the render itself was measured in a browser, which is where layout is true."
    },
    {
      "round": 252,
      "math": {
        "excluded": "Nothing about what a verifier decides changed, and nothing about what a document means. The claim underneath — that a public reader learns existence and time and NOT size — is §10's, stated long before this render existed. What moved is whether our own constant can be checked against that claim rather than trusted: a property of this page's source, not of the protocol."
      },
      "spec": {
        "excluded": "Same reason. §10 already withholds the value's size from a public reader; §14.8 already requires the per-channel distinction. Neither says how wide a glyph run should be, and neither should — a conforming implementation owes the distinction, never our typography."
      },
      "code": "docs/index.html :: `sealFor(privacy, label)` repeats the mode's glyph `label.length` times, so the seal is exactly as wide as the word naming its channel; the count is derived from this page's own vocabulary and cannot encode anything about the hidden value.",
      "vector": {
        "excluded": "A vector pins an input and a declared RESULT a port reproduces. No verdict changed — measured on the same document before and after this edit. Pinning a glyph count into the corpus a stranger runs would make our typography part of their conformance, which is exactly backwards."
      },
      "test": "tools/docs-verifier-parity.mjs :: the page's verifier is checked against the reference on every corpus vector, so a render edit that altered a verdict would be caught; the width itself was measured in the browser, where layout is true — label 10, run 10, on all five channel rows."
    },
    {
      "round": 253,
      "math": {
        "excluded": "No theorem moved. `ust sign` builds what F.7a.1's producer corollary already required to exist, and the clean-room's step 8 implements §14.8, stated and bound in round 248. A second implementation catching up to a rule is evidence the rule was reachable, never a new claim — recording it as math would let an implementation gap masquerade as a discovery."
      },
      "spec": {
        "excluded": "§10 already states the nonce obligation (fresh, unique, never value-derived) and leaves key management to the operator; §14.8 already requires the commitment reproduction this file was missing. The spec binds conforming producers and verifiers — it does not describe our CLI's flags, and writing `--disclosures-out` into it would make one tool's ergonomics an implementer's obligation."
      },
      "code": "packages/ust-cli/index.mjs :: `cmdSign` — privacy declared per partition in the input, one fresh nonce per private member, `--disclosures-out` refused-if-absent, no key invented, and the output re-opened from the envelope before either is written; docs/ust-verify.mjs :: §14 step 8 (commitment reproduction + AES-GCM through WebCrypto, XChaCha honestly reported as this build's limit), synced byte-for-byte to the extension; tools/docs-verifier-parity.mjs :: vectors carry their disclosures and keys into BOTH verifiers, with capability boundaries separated from divergence.",
      "vector": "privacy-encrypted-channels-disagree",
      "test": "tools/docs-verifier-parity.mjs :: the gate is a CI step (`npm run test:docs-parity`) and now prints how many vectors carried an auxiliary input, so a refactor that stopped threading them would show as zero rather than as silent agreement."
    },
    {
      "round": 254,
      "math": {
        "excluded": "A support page states which of OUR tools expose which act. It quantifies over this repository's surfaces, not over documents or verdicts, so there is no property of the protocol to state — and a second implementation could expose an entirely different set while conforming perfectly. Putting it in the model would turn an inventory into an obligation."
      },
      "spec": {
        "excluded": "Same reason one layer down: §16 defines a conforming PRODUCER and VERIFIER, and neither our CLI nor our page is one by virtue of existing. The spec deliberately does not describe this implementation's surfaces, and SUPPORT.md deliberately does not describe the protocol."
      },
      "code": "tools/gen-support-matrix.mjs :: renders SUPPORT.md from the CAPS/SURFACES declarations in capability-parity.mjs — refusing to render at all if its parse of those shapes comes back short, since an empty table would print as an answer; tools/capability-parity.mjs :: `disclosure` renamed `disclosure-produce` and owned by the CLI, `sign` promoted to full and `build-transcript` demoted to subset, each against a live probe.",
      "vector": {
        "excluded": "A vector is an input and a declared RESULT a port reproduces. This page is an inventory of THIS implementation, and pinning it in the corpus a stranger runs would make our tool surface part of their conformance — the precise inversion the page's own header warns against."
      },
      "test": "tools/gen-support-matrix.mjs :: a CI step in its own right (`npm run test:support-matrix`), which regenerates and `git diff --exit-code`s the page, so a hand edit or a drifted declaration fails the build rather than shipping a promise."
    },
    {
      "round": 255,
      "math": {
        "excluded": "Every rule this round brings to the floor is already a theorem or a stated obligation: F.7a.1's per-channel corollary, F.7a.2's unexaminable-channel corollary, §13's breadth bound, §4.4's kind domain. A third implementation catching up to them is evidence they were reachable, never a new claim — and recording it as math would let an implementation gap read as a discovery."
      },
      "spec": {
        "excluded": "§10 already defines both privacy modes and leaves key management to the operator; §4.4 already registers three kinds; §13 already bounds breadth per node. The spec binds any conforming implementation, and `ust-light` was simply not yet one of them on these axes. Nothing here is a rule the standard lacked."
      },
      "code": "packages/ust-light/index.mjs :: `blindPartition` and `encryptPartition` at the MTI (WebCrypto AES-GCM, IV derived from the commitment), §14 step 8 with per-channel reporting, the `absence` kind, §13's breadth bound, the closed private production and the §6 identifier restriction; packages/ust-light/test.mjs :: a corpus leg comparing every document-bearing vector against the core, auxiliary inputs travelling with each.",
      "vector": "partition-kind-absence-admitted",
      "test": "packages/ust-light/test.mjs :: a CI step in its own right (`npm run test:light`), whose corpus leg compares EVERY document-bearing vector against the core with each vector's auxiliary inputs, and asserts the population is a population — a slice that shrank to nothing would report perfect agreement."
    }
  ],
  "first_accounted_round": 76,
  "_first_accounted": "The first version-line round the live coverage leg enforces. Rounds below it predate the rule and are reported, never gated — the same honesty as the ladder pin, without pretending history complied."
}
