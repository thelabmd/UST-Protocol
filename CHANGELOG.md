<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Changelog

The wire format `ust:"1.0"` is **stable across every release candidate** — pin exact package versions. This log
tracks two lines: the **reference checker (L1 TCB)** rev-ladder — a recurring diverse-model adversarial audit →
math-first remediation — and the **protocol milestones**. The normative source is the git history plus the
conformance vectors; this file is the readable map.

## [Unreleased] — rc.37 line

### Reference checker (L1) — the audit rev-ladder

Each rev independently reproduces an external model's findings, adjudicates each against the formal model, and
fixes **structurally, not point-wise**. The conformance vectors + byte corpus + robustness fuzz witness every fix.

| rev | round | what closed |
|-----|-------|-------------|
| **rev55** | 46 | **the Reduction metatheorem: the verifier is an AUTOMATON over canonical bytes, not a COMPUTER over live objects — the trust config is now reduced SIDE-EFFECT-FREE** — stepping back from 46 rounds of boundary bugs to first principles: every P0/P1 of the recent rounds (a two-face getter, a cross-argument mutation, an admission-order dependency, an adapter re-reading a live field) is a SYMPTOM of PROCESSING LIVE OBJECTS. The byte kernel `checkAuthorityProofBytes` (which held every round — 4007-probe fuzz, 0 false accepts) IS the automaton; the object adapters that failed were entries not decomposed as `automaton ∘ reduce`. **Theorem R** (new, in the model): every public entry `E(x₁..xₙ) = A ∘ (ρ₁..ρₙ)` — independent per-argument reductions to canonical form, then a pure automaton. Two reduction disciplines: an UNSIGNED trust config is read from DATA descriptors (an accessor/getter/`toJSON` is REJECTED, **never executed** — the automaton reads its input as DATA, it never runs it); a SIGNED proof is read `[[Get]]`-once + integrity-verified by content-hash. Under Theorem R, cross-argument mutation is IMPOSSIBLE (an unsigned arg's code never runs, so it cannot rewrite a sibling; reductions are independent, so order is irrelevant). **This rev** reduces the authority adapters' consumer `trust`/`policy` config through `admitInert` (side-effect-free): a config getter/`toJSON` no longer executes, superseding the rev45 config-first *ordering* with the automaton-faithful rule (no caller code runs at the boundary). The rev45/rev54 cross-argument P0s are now closed structurally, not by ordering. conformance 607/0; byte 139, arc 65; reference-checker 59/0, fuzz clean; security 34/0. |
| **rev54** | 45 | **cross-argument admission ORDER (trusted before untrusted) + a BEHAVIORAL adapter/kernel gate instead of a source regex (round-45: 1 P0 + 2 P1)** — a from-SOURCE regex gate cannot see a SEMANTIC violation. **P0-01 (R1/R3):** both object adapters encoded the UNTRUSTED argument BEFORE the TRUSTED config — `checkAuthorityProof` ran `encodeLive(obj,'package')` before `encodeLive(config,'config')`, and `verifyAuthorityBundle` ran `JSON.stringify(inputs)` before `JSON.stringify(config)` (a bug introduced at rev52). A hostile getter/`toJSON` in the untrusted arg executed and MUTATED the still-live consumer config (adding witnesses, setting `uniqueness_threshold:1`) before it was captured, so the untrusted proof selected the trust world its own proof was checked under. Fix: admit the TRUSTED config FIRST, then the untrusted proof — a cross-arg mutation now hits only the already-captured original. **P1-01:** `verifyAuthorityBundle` returned a non-`Freshness` judgment as a public `VALID` (a Genesis-only proof read as success) and normalized a malformed `policy` away with `C?.policy || {}`. Fix: success is EXCLUSIVE to `Freshness` (Genesis-only → `INDETERMINATE`), and a present malformed `trust`/`policy` is rejected (defer to the sole checker). **P1-02:** the from-source gate stayed green through all of it — a regex cannot model admission order / cross-argument mutation / target-judgment / adapter-kernel agreement. Fix: a BEHAVIORAL gate that DRIVES each adapter through its entrypoint — a hostile getter must not change the config the verdict uses, the adapter must AGREE with the sole checker, and a non-target judgment must not become success. A class whose invariant is SEMANTIC is closed by a behavioral from-entrypoint gate, not a source regex. conformance 607/0; byte 139, arc 65; reference-checker 59/0, fuzz clean; security 34/0. |
| **rev53** | 44 | **the public adapter must not manufacture what the sole-checker refuses + it is an admission boundary (round-44: 1 P0 + 2 P1)** — rev52's gate scanned only `index.mjs`; the P0 lived one file over. **P0-01:** `verifyAuthorityBundle` (the public authority adapter, in `reference-checker.mjs`) built the config with `uniqueness_threshold: Number.isInteger(trust.uniqueness_threshold) ? trust.uniqueness_threshold : 2` — an ABSENT or MALFORMED consumer threshold was replaced with a MANUFACTURED `2`, so two witnesses returned `VALID`/`witness-attested` under a quorum policy the consumer never selected, while the inner `checkAuthorityProof` fails such input CLOSED (`INDETERMINATE`/`E-CONFIG-THRESHOLD`). The adapter contradicted the sole-checker. Fix: pass the consumer policy THROUGH (absent → omitted → `INDETERMINATE`; malformed → passed → `E-CONFIG-THRESHOLD`). **P1-02:** the adapter read `trust.uniqueness_threshold` TWICE off the live nested config, so a two-face getter upgraded one witness into a quorum; it now takes a one-read inert JSON snapshot of both graphs (`JSON.parse(JSON.stringify)`, like `forkChoice`). **P1-01:** `verifyAuthorityCheckpointChain`'s `recoveryKeys`/`genesisAuthority` selectors were coalesced (`recoveryKeys || {}`) or silently overridden — now they fail closed on a present non-record, and the from-code gate scans BOTH files, bans `X || {}`/`X || []` as well as `Array.isArray(X)?X`, and covers the recovery/authority selectors. A public adapter around a sound kernel is a TCB surface too: admit once, defer to the kernel's contract, never inject a default it would reject. conformance 602/0; byte 139, arc 65; reference-checker 59/0, fuzz clean; security 34/0. |
| **rev52** | 43 | **the coerced-input class closed at a FROM-CODE GATE, not per-site (round-43: 0 P0 + 2 P1)** — the coerced-boolean and falsy-selector classes recurred FOUR rounds (rev48 `maxSupportedBytes`, rev50 `genesis`/`pinnedKeys`, rev51 three grant booleans) because each fix was per-SITE. Round-43 found four MORE: `requirePerFrameValid` (a falsy `0` DISABLED per-frame signature verification → a tampered frame passed as `complete`), `allowExperimentalAttested` (a truthy `"false"` ENABLED the withheld experimental rung), `resolveCadence`'s `keylog` (a present `false` erased a retirement and accepted a change signed by a retired key), and `verifyAuthorityCheckpointChain`'s `else if (genesis)` (a present `false` fell through to a pinned fallback root). Fix: (1) the four sites route through `admitBool`/typed selector admission; (2) the RESTRICTION booleans (`requireAuthoritative`/`requireFreshKeylog`/`requireAnchored`) are measured too (a coerced `0` no longer silently drops the policy); (3) **the structural closure — a from-code conformance GATE: no authority selector may use the `Array.isArray(X) ? X : default` coalesce, and every `require*`/`allow*`/`acceptConsumerOverride` grant boolean is REGISTERED + `admitBool`-admitted + adversarially probed, so a NEW grant flag or coalesce fails the gate until closed.** A defect class is not closed by fixing its known instances — it is closed by a from-code invariant that FAILS when a new one appears. conformance 600/0; byte 139, arc 65; reference-checker 59/0, fuzz clean; security-regression 34/0. |
| **rev51** | 42 | **R3 applies to ARRAY ELEMENTS — the key-log is DEEP-admitted (first P0 since rev45); the override channel is a PROVENANCE gate, not truthiness (round-42: 1 P0 + 2 P1)** — rev50 hardened the top-level snapshots, but R3 was not applied one level INTO an array. **P0-01 (R1/R3):** `resolveKeys` deep-admitted `genesis` but ran the key-log through `admitArray`, which SHALLOW-copied (`out[i] = v[i]`) — every element stayed LIVE. The reducer calls `verify(e)` (validates ONE face) then RE-READS `e.state`/`e.sig`/`contentHash(e)` from the live element, so a two-face entry showed a SIGNED key B to `verify` and an UNSIGNED key C to the re-reads → `resolveKeys` authorized attacker key `C` with no accepted signature. Fix: `admitArray` DEEP-admits + freezes each element — one frozen graph, one face. **R3 is not just "admit the top-level document"; every untrusted ARRAY the reducer re-reads must be deep-admitted.** **P1-01 (F.5a.1):** the `consumer-override` channel was entered by truthiness (`noForkConfirmed || corroborated || servedNoFork`), so an unminted `servedNoFork:{}` crossed into override and, with `acceptConsumerOverride`, reached HIGH. Fix: only an explicit boolean AXIOM (`noForkConfirmed`/`corroborated===true`) creates a LIFTABLE override; an unminted `servedNoFork` is a look-alike that DIVERTS from corroborated (rc35-P0a) but carries `override_liftable:false` and never reaches authoritative. **P1-02 (I4):** the GRANT booleans were read by JS truthiness, so the string `"false"` — truthy — activated the HIGH override; `admitBool` now accepts only a real boolean (else `E-MALFORMED`), and `verifyStream` no longer coalesces a present non-array `keylog` to `[]`. One rule: a security decision reads a MEASURED input, never a coerced one. conformance 592/0; byte 139, arc 65; reference-checker 59/0, fuzz clean. |
| **rev50** | 41 | **"inert" means IMMUTABLE — the admitted `opts` snapshot is FROZEN + the falsy≠absent guard swept to the authority SELECTORS (round-41: 0 P0 + 2 P1)** — rev49 admitted `opts` at every door, but the snapshot was not INERT in the strong sense. **P1-01 (R1/R3):** `admitOpts` returned a null-proto object with `writable` fields, WITHOUT `Object.freeze` (its nested values were frozen by `admitDeep`, the top-level wrapper was not). A preserved capability is invoked with that snapshot as `this` (`opts.resolveRef(h)`), so a hostile resolver ran `this.acceptConsumerOverride = true` — mutating policy DURING verification — and `verifyCore`'s later read flipped the tier LIGHT→HIGH. Fix: `admitOpts` returns `Object.freeze(out)` (read-only fields) — the snapshot is IMMUTABLE, a capability `this`-write is a no-op/strict-throw (fail-closed). This sharpens R1: "after 𝒜, x is DEAD" means IMMUTABLE, not merely a copy. **P1-02 (R1 + I4):** the rev49 falsy≠absent guard (`capAssurance`) was NOT swept to the authority SELECTORS — `resolveAuthority`/`verifyCore` guarded `genesis` with `if (!genesis)`/`if (opts.genesis)`, and `admitDeep` accepts a scalar, so a PRESENT falsy `genesis` (`0`/`''`/`NaN`/`false`) read as ABSENT → `self-asserted`/`VALID:LIGHT` instead of `E-GENESIS`; same shape at `pinnedKeys` (non-array pin set silently dropped) and `verifyStream` (`genesis`/`checkpoint`). Fix: a selector treats only `undefined`/`null` as absent; a present non-record is MALFORMED (`E-GENESIS`/`E-MALFORMED`/`complete:none`). The line that held: EVIDENCE fields (`nameMap`/`noForkEvidence`/`servedNoFork`) fail SAFE (present-invalid → no upgrade, by design); authority SELECTORS must fail CLOSED. conformance 587/0; byte 139, arc 65; reference-checker 59/0, fuzz clean. |
| **rev49** | 40 | **the sync `verify` door admits `opts` (the last un-admitted public opts input) + `capAssurance` distinguishes ABSENT from MALFORMED — falsy ≠ absent (round-40: 0 P0 + 2 P1)** — rev48 made the lattice total, but the audit found two R1 admission gaps one level out. **P1-01 (R1):** the synchronous `verify(doc, opts)` admitted `doc` but forwarded the LIVE `opts` to `verifyCore` — every OTHER public entry (`verifyAsync`/`verifyJson`/`resolveAuthority`/`forkChoice`/`resolveCadence`/`resolveByDiscovery`) already routed `opts` through `admitOpts`, but the most-used door did not; `verifyCore` reads `opts.maxSupportedBytes` more than once, so a two-face `opts` Proxy showed a 1-byte capability to the budget and `undefined` to the guard → a verifier-owned resource refusal became `VALID`. Fix: `verify` admits `opts` at the door (functions preserved, scalars frozen ONCE), the trap now fires 0×. **P1-02 (R1 + F.5 gap 2):** `capAssurance` guarded the ceiling with `if (!ceiling) return s`, so a FALSY ceiling (`0`/`''`/`NaN`/`false`) read as ABSENT returned the full proven state, and a truthy NON-record (`1`/`'x'`/`[]`) defaulted every axis to MAX — both PRESERVED `TOP` where the rule requires ⊥ (the same `falsy = absent` conflation `maxSupportedBytes && …` carried at rev48). Fix: only `undefined`/`null` are absent (identity); a non-record / rejected admission / out-of-range axis caps to ⊥. Both are the SAME rule R1 realized incompletely: admission must happen at EVERY door (`opts`, not only `doc`), and an admission guard must distinguish ABSENT from MALFORMED — a falsy value is not a missing one. conformance 581/0; byte 139, arc 65; reference-checker 59/0, fuzz clean. |
| **rev48** | 39 | **the assurance lattice made a TOTAL function (a door that RETURNS, not throws) + the R4 budget footgun removed (round-39: 0 P0 + 2 P1)** — rev47 claimed the frame uniform, but the audit found the SAME two rules realized as HALF-measures one level below. **P1-01 (R4):** round-38's `admitBudget` correctly returns `null` on a refused budget, but the call site wrote `admitBudget(opts.refBudget, 256) ?? 256` — and `?? 256` converted the refusal back into the FULL budget; a sibling, `maxSupportedBytes`, was read raw so `0`/`NaN` (falsy) bypassed and `Infinity` disabled the capability check. A faculty admission is realized only if its `null` FAILS CLOSED, never coalesces to a default. Fix: both scalars route through `admitBudget`, a `null` returns `E-MALFORMED` (the `?? default` footgun removed). **P1-02 (R1):** the lattice was still PARTIAL — `assuranceState` (the assurance door) THREW a coded `E-ASSURANCE`, so `capAssurance` (reads an untrusted consumer `ceiling`) and the exported `checkBounds` HOST-threw as consumer surfaces, yet both were mis-classified `primitive` and exempt from the sweep. A rule realized as a coded THROW at a consumer surface is not realized: the sweep demands a RETURN, and a lattice op is mathematically TOTAL (meet = min, join = max), so a malformed operand maps to ⊥ and the op RETURNS. Fix: the door mirrors the input door — `assuranceState` RETURNS a reject sentinel (like `admitDeep`→`ADMIT_REJECT`), never throws; every lattice surface fails closed by RETURN (`meet`→⊥, `join` treats a malformed operand as the ⊥ identity so it never synthesizes strength, `assuranceLE`→`false`, `projectTier`→`NONE`, `capAssurance` admits its `ceiling` and caps to ⊥); `checkBounds` admits its `doc` at the public door (the hot path calls private `boundsOf` over the already-admitted snapshot, no re-clone); all RECLASSIFIED as consumer surfaces and swept. The lattice is now total into `AssuranceState ∪ {⊥}`. conformance 577/0; byte 139, arc 65; reference-checker 59/0, fuzz clean. |
| **rev47** | 38 | **R1/R3/R4 made uniform at the last three exported ops (round-38: 0 P0 + 3 P1) — the whole controller frame is now uniform** — rev46 made R1 uniform, but the audit found the SAME three rules still broken one level below. **P1-01 (R1/R3):** `assuranceState` read each axis `s[ax]` TWICE (rank check + output copy), so a two-face Proxy passed a weak face to the check and emitted a strong one → `projectTier` returned a false `TOP` from a value it never validated. Fix: admit `s` ONCE with `admitDeep` (total; a hostile getter → coded `E-ASSURANCE`, never a host throw) and read each axis ONCE from the frozen snapshot. **P1-02 (R1):** `quorumTrustDomains` (iterated the raw `list`) and `compareEvidenceOrder` (admitted neither operand) were classified `primitive` and escaped the surface sweep — a hostile Proxy threw a host exception. Fix: admit each operand with `admitDeep` (fail-closed to `count:0`/`unproven`) + RECLASSIFY both as consumer surfaces in the totality registry. **P1-03 (R4):** `verifyJson.maxInputBytes` had no clamp — a caller `Infinity` EXPANDED the verifier-owned 64 MiB ceiling. Fix: **`admitBudget(supplied, reference)`** — a caller scalar may only TIGHTEN (`min(reference, supplied)`; `Infinity`/`NaN`/≤0 → refused), applied at `maxInputBytes` AND a sibling the sweep found the auditor missed (`refBudget`). The controller frame is now uniform in all four rules: every op admits its input ONCE (R1), computes over the admitted snapshot + its own faculties (R2), emits only projections of it (R3), and lets caller policy only TIGHTEN a verifier-owned faculty (R4). conformance 569/0; byte 71, arc 65; reference-checker 59/0, fuzz clean. |
| **rev46** | 37 | **R1 (admit the input) UNIFIED at every exported op — the last two un-admitted inputs (a Merkle co-path, the lattice operands) (round-37: 1 P0 + 1 P1)** — the authority primitives held; the audit found the SAME controller rule (R1: admit input before processing) missing at two ops one level below. **P0-01:** `verifyKeylogTerminality` / `verifyCheckpointMapUniqueness` checked only that `siblings` was a length-N array, then built node preimages with JS `+` — a `[]` sibling coerced to `''` computed a root under a NON-protocol grammar (minting `corroborated`/experimental `attested` freshness), and a null-proto object threw a host `TypeError` (totality break); the kernel already types its co-paths (`pHashArr`), a public↔kernel split. Fix: **`admitHashPath(siblings, len)`** — a length-`len` array of canonical sha256 hashes, preimages built from admitted strings; both Merkle verifiers route through it. **P1-01:** `joinAssurance`/`meetAssurance`/`assuranceLE` compared RAW per-axis ranks, bypassing `assuranceState`; an out-of-domain axis (rank −1) let the other operand's value replace it and `join` SYNTHESIZED a valid-looking `TOP`. Fix: every lattice op ADMITS both operands with `assuranceState` (out-of-domain → `E-ASSURANCE`, never a synthesized state). A whole-surface sweep confirmed this was the LAST gap: every exported verifier/algebra op now admits its untrusted input through ONE primitive (`admitDeep`/`admitOpts`/`admitSigner`/`admitAuthorityKey`/`admitHashPath`/`assuranceState`/`decodeExact`) before processing — **R1 is uniform at every level**, the invariant the recurring findings were tracing toward. conformance 563/0; byte 71, arc 65; reference-checker 59/0, fuzz clean. |
| **rev45** | 36 | **the nested authority KEY-PAIR at ONE primitive (`admitAuthorityKey`) + canonical wire at the crypto LEAF — the first 0-P0 round (round-36: 0 P0 + 2 P1 + 1 P2)** — rev43 closed the SIGNER wrapper at `admitSigner`, but the audit found the SAME shape one level down: a `{ key_id, pub }` authority PAIR was typed as two INDEPENDENT strings with no identity RELATION, so when both recovery signers agreed on a CONTRADICTORY pair (`key_id` of A, `pub` of B) the quorum carried it and `verifyCheckpointRecovery` returned a usable-looking authority with `key_id ≠ keyId(pub)` (P1 — the chain verifier then fails closed absent a hash collision). Fix: **`admitAuthorityKey({key_id,pub})`** (exact pair + Pub32 + `key_id === keyId(pub)`), routed through EVERY authority pair — recovery replacement, transition destination, genesis authority, pinned prior, checkpoint rotation, each genesis recovery key. **P1-02:** `edVerifyStrict` was MISNAMED — it delegated sig bytes to Node's permissive base64url decoder, so the provenance `src_sig` path authenticated a non-canonical alias a strict impl rejects (raw-wire split). Fix at the crypto LEAF: `edVerifyStrict` now requires canonical Pub32/Sig64 before verifying, so EVERY caller is canonical-safe with no per-site guard. **P2-01:** the vacuous recovery regression vector (one malformed + one genuine → quorum-not-met for the wrong reason) was replaced by a threshold-complete malformed quorum that exercises the property. conformance 551/0; byte 71, arc 65; reference-checker 59/0, fuzz clean. **P0 count 1→2→4→3→0: the class converges when each recurring shape closes at a single primitive or the leaf + a gate, not a hand-mirror.** |
| **rev44** | 35 | **proactive self-review hardening (before the next audit): `valid_as_of` removed from the no-fork claim** — the rev43 self-review noticed a self-declared TIME field (`valid_as_of`) still permitted in the signed `ust:name-no-fork` claim (unused, optional). Per assurance-never-self-declared, a signer cannot self-declare time in a signed authority claim (time comes from an external anchor, not the witness). Removed from `noForkClaim` + `NOFORK_CLAIM_SCHEMA`; a signed no-fork claim carrying `valid_as_of` now decodes to a closed-ADT rejection. Handling what the review saw instead of posting it as a divergence for the audit to find. conformance 547/0. |
| **rev43** | 35 | **the signer-admission class closed at ONE primitive (`admitSigner`), not per witness (round-35: 3 P0 + P1)** — rev42 swept the closed-ADT across witnesses but HAND-mirrored each sig wrapper, and the hand-work left the SAME sub-check out at three sites: `verifyCheckpointUniqueness` / `verifyCheckpointRecovery` / `verifyNoForkEvidence` bound `keyId(pub)===issuer_id` but never `sig.key_id` — so a witness with a FOREIGN `sig.key_id` (or, for no-fork, `alg:"RSA"` + an open wrapper/envelope) was admitted publicly and minted the strong coordinate (`attested` / a recovery `ChainHandle` / `authoritative`→`VALID:HIGH`) while the kernel binds `sig.key_id === issuer_id === keyId(pub)` and rejects it. Fix (the audit's + the meta-lesson's recommendation): do NOT repair the sites independently — **one shared `admitSigner(sig, expectedKeyId)`** (exact Ed25519 `{alg,key_id,pub,sig}` + `expectedKeyId===sig.key_id===keyId(pub)` + Pub32/Sig64) consumed by EVERY signed authority witness (receipt, checkpoint, transition, uniqueness, recovery, no-fork) BEFORE grouping/identity/verdict/mint; no-fork is now inside the closed-ADT sweep. A machine-check gate asserts the choke-point rejects every sig-wrapper tampering class, so a wrapper divergence cannot ship. **P1-01:** the recovery `reason` (rev42) was removed from the signed claim — the normative tuple is `(domain, epoch, last_accepted, effective_sequence, replacement_authority)`. conformance 546/0; byte 71, arc 65; reference-checker 59/0, fuzz clean. Meta: the audit P0 count ran 1→2→4→3 — the fix that converges is a single choke-point + a gate, not another hand-mirror. |
| **rev42** | 34 | **the closed-ADT-at-admission class SWEPT across every signed authority witness (round-34: 4 P0, one class)** — rev41 closed the class for evidence receipts; I posed the sweep as a divergence instead of doing it, and round-34 answered **P0=4**: the class was open across genesis/checkpoint/uniqueness/epoch/recovery. **P0-01:** `resolveCheckpointRoots`/`authorityCheckpointSigOk`/`verifyAuthorityCheckpointChain` checked only `keyId(pub)===key_id`, never strict Pub32 — Node's base64url decoder maps a non-canonical trailing-bit alias (`…YKc`→`…YKd`) to the same 32 bytes, so an alias rooted a branded authority the kernel `strictPub` rejects. **P0-02:** the checkpoint sig envelope was not closed, so an unsigned `sig.extra` shifted the `authorityCheckpointId` (hashes `{body,sig}`) and minted a chain pin. **P0-03:** `verifyCheckpointUniqueness` accepted an `as_of`/extra-field claim + `alg:"RSA"` sig the kernel `VOTE_CLAIM`/`SIG_ENV` reject (and the builder emitted `as_of`/`observed_map_root` ∉ the kernel claim). **P0-04:** `verifyEpochTransition`/`verifyCheckpointRecovery` accepted open records + untyped sig wrappers. Fix: a shared strict `Pub32` at every authority-bearing public field + `closedCheckpointWitness`/`closedTransitionWitness`/`closedVoteWitness`/`closedRecoveryWitness` (mirroring the kernel `closedCheckpoint`/`closedTransition`/`SIG_ENV`/`CHECKPOINT_BODY`/`TRANSITION_CLAIM`/`VOTE_CLAIM`) BEFORE any keyId/verify/id/mint; the uniqueness builder aligned to the kernel `VOTE_CLAIM`. With a closed claim + binding, a uniqueness quorum cannot split (conflict is real only in recovery, where the payload differs — the M5 conflict-determinism tests moved there). conformance 538/0; byte 71, arc 65; reference-checker 59/0, fuzz 4007 clean. **divergence_2 overturned — the sweep I owed in rev41.** |
| **rev41** | 33 | **the public receipt ADMISSION now applies the kernel's closed typed ADT; issuer_id read from the admitted snapshot (round-33: 2 P0, both divergences overturned)** — rev40 mirrored the kernel only at the ORDER COORDINATE, but the FULL public-vs-kernel evidence judgment still SPLIT. **P0-01:** `verifyEvidenceReceipt` validated loosely — `facts` merely "an object" (extra signed fields allowed), `issued_at` only `RFC3339Z.test` (shape, so `9999-99-99T99:99:99Z` passed), no registered-`proof_kind`, no exact per-kind facts. A receipt the KERNEL rejects (`decodeRec(FACTS_SCHEMA[kind])` / `bad:issued_at`) minted a BRANDED VerifiedEvidence handle → corroborated freshness (reproduced live). The brand is a capability certificate for the WHOLE receipt, so consumer-side typing cannot repair it — **admission typing is required** (divergence_2, conceded). Fix: `verifyEvidenceReceipt` applies the SAME CLOSED TYPED ADT as the kernel `closedReceipt` — exact envelope `{ claim, issuer_id, sig }`, typed sig wrapper, exact+typed claim (real-calendar `issued_at` = `isRealRfc3339Z`/`pRFC`), registered `proof_kind`, exact per-kind facts. **P0-02:** the seam re-read raw `receipt.issuer_id` after admission (R3 violation a two-face Proxy split). Fix: read `R.issuer_id` from the admitted snapshot; a whole-surface sweep confirmed every other `admitDeep` site reassigns its argument — this was the one residual. conformance 534/0; byte 71, arc 65. **Both divergences (mirror-complete / consumer-typing-sufficient) overturned — same class as rev40, closed one layer deeper at admission.** |
| **rev40** | 32 | **the public evidence-order path unified with the kernel's closed decode; the Horn trace pinned to the canonical projection (round-32: 1 P0 + 1 P1 + 1 P2, all class-closed)** — the temporal-order predicate had **two decoders**. The reference KERNEL (`checkAuthorityProof`, the independent second derivation) types facts through a CLOSED per-proof-kind ADT (`FACTS_SCHEMA`/`ORDER_COORD`/`orderSemantic`): a position coordinate read ONLY from the kind's authorised fields (`pow-header-chain`→substrate/position, `transparency-log`→log_id/index), proof-kind-namespaced; a `rfc3161-tsa` a SAME-CLOCK pair of REAL calendar instants with `not_before ≤ not_after`. But the PUBLIC path (`deriveCheckpointFreshness → compareEvidenceOrder`) never applied it — it read generic `substrate`/`position`/`not_before`/`not_after`, so a **cross-kind receipt** (`transparency-log` wearing `{substrate, position}`), a **non-calendar** string (`9999-99-99T99:99:99Z`, shape-valid), an **inverted interval**, or a **cross-clock** pair minted `proven-after`, lifting corroborated freshness (reproduced live). **P0 fix:** the public `compareEvidenceOrder` now mirrors the kernel's decode EXACTLY (same closed kind set, same coordinate fields, `isRealRfc3339Z` = the kernel's `pRFC`, same proof-kind/clock namespacing) — the two INDEPENDENT derivations agree; the kernel does its OWN inline magnitude compare and no longer delegates its verdict to the producer. **P2 fix:** the Horn trace contradicted `projectTier` (a `pinned` identity mapped to `name-bound` → derived `TierHIGH` while `projectTier` returns `LIGHT`); `pinned` now maps to a `key-pinned` atom that lifts no Tier, and a grid check pins every derived `Tier*` to `projectTier` over the whole identity×time lattice. **P1 conceded:** the byte-vectors are a portability/regression oracle, not the semantic root (spec + independent impl + external pinning + human review are). conformance 526/0; byte 71, arc 65. |
| **rev39** | 31 | **the nested-input class closed at ONE control, not per resolver (round-31 completion)** — rev38 deep-admitted the 3 nested docs the audit named, but a self-sweep found the same shape (`admitOpts` is shallow → a nested untrusted DATA object in `opts`/`config` survives) in **~8 resolvers**. Point-fixing found instances is the recurring failure. Closed at the ONE boundary: **`admitOpts` now DEEP-admits every nested value** — a function stays a capability (`fetchImpl`/`substrateVerify`); a verifier-MINTED branded token (anchor/served/fresh/handle, uncounterfeitable) is passed through by `admitDeep`; every other nested object/array is frozen inert. No resolver — patched or not, present or future — can hold a live nested caller object to re-read after verification. Verified on FOUR resolvers never individually patched (`verifyNoForkEvidence`/`verifyCheckpointRecovery`/`verifyEpochTransition`/`verifyActiveGenesisUniqueness`: nested genesis now read ≤1); the grid covers 7 nested positions + an R3 ONE-CONTROL closure. conformance 518/0. **Round-31 now fully closed (3 P0 + P1 + P2) at the class level, not the instance level.** |
| **rev38** | 31 | **R3 spans the NESTED input graph; honest bound on the R2 self-proof (round-31 P0-01/02/03 + P1-01/P2-01)** — R3 was STILL realized only at the obvious point: rev35 admitted each resolver's PRIMARY argument, but `admitOpts` is a SHALLOW admission (it preserves function capabilities), so the untrusted DATA objects NESTED in `opts`/`config` stayed live. `resolveAuthority` verified `genesis` via `resolveKeys` then RE-READ the raw nested genesis for capacity → false `VALID:HIGH` with an unsigned `max_partitions`; `verifyStream` verified then re-read the raw nested `checkpoint`; `deriveCheckpointFreshness` verified the chain then re-read raw `chain[last].body` for scope/sequence — three false HIGH/corroborated outputs on a two-face Proxy (all reproduced live). Fix: EVERY untrusted DATA object a resolver verifies is DEEP-admitted ONCE and read only from the frozen snapshot, primary arg OR nested; the read-count grid now covers the nested positions. **Honest correction (P1-01):** the rev37 in-process enforcement closes the forged-MANIFEST defect (a registered check RAN + returned truthy) but does NOT prove ADVERSARIAL SEMANTICS — a same-commit weakening to `check(id, true)` stays green; label membership is a drift control, the SEMANTIC root is the immutable byte-vectors (input bound to expected result) + independent review. The model now claims only what each layer proves. **(P2-01):** a signature-registry fixture (`verifyKeylogTerminality`) short-circuited before the hostile position — replaced with a real key-log head so every declared position is reached. **Round-31: 3 P0 + P1 + P2; both my divergences conceded (I over-claimed R3-complete and R2-proof).** |
| **rev37** | 30 | **R2's proof of execution is observed IN-PROCESS, not read from a forgeable artifact (round-30 P1-02)** — the rev34 R2 fix (bind the executed manifest to `sha256(conformance.mjs)+sha256(index.mjs)`) was still insufficient: the source digest proves which source the manifest CLAIMS to describe, but the `checks` array is caller-authored data — an attacker who DISABLES a registered check, RECOMPUTES the two source hashes, and keeps the old array forges a fresh, internally-consistent manifest the standalone gate accepts (reproduced). A committed artifact can never be its own proof of execution. Fix: the lockstep validation runs in the SAME process that ran the checks, over the IN-MEMORY executed set — a disabled registered check is simply ABSENT, so the run hard-fails and exits non-zero (verified: dropping the R3 RESOLVER check → conformance exits 1, `LOCKSTEP IN-PROCESS … → R35-r3-resolver`). The committed manifest is kept only as a human/drift artifact, no longer the proof. Completes the R2 lesson from rev34: a gate that proves execution must OBSERVE it, not trust an artifact that names it. **Round-30 fully closed (2 P0 + 2 P1); the controller (R1–R4) now holds across the whole surface, its faculty, and its self-proof.** |
| **rev36** | 30 | **R4's faculty is a monotonic ELAPSED clock, not a wall clock behind a wrapper (round-30 P1-01)** — the rev33 R4 fix was still wrong at the FACULTY: the whole-op witness budget was `Date.now()` behind a non-decreasing WRAPPER (clamp a backward step to its last value) — exactly the "wall-clock value with a monotone wrapper" F.9 forbids. On a backward step (an NTP correction jumping forward then back; a test rollback) the wrapper FROZE time at `_last`, so the operation deadline was never reached, the aggregate budget DISAPPEARED, and a slow connector confirmed a served-list HIGH a forward clock would refuse (reproduced live: the rollback clock froze `witnessNow` at 1000). Fix: not a better wrapper but the RIGHT source — `performance.now()`, a MONOTONIC elapsed-time clock (non-decreasing by construction, immune to NTP/wall-clock correction). It cannot go backward → needs no wrapper → cannot freeze; the budget is elapsed against an op-local start, never a `Date.now()` deadline. Also fixed a scale-mix (one deadline comparison in the substrate router still used `Date.now()` against the `performance.now`-scale deadline). Machine-checked: the production clock is non-decreasing across reads. _(round-30 P1-02 — manifest execution-proof — tracked for rev37.)_ |
| **rev35** | 30 | **R3 spans the whole controller, not just `verify` (round-30 P0-01/P0-02)** — the rev32 R3 fix was INCOMPLETE: it closed `verify`'s own path but the RESOLVERS — themselves verifier operations — called `verify(x)` then RE-READ the raw `x`. `resolveKeys` verified its genesis then read `genesis.state`/`contentHash(genesis)` off the original object (8 raw reads); the provenance walk verified `refDoc` then recursed through raw `refDoc.state`. A stateful Proxy showed the SIGNED face to the admission and a different signed face to the re-reads → `resolveKeys` emitted keys for a genesis `verify` never vouched for, and a missing nested referent was falsely reported `verified` (both reproduced on live code). Fix: every resolver ADMITS its untrusted input ONCE at its door and operates only on the frozen snapshot (`resolveKeys` genesis + each resolved referent `admitDeep`'d once; `verify` re-admits idempotently; every read and recursion is over the snapshot). Machine-checked by extending the input-boundary grid to the resolver surface (a raw re-read fires the read-counting getter ≥2 — `resolveKeys` read `state` 8× before) + an adversarial closure. Lesson: a controller rule realized only at the obvious entry is not realized. _(round-30 P1-01 clock-rollback / P1-02 manifest-execution — tracked for rev36/37.)_ |
| **rev34** | 29 | **R1/R2 self-verification — the gates that PROVE the controller are themselves machine-grounded (round-29 P1-01/P1-02)** — the audit showed the GATES proving R1 (admission total) and R2 (enforcement) rested on unsound proxies — the controller discipline broken at the META level, where a gate trusts its own un-formalized input. (P1-01) the totality sweep used `fn.length` for the arity (stops at the first default — `resolveCadence.length===1` though it takes 4 args) and `{}` for the other args (short-circuits before the hostile position — `verifyAnchor` returns early on a bad proof), so "hostile in ANY position" was never exercised: `resolveCadence(_,_,_,hostile)` and `verifyAnchor(hostile,validProof)` threw host exceptions. Fixed with a machine **SIGNATURE REGISTRY** — real arity + a valid reachability fixture per position, so the Proxy is actually reached with the other args valid; a new surface export fails until declared. Both gaps are now total at their door, and **`witnessNoFork`** (exported, takes an untrusted body, its verdict gates the served-list basis) is reclassified **consumer surface + made total** (div1 adopted). (P1-02) the lockstep gate trusted a committed manifest without binding it to the code it describes — a disabled check with an un-regenerated manifest passed. Fixed by making the manifest **content-bound to its source**: it carries the sha256 of `conformance.mjs`+`index.mjs`, and the gate recomputes and rejects a stale manifest at the SHIPPED gate itself (not by CI order), exactly as a UST receipt binds to the state it attests. Principle: a gate that proves a rule must MACHINE-VERIFY it self-containedly — reach the real code path, bind evidence to source. **Round-29 fully closed (2 P0 + 2 P1); both my divergences conceded.** |
| **rev33** | 29 | **R4 — the verifier's faculties are its own, never caller-supplied (round-29 P0-02)** — F.9 puts every verifier in the pair `(ℐ_v, ρ_v)`: the resource budget belongs to the VERIFIER. The code violated it — the witness-budget clock was a public `opts.__nowMs` field, so a caller could pass a **non-monotonic** clock through the DATA PATH that rewinds the deadline, expands the leaf timeout, lets a slow connector return a final receipt, and flips `INDETERMINATE(resource_limit)` into a served-list `VALID:HIGH` (reproduced on live code; the "self-limits, mints no trust" claim was false). Fix: the input may carry only a formalized POLICY that TIGHTENS `ρ_v` (the scalar `maxWitnessOpMs`), never the MECHANISM. The clock moved to an **internal module** outside the package's public API (a wire caller passing a document cannot reach it), is **monotone-guarded** (a backward source is clamped), and the per-leaf timeout is bounded independently. A hostile `__nowMs` in public opts is now DEAD (never read) — same document, same tight budget, adversarial clock → the SAME resource-limited verdict. The conformance harness drives the clock through that internal module, which also retires the wall-clock CI flake at its root (no public seam). _(round-29 P1-01 / P1-02 — totality arg-coverage, manifest source-binding — tracked for rev34.)_ |
| **rev32** | 29 | **the controller invariant — single admission, non-bypass output (round-29 P0-01)** — a round-29 audit showed the whole "`admitDeep` byte-transparent to canon" target was WRONG: it tries to make the admission a faithful MIRROR of the raw input so a reader may consult either, but for a stateful `Proxy` a faithful mirror is IMPOSSIBLE — `canon` reads a value through `[[Get]]` while `admitDeep` read it through the DESCRIPTOR, and a Proxy answers one value to the descriptor (signed) and another to `[[Get]]` (tampered). `verify` vouched for the descriptor face while `contentHash` addressed the `[[Get]]` face → a **false VALID** for data the consumer never had. Fix is the **non-bypass** rule, not a perfect mirror: (R1) one admission into an inert frozen `x̂`, then `x` is dead; (R2) verification is a function of `x̂` + the verifier's own faculties; (R3) every emitted quantity is a projection over `x̂`, nothing re-reads `x`. Realized: `admitDeep` snapshots each value through canon's OWN `[[Get]]` channel (`v[k]`, not `descriptor.value`) — a Proxy's tampered `[[Get]]` face is what gets verified and FAILS the signature → **INVALID**; and `verify` **emits `id(x̂)`** (the admitted snapshot's content hash) so a consumer addresses the transcript by the returned id, never by re-hashing a mutable object. A declared accessor is still rejected at its descriptor (round-26 TOCTOU holds). "byte-transparent to canon" is kept only as core hygiene. _(round-29 P0-02 / P1-01 / P1-02 — clock-as-faculty, totality arg-coverage, manifest source-binding — tracked for rev33+.)_ |
| **rev31** | 28 | **the boundary proven, not asserted** — round-28 disproved the rev30 canon-transparency claim three ways; all closed at the primitive + PROVEN by tests. (P0-01) `admitDeep` **DROPPED** enumerable keys named `__proto__`/`constructor`/`prototype`, so an attacker could attach an **unsigned** member under those names and still get `VALID` (a false accept, looser than canon). Now `admitDeep` traverses EXACTLY canon's domain — `Object.keys` (enumerable own string names INCLUDING those three as own data → the exact-key grammar REJECTS the extra member), array HOLES preserved (was densified), non-plain protos rejected. A **differential FUZZ** (3000 random inputs) proves `admitDeep` never looser than canon + byte-identical when accepted (P1-01). (P1-02) coverage is now enumerated **FROM the module exports** (not a hand-list) — it caught `resolveAuthority`/`resolveCadence`/`verifyAnchor`/`noEventBacking`/`verifyAuthorityBundle` **throwing host exceptions** on a hostile Proxy; all now admit at their door (`verifyAnchor` split into a public door + `verifyAnchorCore`), returning structured (I4 totality). (P1-03) the model-lockstep gate now verifies each registered check against the **EXECUTED-check manifest** conformance emits — a disabled/renamed check no longer passes on source-substring presence. _Follow-up (same rev): killed the recurring witness-budget CI flake at the ROOT — the whole-op budget clock is now injectable (`__nowMs`, default `Date.now`), so conformance drives budget-exhaustion deterministically (consulting a leaf flips the clock past the deadline) instead of racing wall-clock jitter; the seam governs ONLY the consumer's own resource budget, never a security time source (it cannot flip a verdict or hide a fork)._ _Follow-up (div1, adopted from the round-28 audit): the input-boundary grid now AWAITS each verifier to settlement before counting getter reads — a synchronous read-count would miss an async verifier that re-reads a caller field after an await; a negative control proves the mechanism observes such a post-await re-read (count 2)._ |
| **rev30** | 27 (self-audit) | **the boundary primitive made provably canon-transparent + two more surfaces closed.** A meaningful self-audit before the round-28 bundle probed `admitDeep` against `canon` on hard edges and found: (a) a **depth-64 cap that FALSE-REJECTED a valid deep `class:data` doc** canon accepts (a real regression rev29 would have shipped) — removed (canon has no depth cap; cycles stay refused by the visited-set, pathological depth overflows the stack in both and is caught); (b) `resolveByDiscovery` read the discovery **shard off the LIVE doc** while `verify` admitted a separate snapshot — a getter could fetch one domain's genesis/witness but verify another; now it admits at its own door. `admitDeep` is exported as THE input-boundary primitive and locked by a **canon-transparency vector** (`canon(admitDeep(x))` is byte-identical to `canon(x)` when accepted, and admitDeep is never looser than canon — the soundness linchpin, now tested, not assumed). The input-boundary grid gained `resolveByDiscovery` + `verifyAsync` rows. |
| **rev29** | 27 (structural close) | **ONE controlling input boundary + the coverage gate that enforces it.** Pre-bundle structural review found rev28 repeated the recurring weakness: `admitDeep` hand-placed at ~12 entries (a footgun), its canon-transparency untested, and `verify()` itself an 8–12× re-read TOCTOU I had reverted and rationalized. Fixed at the root: (1) **`admitDeep` is canon-EXACT** — it REJECTS a function/symbol value exactly as `canon()` throws on one, so `canon(admitDeep(x))` is byte-identical to `canon(x)` and the snapshot can never flip a verdict on the edge (an earlier DROP diverged and was the hidden cause of the forkChoice breakage). (2) **`verify` split into a public door + `verifyCore`** — the public `verify`/`verifyAsync` admit the untrusted doc ONCE (`admitDeep`; a getter → `E-MALFORMED`), internal machinery (the substrate-receipt identity shim, forkChoice) calls `verifyCore` and trusts — closing verify's own TOCTOU without breaking the internal coupling. (3) **the input-boundary GRID** — a conformance control that enumerates every exported verifier from CODE and asserts each admits its input (a read-counting getter fires ≤1 time; a TOCTOU re-read ≥2 fails). This answers "did every entry admit?" in CI, not from memory — it would have caught round-27 P0-01/02/03 before shipping. |
| **rev28** | 27 | **the WHOLE authority graph crosses ONE snapshot boundary + the lockstep gate made sound.** Round-26 rev24 put the inert `admitDeep` snapshot on the evidence/genesis-context entries but NOT the chain verifier — GPT round-27 found the SAME getter-TOCTOU class on three adjacent public surfaces (a class-level miss, not a patch failure). Now `verifyAuthorityCheckpointChain` snapshots the complete `(chain, config)` at entry (branded handles pass through; a getter/accessor at any depth → `E-MALFORMED`), and `resolveCheckpointRoots` / `verifyCheckpointRecovery` / `verifyEpochTransition` / the uniqueness+stream verifiers each snapshot their primary input — so a getter can no longer sign one checkpoint/genesis/recovery body and mint another (**P0-01/02/03**). `admitDeep` now DROPS function/symbol values (a keylog `prove` closure is legal) and passes branded handles through. **P1-01:** the witness budget is checked AFTER every awaited leaf (a final-leaf exhaustion was misreported as `pending`) and `combineSubstrates` stops starting plugins past the deadline (no detached work). **P2-01:** only an ABSENT `maxWitnessOpMs` selects the default; `0`/`-1`/`NaN`/`Infinity`/fractional are refused. **P1-02:** the `model-lockstep` gate was itself a lockstep-lie — an English-token regex over `(rev…` blocks, trivially bypassed (an untagged note, a positive check named "…never…"). Rebuilt as a structured DATA registry (`tools/lockstep-registry.json`): each record ties a model claim to its exact public-entry adversarial check; the gate cross-checks every `**Realization` citation against the registry — no token inference. |
| **rev27** | 26 (step 4 of 4 + enforcement) | **policy through the call graph + the lockstep gate.** `resolveByDiscovery` (the principal public entry) now THREADS the consumer's `maxWitnessOpMs` into `witnessNoFork` — rev23 wired the budget only at the leaf, so the verifier's `ρ_v.time` policy was **unreachable** through the real API and a witness could upgrade assurance to a served-list HIGH after the consumer's declared budget expired (**P1-03**). Closes **L4** with a public-entry adversarial check (a tight budget resource-limits the witness → no false HIGH). **New CI gate `test:model-lockstep`:** every `**Realization (rev…` enforcement note in the formal model must cite an ADVERSARIAL-closure conformance check, not just a happy-path one — the mechanism that hid P0-01/02/03 behind a green board is now itself gated. **Round-26 complete: all 3 P0 + 3 P1 closed structurally at their choke points, all 5 lockstep-lies (L1–L5) closed, each by a public-entry adversarial check.** |
| **rev26** | 26 (step 3 of 4) | **scalar decoder sweep — the CanonicalSeq class completed.** The rev23 `isSeq` guard reached the sequence coordinates but not every signed scalar; this rev routes the last three through it: the key-log **length** (`BigInt(["1"]) === 1n` coerced an array → now `isSeq` before `BigInt`), the **Merkle proof index** (`String(hp.index)` → `isSeq` + `BigInt` compare), and the stream **frame_count** (`String(a.frame_count)` → `isSeq`). A coercible `["N"]` on any of them no longer decodes to the canonical value. Class E (policy threading through the call graph + the `model-lockstep` gate, L4) is the last step — AUDIT-PLAN. |
| **rev25** | 26 (step 2 of 4) | **one authority root.** A branded `context` (`GenesisHandle`) supplied to `verifyAuthorityCheckpointChain` / `deriveCheckpointFreshness` is now the SOLE root: the entire raw authority-root family (`pinnedPrior`, `genesis`, `genesisAuthority`, `recoveryKeys`, `recoveryThreshold`) alongside it is rejected `E-AUTHORITY`. Closes **P0-01** (a foreign `pinnedPrior` seized the chain scope/authority while still reporting `verified-context`) and **P0-02** (raw recovery INJECTED when the context carried none). Two formal-model **lockstep-lies** closed with public-entry adversarial checks: M2 "every downstream layer takes the derived context, **never raw fields**" (L1) and F.5l recovery "immutable within the epoch" (L2) — the positive C1 test never exercised the mixed-configuration attack. Classes B (scalar decoder) and E (policy threading + the `model-lockstep` gate) remain — AUDIT-PLAN. |
| **rev24** | 26 (step 1 of 4) | **the ONE input boundary** — round-26 found the recurring classes on new surfaces; this rev closes the two that share a seam. `admitDeep` takes a single inert DEEP snapshot of a caller object at entry: every value is read ONCE, so a getter cannot return the signed value during verification and an unsigned value during handle construction (the **getter-TOCTOU** that minted a genuine `EvidenceHandle` / genesis context with unsigned facts — P0-03; `verifyEvidenceReceipt` + `verifiedGenesisContext` snapshot at entry) — and an accessor / non-plain proto / cycle is structured-rejected, so **malformed non-null totality** extends past the null matrix to trailing args (`verifyJson` opts, `resolveCadence` 4th arg, `witnessNoFork` opts). Two formal-model **lockstep-lies** closed with PUBLIC-entry adversarial checks: M3 "preserves the exact verified facts" (L3) and I4 "TOTAL" beyond null config (L5). Classes A (one authority root), B (scalar decoder) and E (policy threading) are the next revs — tracked in the binding audit plan. |
| **rev23** | 25 | the recurring classes on the surfaces round-24 hadn't reached: the exported `provePredicates` was a **public brand-minting oracle** (it minted the `PredicateGraph` brand from caller-shaped labels → `deriveAssurance` blessed TOP with zero verified evidence) — now it is the UNBRANDED pure mapper and only `verify()` (a module-private seal) mints the brand; a verified-genesis context's **recovery roots/threshold can no longer be overridden** by raw config (E-AUTHORITY); `deepFreeze` now uses a **visited-set** so a shallow-frozen subtree inside an EvidenceHandle is fully frozen; the TCB structural registries (`ASSURANCE_AXES`/`REGISTRY`/`EVIDENCE_CAPS`/`RULE_CONTRACTS`) are **deep-frozen** (a nested array could change tier ranks in-process); **one CanonicalSeq decoder** (`isSeq`) at every signed sequence coordinate — a coercible `["0"]`/`["1"]` no longer attests uniqueness / authorizes a recovery / seeds an epoch, and a transition now REQUIRES the verified prior-chain final sequence (no partial verification); **malformed non-null totality** extended past the null matrix (a numeric-extra claim, a null proof, a null seam arg, and a non-binary `verifyJson` all return structured verdicts, never a host throw); the whole-operation witness budget is now `min(reference 30 s, consumer deadline)` (ρ_v belongs to the verifier, F.9) |
| **rev22** | 24 | every recurring class on the surfaces it had never reached: branded handles **deep-frozen** (a nested authority could be mutated after verification → forgery); signed genesis capacity/recovery fields decoded by a **canonical uint** decoder (`["4096"]`/`"0x80"` no longer coerce via `Number()`); quorum trust-domains reject **lone surrogates**; **null-totality** swept across 9 public proof surfaces (+2 the self-audit found: `deriveCheckpointFreshness`, `verifyStream`); the signed epoch `from_sequence` is checked; `evidenceCaps` returns a **frozen copy**; a **whole-operation** connector deadline (per-leaf alone let a legal witness burn ~21 min) |
| **rev21** | 23 | the recurring class applied to the **quorum** surface it never touched: object trust-domains faked an independent quorum (now NFC-string admission), the M5 result was arrival-ordered (now sorted voters/tags), and quorum functions threw on `null` config (now `admitOpts`). `forkChoice` order key made total via `JSON.stringify` (canon could throw on proof extras → arrival order); every diagnostic array tie-broken by full record. `combineSubstrates`/`anchoredByProofs` bound each connector by a deadline so a never-settling plugin can't block forever |
| **rev20** | 22 | the rev18/19 witness fix *still* projected before UNION (it filtered `superseded_by` before grouping — an active+superseded rival hid its anchor) and used concat not set-union; now the equivalence class is grouped by `content_hash` first, status is reconciled (active+superseded = contradiction, fail-closed), and proofs are set-unioned by canon. `combineSubstrates` isolates a throwing plugin. `forkChoice` returns a deterministic full document (min-canon winner + sorted diagnostic arrays) so the same set in any order emits byte-identical output |
| **rev19** | 21 (self-catch) | the rev18 P1-02 `forkChoice` content-hash dedupe was itself *lossy* — `content_hash` covers state, not `proof`, so a candidate with a valid anchor proof was dropped behind a same-state one with an invalid proof, hiding an equivocation; dedupe removed (the candidate budget alone caps the fan-out) |
| **rev18** | 21 | the rev17 witness fan-out fix was *lossy* — first-wins dedupe + `slice()` truncation discarded a rival's anchor then minted HIGH; now the served list is UNIONed (no `anchor`/`anchors` shadow) and an over-budget input is REFUSED (`resource_limit`), never truncated; witness connector throws are caught (unavailable, not a host exception); `forkChoice` candidate budget + content-hash dedupe |
| **rev17** | 20 | witness crosses the same raw-byte dup-member boundary as genesis/key-log; null-proto `admitOpts` (`__proto__` injection); F.9 structural fan-out budget; `forkChoice` admits opts |
| **rev16** | 19 | `forkChoice` snapshot-before-every-read (no live fallback); substrate closed-ADT own-data; ONE Unicode byte-admission shared with the byte checker |
| **rev15** | 18 | `forkChoice` snapshot-through; ONE substrate decoder; present≠absent key-log → `resource_limit`; total-for-null; strict-UTF-8 discovery |
| **rev14** | 17 | snapshot-before-await; proven-anchor token `U`; typed substrate leaf; total resolvers; bounded discovery |
| **rev13** | 16 | proven-`U`-only `K_n(t)`; earned key-log freshness; nondecreasing key-log timeline |
| **rev12** | 15 | identity relevance to subject; two-sided `K_n(t)` intervals; reducer-as-TCB-unit; ceiling-before-copy |
| **rev11** | 14 | monotonic compromise; canonical order coordinate; total resource bounds |
| **rev10** | 13 | byte injectivity (BOM/surrogate); real calendar values; reference budget |
| **rev9** | 12 | own-key membership + value validity (prototype-safe typed decode) |
| **rev8** | 11 | typed leaf decoders — key-closure is not value-typing |
| **rev7** | 10 | typed decode carried to every leaf; inner signed objects closed |
| **rev6** | 9 | total closed config ADT + a witness ADT per kind |
| **rev5** | 8 | total decode boundary over witnesses / config / limits |
| **rev4** | 7 | M-DEC / M-KEY / M-ORDER / M-CONFIG / M-BYTE total over their domain |
| **rev3** | 3–6 | the byte-boundary TCB: `checkAuthorityProofBytes` + exact Term ADT + `RULE_CONTRACTS` + language-neutral byte vectors |

### Milestones

- **LIGHT** — the byte-verdict floor · **done**
- **HIGH** — name-binding authority · **done**
- **TOP** — anchored time · protocol done, operator wiring pending
- **Formal model** — assurance product-lattice · human + machine audit pending
- **Chains** — state linked by provenance · in progress
- **Tools & surfaces** — MCP / CLI / web-signer / connectors · shipped

See [milestones](https://github.com/thelabmd/UST-Protocol/milestones) for the live status.

### Supply chain

- **Zero third-party dependencies by default.** `ust-protocol`, `ust-rekor-verify`, `ust-web-signer` and `ust-lite`
  are dependency-free. `ust-ots-verify` (`rc.9`) moved `opentimestamps` from `optionalDependencies` (which npm
  auto-installs, pulling the deprecated `request`/`bitcore-lib` tree, ~87 packages with CVEs) to an **optional peer**
  — it is lazily loaded only if the operator installs it, so a default install is dependency-free.
- **`@ust-protocol/cli` (`rc.41`) no longer shells out via `npx`.** The `ust publish cf` ceremony calls a locally
  installed `wrangler` (declared as an optional peer) rather than `npx wrangler`, which would download-and-run it
  ad-hoc. `gh` remains a documented external system tool. No package has install scripts.
- `@ust-protocol/mcp` requires the official `@modelcontextprotocol/sdk` — that (and only that) surface carries a
  third-party tree, by necessity.

## Earlier (rc.6 → rc.36)

- **Assurance product-lattice** — the measure-theoretic model; the tier as one monotone policy projection (#76, #78).
- **Authority-checkpoint** normative wire specification (#77).
- **HIGH / TOP evidence layer** — witness no-fork as the honest default; substrate anchors (Bitcoin/OTS, Rekor) with fabricatable-proof overclaims closed (#68, #69, #70, #71).
- **Bounds earned by ceremony** — the §13 size / capacity / resource ladder; canonical size semantics (#59, #61, #63).
- **Downgrade-resistance + fork-choice** — canonical = anchor-included (#45); agent-safety SDK patterns (#44).
- **Identity & portability** — IDN/homograph `domain_shard`, key-log freshness/revocation; the JCS canonicalization trap (#40, #41).
- **Negative / absence observation** — signing "nothing happened" / "source was down" (#39).

[milestones]: https://github.com/thelabmd/UST-Protocol/milestones
